load("@aspect_rules_js//js:defs.bzl", "js_binary", "js_library", "js_run_binary")
load("@npm_ducktape//:defs.bzl", "npm_link_all_packages")
load("//devinfra/js:spa_bundle.bzl", "spa_bundle")
load("//util/testing/frontend_visual:visual_test.bzl", "visual_test")

npm_link_all_packages(name = "node_modules")

# Hermetic font assets bundled with both the production frontend and the
# visual-regression test harness. The two woff2 files are latin-subset
# variable fonts (Playfair Display v40 / Outfit v15, OFL-licensed); fonts.css
# wires them into @font-face declarations the React component expects. The
# files live under public/ so Vite copies them to dist/ verbatim during the
# production build.
filegroup(
    name = "casino_fonts",
    srcs = [
        "public/fonts/Outfit-latin.woff2",
        "public/fonts/PlayfairDisplay-latin.woff2",
        "public/fonts/fonts.css",
    ],
    visibility = ["//study_casino:__subpackages__"],
)

# ── Per-file source libraries ──────────────────────────────────────────────────

js_library(
    name = "sync",
    srcs = ["sync.js"],
    deps = [
        ":node_modules/react",
        "//study_casino/frontend/lib:schema_zod",
    ],
)

js_library(
    name = "use_casino",
    srcs = ["use_casino.js"],
    deps = [
        ":node_modules/react",
        ":shared",
        ":sync",
    ],
)

js_library(
    name = "use_url_state",
    srcs = ["use_url_state.js"],
    deps = [":node_modules/react"],
)

js_library(
    name = "sync_icon",
    srcs = ["SyncIcon.jsx"],
    deps = [
        ":node_modules/react",
        ":sync",
    ],
)

js_library(
    name = "shared",
    srcs = ["shared.jsx"],
    deps = [":node_modules/react"],
)

js_library(
    name = "study_view",
    srcs = ["StudyView.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "roulette",
    srcs = ["Roulette.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "slots",
    srcs = ["Slots.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "blackjack",
    srcs = ["Blackjack.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "prizes_view",
    srcs = ["PrizesView.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "stats_view",
    srcs = ["StatsView.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
        ":sync",
    ],
)

js_library(
    name = "admin_view",
    srcs = ["AdminView.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
        ":sync",
    ],
)

js_library(
    name = "casino_stats_view",
    srcs = ["CasinoStatsView.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
        ":sync",
    ],
)

js_library(
    name = "changelog_modal",
    srcs = ["ChangelogModal.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "streak_strip",
    srcs = ["StreakStrip.jsx"],
    deps = [
        ":node_modules/react",
        ":shared",
    ],
)

js_library(
    name = "study_casino",
    srcs = ["study_casino.jsx"],
    deps = [
        ":admin_view",
        ":blackjack",
        ":casino_stats_view",
        ":changelog_modal",
        ":node_modules/react",
        ":prizes_view",
        ":roulette",
        ":shared",
        ":slots",
        ":stats_view",
        ":streak_strip",
        ":study_view",
        ":sync_icon",
        ":use_casino",
        ":use_url_state",
    ],
)

js_library(
    name = "main",
    srcs = ["main.jsx"],
    deps = [
        ":node_modules/react",
        ":node_modules/react-dom",
        ":study_casino",
    ],
)

# ── Production bundle (esbuild) ────────────────────────────────────────────────
#
# esbuild bundles main.jsx → dist/main.js; index.html (referencing /main.js) and
# the public/ tree (manifest, icon, fonts) are copied in alongside. The .js files
# carry JSX, so the loader treats them as such. Served verbatim by the FastAPI
# app's StaticFiles mount.

spa_bundle(
    name = "bundle",
    entry_point = "main.jsx",
    jsx = "automatic",
    loader = {".js": "jsx"},
    public = glob(["public/**/*"]),
    visibility = [
        "//study_casino:__pkg__",
        "//study_casino/tests:__pkg__",
    ],
    deps = [":main"],
)

# ── Visual render-health tests ───────────────────────────────────────────────
#
# Renders the StudyCasino component with mocked state and publishes the
# screenshot for PR visual review (publishOnly — no checked-in baselines).
# Reuses the shared Puppeteer lib at //util/testing/frontend_visual.
# Casino fonts above are wired in instead of the lib's Inter so the test
# captures the same typography users see in production.

js_library(
    name = "visual_scenarios",
    srcs = [
        "tests/harness/scenarios.mjs",
        "tests/scene_ready.mjs",
    ],
)

js_library(
    name = "visual_harness_lib",
    srcs = ["tests/harness/harness.jsx"],
    tags = ["no-lint"],
    deps = [
        ":changelog_modal",
        ":node_modules/react",
        ":node_modules/react-dom",
        ":shared",
        ":study_casino",
        ":study_view",
        ":sync",
    ],
)

js_binary(
    name = "build_visual_harness",
    data = [
        "tests/harness/esbuild.config.mjs",
        ":casino_fonts",
        ":node_modules",
        ":visual_harness_lib",
        ":visual_scenarios",
    ],
    entry_point = "tests/harness/esbuild.config.mjs",
)

js_run_binary(
    name = "visual_harness_bundle",
    srcs = [
        "tests/harness/esbuild.config.mjs",
        ":casino_fonts",
        ":visual_harness_lib",
        ":visual_scenarios",
    ],
    outs = [
        "tests/harness/dist/harness.js",
    ],
    args = ["tests/harness/dist"],
    chdir = package_name(),
    tool = ":build_visual_harness",
)

# Single-file alias so $(rootpath) resolves to one path the test can read.
filegroup(
    name = "visual_harness_js",
    srcs = ["tests/harness/dist/harness.js"],
)

# Every scenario in tests/harness/scenarios.mjs, swept on one browser per shard. BUILD carries a
# shard count, not a scenario list. Run one with `--test_filter=<scenario>`.
visual_test(
    name = "visual",
    size = "small",
    assets = ["tests/harness/index.html"],
    entry_point = "tests/visual_runner.mjs",
    font_family = "Outfit",
    fonts = ":casino_fonts",
    harness = ":visual_harness_js",
    scenarios = ":visual_scenarios",
    shard_count = 2,
)
