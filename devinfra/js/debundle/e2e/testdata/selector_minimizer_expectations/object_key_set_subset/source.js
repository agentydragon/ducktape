const targetShape = {
  alpha: ref.shared,
  beta: ref.shared,
  gamma: ref.shared,
  delta: ref.shared,
};

const firstSibling = {
  alpha: ref.shared,
  beta: ref.shared,
  gamma: ref.shared,
};

const secondSibling = {
  beta: ref.shared,
  gamma: ref.shared,
  delta: ref.shared,
};

export { targetShape };
