load("//devinfra/js/debundle/skills/shared:defs.bzl", "DEBUNDLE_REFERENCES")
load("//skills:defs.bzl", "skill_mapping", "skill_package")

skill_package(
    name = "debundle_lane_worker",
    contents = [
        skill_mapping(
            srcs = ["SKILL.md"],
            preserve_paths = True,
        ),
    ] + DEBUNDLE_REFERENCES,
)
