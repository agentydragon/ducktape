"""Env scripts sourced by the hook daemon's startup_env_script.

Exported so the hook daemon container E2E test can stage the real scripts
into its test workspace (copies, not Bazel deps).
"""

exports_files(
    [
        "_common.sh",
        "web_env.sh",
    ],
    visibility = ["//devinfra/claude/hook_daemon:__subpackages__"],
)
