"""Env scripts sourced by the Rust hook daemon's startup_env_script.

Exported so the Rust hook container E2E test can stage the real scripts
into its test workspace (copies, not Bazel deps).
"""

exports_files(
    [
        "_common.sh",
        "web_env.sh",
    ],
    visibility = ["//devinfra/claude/claude_hook:__subpackages__"],
)
