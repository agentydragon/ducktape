# Build and push the BuildBuddy RBE worker image to GHCR.
#
# Based on BuildBuddy's rbe-ubuntu24-04 image, with extra deps for Rust, GHC,
# and Chromium headless shell. See tools/rbe_image/Dockerfile.
name: RBE Worker Image

on:
  push:
    branches: [main, devel]
    paths:
      - "tools/rbe_image/**"
      - ".github/workflows/rbe-image.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/rbe-worker

jobs:
  build-and-push:
    name: Build and Push
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: tools/rbe_image
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}
