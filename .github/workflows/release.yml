# AUTO-GENERATED from devinfra/ci/workflows.yaml - DO NOT EDIT DIRECTLY
# Regenerate with: bazel run //devinfra/ci:generate_ci_bin
name: Release
"on":
  push:
    branches:
      - devel
      - main
  workflow_dispatch:
    inputs:
      package:
        description: Package to release
        type: choice
        options:
          - all
          - claude-hooks
          - ducktape
          - headscale-cleanup
          - gterm-theme
        default: all
      force:
        description: Force release even if no changes detected
        type: boolean
        default: false
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false
permissions:
  contents: write
jobs:
  check-releases:
    name: Check releases
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      claude_hooks_needed: ${{ steps.check-claude_hooks.outputs.release_needed }}
      ducktape_needed: ${{ steps.check-ducktape.outputs.release_needed }}
      headscale_cleanup_needed: ${{ steps.check-headscale_cleanup.outputs.release_needed }}
      gterm_theme_needed: ${{ steps.check-gterm_theme.outputs.release_needed }}
    steps:
      - name: Check out code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - id: bazel
        uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Check claude-hooks
        id: check-claude_hooks
        uses: ./.github/actions/check-release-needed
        with:
          package_prefix: claude-hooks
          bazel_targets: //:claude_hooks_wheel
          latest_release_tag: claude-hooks-latest
      - name: Check ducktape
        id: check-ducktape
        uses: ./.github/actions/check-release-needed
        with:
          package_prefix: ducktape
          bazel_targets: //:wheel
          latest_release_tag: ducktape-latest
      - name: Check headscale-cleanup
        id: check-headscale_cleanup
        uses: ./.github/actions/check-release-needed
        with:
          package_prefix: headscale-cleanup
          bazel_targets: //headscale_cleanup:wheel
          latest_release_tag: headscale-cleanup-latest
      - name: Check gterm-theme
        id: check-gterm_theme
        uses: ./.github/actions/check-release-needed
        with:
          package_prefix: gterm-theme
          bazel_targets: //gterm_theme:wheel
          latest_release_tag: gterm-theme-latest
  test-claude_hooks:
    name: Test claude-hooks
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: check-releases
    if:
      always() && !cancelled() && !failure() && (needs.check-releases.outputs.claude_hooks_needed == 'true' || (github.event_name
      == 'workflow_dispatch' && (inputs.package == 'all' || inputs.package == 'claude-hooks')))
    steps:
      - uses: actions/checkout@v6
      - id: bazel
        uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Run tests
        run: |-
          bazel test --keep_going \
            --test_output=all \
            --nocache_test_results \
            --test_env=CI=true \
            //devinfra/claude/...
      - uses: ./.github/actions/collect-test-logs
        if: always()
        with:
          artifact-name: claude-hooks-test-logs
  release-claude_hooks:
    name: Release claude-hooks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - check-releases
      - test-claude_hooks
    if:
      always() && !cancelled() && needs.test-claude_hooks.result == 'success' && (needs.check-releases.outputs.claude_hooks_needed
      == 'true' || (github.event_name == 'workflow_dispatch' && inputs.force == true && (inputs.package == 'all' || inputs.package
      == 'claude-hooks')))
    outputs:
      released: "true"
      tag: claude-hooks-${{ env.SHORT_SHA }}
      short_sha: ${{ env.SHORT_SHA }}
    steps:
      - uses: actions/checkout@v6
      - id: bazel
        uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Set short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV
      - name: Build wheel
        run: bazel build --remote_download_toplevel //:claude_hooks_wheel
      - name: Prepare wheel
        run: |-
          mkdir -p dist
          cp bazel-bin/claude_hooks-*.whl dist/
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: claude-hooks-${{ env.SHORT_SHA }}
          name: claude-hooks (${{ env.SHORT_SHA }})
          body: |-
            claude-hooks wheel for Claude Code integration:

            - `claude-hook` - Unified hook dispatcher for Claude Code web
            - `claude-statusline` - Statusline for Claude Code

            Requires Python 3.13+

            Commit: ${{ github.sha }}
            Branch: ${{ github.ref_name }}
          files: dist/*.whl
      - uses: ./.github/actions/update-latest-release
        with:
          package_prefix: claude-hooks
          latest_tag: claude-hooks-latest
          title: claude-hooks (latest)
          body: |
            claude-hooks wheel for Claude Code integration:

            - `claude-hook` - Unified hook dispatcher for Claude Code web
            - `claude-statusline` - Statusline for Claude Code

            Requires Python 3.13+
          files: dist/*.whl
  release-ducktape:
    name: Release ducktape
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - check-releases
    if:
      always() && !cancelled() && !failure() && (needs.check-releases.outputs.ducktape_needed == 'true' || (github.event_name
      == 'workflow_dispatch' && inputs.force == true && (inputs.package == 'all' || inputs.package == 'ducktape')))
    outputs:
      released: "true"
      tag: ducktape-${{ env.SHORT_SHA }}
      short_sha: ${{ env.SHORT_SHA }}
    steps:
      - uses: actions/checkout@v6
      - id: bazel
        uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Set short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV
      - name: Build wheel
        run: bazel build --remote_download_toplevel //:wheel
      - name: Prepare wheel
        run: |-
          mkdir -p dist
          cp bazel-bin/ducktape-*.whl dist/
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ducktape-${{ env.SHORT_SHA }}
          name: ducktape (${{ env.SHORT_SHA }})
          body: |-
            ducktape wheel containing CLI tools:

            - `git-commit-ai` - AI-powered commit message generator
            - `difftree` - Tree-style visualization of diffs
            - `gmail-archiver` - Gmail email auto-archiving tool

            Requires Python 3.13+

            Commit: ${{ github.sha }}
            Branch: ${{ github.ref_name }}
          files: dist/*.whl
      - uses: ./.github/actions/update-latest-release
        with:
          package_prefix: ducktape
          latest_tag: ducktape-latest
          title: ducktape (latest)
          body: |
            ducktape wheel containing CLI tools:

            - `git-commit-ai` - AI-powered commit message generator
            - `difftree` - Tree-style visualization of diffs
            - `gmail-archiver` - Gmail email auto-archiving tool

            Requires Python 3.13+
          files: dist/*.whl
  release-headscale_cleanup:
    name: Release headscale-cleanup
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - check-releases
    if:
      always() && !cancelled() && !failure() && (needs.check-releases.outputs.headscale_cleanup_needed == 'true' || (github.event_name
      == 'workflow_dispatch' && inputs.force == true && (inputs.package == 'all' || inputs.package == 'headscale-cleanup')))
    outputs:
      released: "true"
      tag: headscale-cleanup-${{ env.SHORT_SHA }}
      short_sha: ${{ env.SHORT_SHA }}
    steps:
      - uses: actions/checkout@v6
      - id: bazel
        uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Set short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV
      - name: Build wheel
        run: bazel build --remote_download_toplevel //headscale_cleanup:wheel
      - name: Prepare wheel
        run: |-
          mkdir -p dist
          cp bazel-bin/headscale_cleanup/headscale_cleanup-*.whl dist/
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: headscale-cleanup-${{ env.SHORT_SHA }}
          name: headscale-cleanup (${{ env.SHORT_SHA }})
          body: |-
            headscale-cleanup wheel.

            CLI tool for cleaning up stale Headscale nodes (controlplane* and worker* nodes).

            Commit: ${{ github.sha }}
            Branch: ${{ github.ref_name }}
          files: dist/*.whl
      - uses: ./.github/actions/update-latest-release
        with:
          package_prefix: headscale-cleanup
          latest_tag: headscale-cleanup-latest
          title: headscale-cleanup (latest)
          body: |
            headscale-cleanup wheel.

            CLI tool for cleaning up stale Headscale nodes (controlplane* and worker* nodes).
          files: dist/*.whl
  release-gterm_theme:
    name: Release gterm-theme
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - check-releases
    if:
      always() && !cancelled() && !failure() && (needs.check-releases.outputs.gterm_theme_needed == 'true' || (github.event_name
      == 'workflow_dispatch' && inputs.force == true && (inputs.package == 'all' || inputs.package == 'gterm-theme')))
    outputs:
      released: "true"
      tag: gterm-theme-${{ env.SHORT_SHA }}
      short_sha: ${{ env.SHORT_SHA }}
    steps:
      - uses: actions/checkout@v6
      - name: Install system dependencies
        run:
          sudo apt-get update && sudo apt-get install -y libcairo2-dev libgirepository-2.0-dev libdbus-1-dev libxcb1-dev
          pkg-config
      - id: bazel
        uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
      - name: Set short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV
      - name: Build wheel
        run: bazel build --remote_download_toplevel //gterm_theme:wheel
      - name: Prepare wheel
        run: |-
          mkdir -p dist
          cp bazel-bin/gterm_theme/gterm_theme-*.whl dist/
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: gterm-theme-${{ env.SHORT_SHA }}
          name: gterm-theme (${{ env.SHORT_SHA }})
          body: |-
            gterm-theme wheel (GNOME Terminal theme follower).

            **Note**: Requires system libraries (libgirepository-2.0-dev, libdbus-1-dev).

            Commit: ${{ github.sha }}
            Branch: ${{ github.ref_name }}
          files: dist/*.whl
      - uses: ./.github/actions/update-latest-release
        with:
          package_prefix: gterm-theme
          latest_tag: gterm-theme-latest
          title: gterm-theme (latest)
          body: |
            gterm-theme wheel (GNOME Terminal theme follower).

            **Note**: Requires system libraries (libgirepository-2.0-dev, libdbus-1-dev).
          files: dist/*.whl
  update-downstream:
    name: Update downstream references
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - release-claude_hooks
      - release-ducktape
      - release-headscale_cleanup
      - release-gterm_theme
    if: |-
      always() && !cancelled() &&
           (needs.release-claude_hooks.outputs.released == 'true' ||
           needs.release-ducktape.outputs.released == 'true' ||
           needs.release-headscale_cleanup.outputs.released == 'true' ||
           needs.release-gterm_theme.outputs.released == 'true')
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.ref_name }}
      - uses: cachix/install-nix-action@v30
      - name: Update references and push
        run: |-
          FAILED_INPUTS=()
          update_flake_input() {
            local input_name="$1"
            if nix flake update "$input_name"; then
              echo "Updated flake input: $input_name"
            else
              echo "::warning::Failed to update flake input: $input_name"
              FAILED_INPUTS+=("$input_name")
            fi
          }
          if [ "${{ needs.release-claude_hooks.outputs.released }}" = "true" ]; then
            sed -i 's|releases/download/claude-hooks-[^/]*/claude_hooks|releases/download/${{ needs.release-claude_hooks.outputs.tag }}/claude_hooks|' flake.nix
            sed -i "s|releases/download/claude-hooks-[a-f0-9]*/claude_hooks|releases/download/${{ needs.release-claude_hooks.outputs.tag }}/claude_hooks|g" .claude/settings.json
            update_flake_input claude-hooks-wheel
          fi
          if [ "${{ needs.release-ducktape.outputs.released }}" = "true" ]; then
            sed -i 's|releases/download/ducktape-[^/]*/ducktape|releases/download/${{ needs.release-ducktape.outputs.tag }}/ducktape|' flake.nix
            update_flake_input ducktape-wheel
          fi
          if [ "${{ needs.release-headscale_cleanup.outputs.released }}" = "true" ]; then
            sed -i 's|releases/download/headscale-cleanup-[^/]*/headscale_cleanup|releases/download/${{ needs.release-headscale_cleanup.outputs.tag }}/headscale_cleanup|' flake.nix
            update_flake_input headscale-cleanup-wheel
          fi
          if [ "${{ needs.release-gterm_theme.outputs.released }}" = "true" ]; then
            sed -i 's|releases/download/gterm-theme-[^/]*/gterm_theme|releases/download/${{ needs.release-gterm_theme.outputs.tag }}/gterm_theme|' flake.nix
            update_flake_input gterm-theme-wheel
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add flake.nix flake.lock .claude/settings.json

          if git diff --cached --quiet; then
            echo "No downstream changes"
            if [ ${#FAILED_INPUTS[@]} -gt 0 ]; then
              echo "::error::Failed to update flake inputs: ${FAILED_INPUTS[*]}"
              exit 1
            fi
            exit 0
          fi

          git commit -m "chore: bump release artifacts [skip ci]"

          BRANCH="${{ github.ref_name }}"
          git pull --rebase origin "$BRANCH"
          git push origin "HEAD:${BRANCH}"

          if [ ${#FAILED_INPUTS[@]} -gt 0 ]; then
            echo "::error::Partial update committed, but failed to update: ${FAILED_INPUTS[*]}"
            exit 1
          fi
