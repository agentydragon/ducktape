# AUTO-GENERATED from tools/ci/workflows.yaml - DO NOT EDIT DIRECTLY
# Regenerate with: bazel run //tools/ci:generate_ci_bin
name: Bazel Harbor Images
"on":
  workflow_dispatch: null
  workflow_call:
    inputs:
      rbe_image:
        description: Override RBE container image (optional)
        required: false
        type: string
        default: ""
    secrets:
      BUILDBUDDY_API_KEY:
        required: false
      HARBOR_ROBOT_USERNAME:
        required: true
      HARBOR_ROBOT_TOKEN:
        required: true
concurrency:
  group: bazel-harbor-images-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  build-and-push:
    name: Build and Push
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-bazel
        with:
          buildbuddy_api_key: ${{ secrets.BUILDBUDDY_API_KEY }}
          rbe_image: ${{ inputs.rbe_image }}
      - name: Login to Harbor
        uses: docker/login-action@v3
        with:
          registry: registry.allegedly.works
          username: ${{ secrets.HARBOR_ROBOT_USERNAME }}
          password: ${{ secrets.HARBOR_ROBOT_TOKEN }}
      - name: Build and push oauth-broker
        run: |-
          .github/scripts/harbor_push.sh \
            //oauth_broker:load \
            oauth-broker:latest \
            registry.allegedly.works/oauth-broker/oauth-broker
      - name: Build and push inventree-token-provisioner
        run: |-
          .github/scripts/harbor_push.sh \
            //cluster/k8s/applications/inventree-token-provisioner:load \
            inventree-token-provisioner:latest \
            registry.allegedly.works/inventree/token-provisioner
      - name: Build and push props-backend
        run: |-
          .github/scripts/harbor_push.sh \
            //props/backend:load \
            props-backend:latest \
            registry.allegedly.works/props/props-backend
      - name: Build and push approval-gate
        run: |-
          .github/scripts/harbor_push.sh \
            //approval_gate:load \
            approval-gate:latest \
            registry.allegedly.works/approval-gate/approval-gate
      - name: Build and push approval-gate-exec-backend
        run: |-
          .github/scripts/harbor_push.sh \
            //mcp_infra/exec:exec_backend_load \
            approval-gate-exec-backend:latest \
            registry.allegedly.works/approval-gate/exec-backend
      - name: Build and push homeassistant-proxy
        run: |-
          .github/scripts/harbor_push.sh \
            //homeassistant_proxy:load \
            homeassistant-proxy:latest \
            registry.allegedly.works/homeassistant-proxy/homeassistant-proxy
