rationale: |
  extract_batch() takes emails as list[tuple[str, str, str, str]] - a raw
  positional tuple with no indication of what each element means (message_id,
  subject, body, received_date). Should use a typed structure (dataclass,
  Pydantic model, or similar) instead of relying on positional indices.
should_flag: true
occurrences:
  - occurrence_id: occ-0
    files:
      gmail_archiver/event_classifier.py:
        - [171, 171]
    note: emails parameter in extract_batch() - tuple of (message_id, subject, body, received_date)
    critic_scopes_expected_to_recall:
      - - gmail_archiver/event_classifier.py
    match_file_restriction:
      - gmail_archiver/event_classifier.py
