load("//props/specimens:defs.bzl", "specimen_targets")

# Snapshot of ducktape at c263d680.
# Included: adgn/, wt/
# Excluded: adgn/src/adgn/props/specimens/
specimen_targets(
    name = "specimen",
    code_srcs = glob(["code/**/*"]),
    slug = "ducktape/2025-11-22-01",
    split = "valid",
    # Measured at ~60s, and `specimen_targets` defaults to `small`, a 60s timeout.
    test_size = "medium",
)
