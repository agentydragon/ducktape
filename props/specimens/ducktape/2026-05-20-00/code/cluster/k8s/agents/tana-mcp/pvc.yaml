apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tana-mcp-config
  namespace: tana-mcp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 1Gi
