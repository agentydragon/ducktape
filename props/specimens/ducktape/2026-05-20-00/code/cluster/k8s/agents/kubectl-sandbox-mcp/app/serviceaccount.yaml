apiVersion: v1
kind: ServiceAccount
metadata:
  name: kubectl-sandbox-mcp
  namespace: kubectl-sandbox-mcp
  annotations:
    description: "SA for kubectl-sandbox-mcp pod. No k8s permissions — server uses token exchange to scope callers to sandbox group."
