apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpap-data
  namespace: cpap-sync
spec:
  accessModes:
    - ReadWriteOnce
  # OpenEBS LVM HDD on wyrm2 — co-located with the WiFi stick
  storageClassName: lvm-proxmox-hdd-shared
  resources:
    requests:
      storage: 50Gi
