apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: proxmox-proxy-config
    namespace: proxmox-proxy
    files:
      - config/nginx.conf
