apiVersion: v1
kind: Service
metadata:
  name: proxmox-proxy
  namespace: proxmox-proxy
spec:
  selector:
    app.kubernetes.io/name: proxmox-proxy
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
