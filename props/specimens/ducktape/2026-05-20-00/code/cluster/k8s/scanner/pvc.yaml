apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scanner-data
  namespace: scanner
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-proxmox
  resources:
    requests:
      storage: 20Gi
