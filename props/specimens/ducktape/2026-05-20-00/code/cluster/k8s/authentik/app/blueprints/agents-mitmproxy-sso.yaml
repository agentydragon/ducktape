version: 1
metadata:
  name: SSO - OpenClaw Mitmproxy
  labels:
    blueprints.goauthentik.io/description: "OpenClaw mitmproxy traffic viewer proxy provider and application"

entries:
  - model: authentik_providers_proxy.proxyprovider
    id: agents-mitmproxy-provider
    state: present
    identifiers:
      name: agents-mitmproxy
    attrs:
      external_host: "https://agents-mitmproxy.allegedly.works"
      internal_host: "http://mitmproxy.agents-mitmproxy.svc.cluster.local:8081"
      mode: proxy
      authentication_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      access_token_validity: "hours=1"

  - model: authentik_core.application
    id: agents-mitmproxy-app
    state: present
    identifiers:
      slug: agents-mitmproxy
    attrs:
      name: OpenClaw Mitmproxy
      provider: !KeyOf agents-mitmproxy-provider
      meta_description: "OpenClaw outbound traffic viewer"
      meta_launch_url: "https://agents-mitmproxy.allegedly.works"
      open_in_new_tab: true

  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !KeyOf agents-mitmproxy-app
      group: !Find [authentik_core.group, [name, authentik Admins]]
      order: 0
