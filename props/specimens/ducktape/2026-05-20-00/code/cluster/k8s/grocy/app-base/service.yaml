apiVersion: v1
kind: Service
metadata:
  name: grocy
spec:
  selector:
    app.kubernetes.io/name: grocy
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
