apiVersion: v1
kind: Service
metadata:
  name: activitywatch-readonly
  namespace: activitywatch
spec:
  selector:
    app.kubernetes.io/name: activitywatch
  ports:
    - port: 5600
      targetPort: 5601
      name: http
