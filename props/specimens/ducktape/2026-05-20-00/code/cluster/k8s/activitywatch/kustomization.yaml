apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - rbac.yaml
  - pvc.yaml
  - service.yaml
  - cilium-network-policy.yaml
  - nebula-config.yaml
  - nebula-certs.sops.yaml

configMapGenerator:
  - name: activitywatch-readonly-proxy
    namespace: activitywatch
    files:
      - default.conf=readonly-proxy.conf
