apiVersion: batch/v1
kind: Job
metadata:
  name: provision-matrix-users
  namespace: matrix
  annotations:
    # Recreate job on each reconciliation to ensure idempotent provisioning
    kustomize.toolkit.fluxcd.io/force: enabled
spec:
  backoffLimit: 5
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: provision
          image: python:3.13-alpine
          command: ["python3", "/scripts/provision_matrix_users.py"]
          env:
            - name: REGISTRATION_SECRET
              valueFrom:
                secretKeyRef:
                  name: synapse-registration-secret
                  key: registration_shared_secret
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: synapse-admin-credentials
                  key: password
            - name: BOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: openclaw-matrix-bot-password
                  key: password
          volumeMounts:
            - name: script
              mountPath: /scripts
              readOnly: true
      volumes:
        - name: script
          configMap:
            name: provision-matrix-users-script
