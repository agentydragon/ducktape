apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: app-with-violation
  namespace: test-namespace
spec:
  interval: 30m
  chart:
    spec:
      chart: test-chart
      version: "1.0.0"
      sourceRef:
        kind: HelmRepository
        name: test-repo
