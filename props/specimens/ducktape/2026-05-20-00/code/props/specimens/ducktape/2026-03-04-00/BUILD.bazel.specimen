load("//props/specimens:defs.bzl", "specimen_targets")

# Snapshot of ducktape at b69043f19.
# Included: agent_server/, editor_agent/, mcp_infra/, agent_core/,
#   openai_utils/, util/, mcp_utils/, mako_utils/, agent_pkg/,
#   gnome_terminal_profile_switcher/
#   (transitive build deps of agent_server, editor_agent, mcp_infra)
# Excluded: everything else (props/, ansible/, cluster/, nix/, dotfiles/,
#   website/, trilium/, finance/, gmail_archiver/, inventree_utils/, etc.)
specimen_targets(
    name = "specimen",
    code_srcs = glob(["code/**/*"]),
    slug = "ducktape/2026-03-04-00",
    split = "train",
)
