<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<!--
  Hermetic fontconfig for visual regression tests.

  Normalizes font rendering settings across environments to reduce
  pixel-level differences from system fontconfig variations.
  System font dirs are included so Chrome has fallbacks for UI elements.
-->
<fontconfig>
  <dir>/usr/share/fonts</dir>

  <!-- Disable all rendering-variable settings -->
  <match target="font">
    <edit name="hinting" mode="assign"><bool>false</bool></edit>
    <edit name="autohint" mode="assign"><bool>false</bool></edit>
    <edit name="hintstyle" mode="assign"><const>hintnone</const></edit>
    <edit name="antialias" mode="assign"><bool>false</bool></edit>
    <edit name="rgba" mode="assign"><const>none</const></edit>
    <edit name="lcdfilter" mode="assign"><const>lcdnone</const></edit>
    <edit name="embeddedbitmap" mode="assign"><bool>false</bool></edit>
  </match>
</fontconfig>
