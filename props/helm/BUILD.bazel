load("@rules_helm//helm:defs.bzl", "helm_chart", "helm_lint_test")

helm_chart(
    name = "helm",
    deps = ["@helm_charts__postgresql//:postgresql"],
)

helm_lint_test(
    name = "helm_lint_test",
    chart = ":helm",
)
