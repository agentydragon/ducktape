# A ClusterSecretStore with no spec.conditions: readable from every namespace.
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: unfenced-store
spec:
  provider:
    kubernetes:
      auth:
        serviceAccount:
          name: external-secrets
          namespace: external-secrets-system
      remoteNamespace: flux-system
