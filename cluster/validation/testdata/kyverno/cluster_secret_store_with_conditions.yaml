apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: fenced-store
spec:
  conditions:
    - namespaces: [claude-sandbox]
  provider:
    kubernetes:
      auth:
        serviceAccount:
          name: external-secrets
          namespace: external-secrets-system
      remoteNamespace: flux-system
