apiVersion: v1
kind: Pod
metadata:
  name: seaweedfs-consumer
  namespace: agent-workspaces
spec:
  containers:
    - name: app
      image: busybox
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: seaweedfs-consumer-data
