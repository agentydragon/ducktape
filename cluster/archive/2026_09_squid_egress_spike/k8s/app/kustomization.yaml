apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - certificate.yaml
  - fake-credentials.yaml
  - echo-origin.yaml
  - deployment.yaml
  - networkpolicy.yaml
  - icap-stub.yaml

configMapGenerator:
  - name: squid-spike-config
    namespace: squid-egress-spike
    files:
      - squid.conf
      - credentials.conf.tmpl
      - icap_stub.py
