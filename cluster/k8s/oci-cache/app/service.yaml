apiVersion: v1
kind: Service
metadata:
  name: oci-cache
  namespace: oci-cache
  labels:
    app.kubernetes.io/name: zot
spec:
  selector:
    app.kubernetes.io/name: zot
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP
