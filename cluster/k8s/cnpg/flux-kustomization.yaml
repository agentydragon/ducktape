apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cnpg
  namespace: ducktape-flux
spec:
  dependsOn:
    - name: cert-manager
      namespace: ducktape-flux
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: cnpg
      namespace: cnpg-system
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: plugin-barman-cloud
      namespace: cnpg-system
    - apiVersion: apps/v1
      kind: Deployment
      name: plugin-barman-cloud
      namespace: cnpg-system
  interval: 10m
  path: ./cluster/k8s/cnpg
  prune: true
  retryInterval: 1m
  sourceRef:
    kind: ExternalArtifact
    name: cnpg
    namespace: ducktape-flux
  timeout: 10m
  wait: true
