apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: analytics-clickhouse
  namespace: ducktape-flux
spec:
  retryInterval: 1m
  interval: 10m
  path: ./cluster/k8s/analytics/cluster
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age-cluster-secrets
  sourceRef:
    kind: GitRepository
    name: ducktape
    namespace: ducktape-flux
  timeout: 20m
  wait: true
  healthChecks:
    - apiVersion: clickhouse-keeper.altinity.com/v1
      kind: ClickHouseKeeperInstallation
      name: analytics-keeper
      namespace: analytics
    - apiVersion: clickhouse.altinity.com/v1
      kind: ClickHouseInstallation
      name: analytics
      namespace: analytics
  healthCheckExprs:
    - apiVersion: clickhouse-keeper.altinity.com/v1
      kind: ClickHouseKeeperInstallation
      current: "status.status == 'Completed'"
      failed: "status.status == 'Aborted'"
      inProgress: "status.status != 'Completed' && status.status != 'Aborted'"
    - apiVersion: clickhouse.altinity.com/v1
      kind: ClickHouseInstallation
      current: "status.status == 'Completed'"
      failed: "status.status == 'Aborted'"
      inProgress: "status.status != 'Completed' && status.status != 'Aborted'"
  dependsOn:
    - name: clickhouse-operator
      namespace: ducktape-flux
    - name: seaweedfs-analytics-backup-bucket
      namespace: ducktape-flux
