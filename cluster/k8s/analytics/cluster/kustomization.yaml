apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - agent-diagnostics-rbac.yaml
  - admin-credentials.sops.yaml
  - aiquota-credentials.sops.yaml
  - grafana-credentials.sops.yaml
  - public-coder-credentials.sops.yaml
  - keeper.yaml
  - clickhouse.yaml
  - networkpolicy.yaml
  - podmonitor.yaml
  - backup-cronjob.yaml

configMapGenerator:
  - name: clickhouse-analytics-config
    namespace: analytics
    files:
      - system_logs.xml
configurations:
  - kustomizeconfig
