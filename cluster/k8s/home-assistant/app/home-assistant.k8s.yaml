apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-config
  namespace: home-assistant
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi
  storageClassName: local-path-home-ssd
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-images-creds
  namespace: home-assistant
spec:
  dataFrom:
    - extract:
        key: forgejo-images-creds
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes-forgejo-images-secret-store
  target:
    name: forgejo-images-creds
    template:
      mergePolicy: Merge
      type: kubernetes.io/dockerconfigjson
---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: home-assistant-metrics-token
  namespace: home-assistant
spec:
  allowRepeat: true
  digits: 12
  length: 48
  noUpper: false
  symbols: 0
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: home-assistant-metrics-token
  namespace: home-assistant
spec:
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: home-assistant-metrics-token
  refreshPolicy: CreatedOnce
  target:
    creationPolicy: Owner
    name: home-assistant-metrics-token
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: home-assistant-component-installer
  namespace: home-assistant
data:
  settings.yaml: |
    config_dir: /config
    components:
      - version: 2.2.1
        url: https://github.com/dreo-team/hass-dreoverse/archive/refs/tags/v2.2.1.zip
        sha256: 555e4e3470b64574bfe5a050ca5557ce3f509844e2e8827ecd7e10c8a3d77d24
        archive_path: "*/custom_components/dreo"
        install_dir: dreo
        manifest_domain: dreo
      - version: 1.1.1
        url: https://github.com/christiaangoossens/hass-oidc-auth/releases/download/v1.1.1/hass-oidc-auth.zip
        sha256: 9ce9e6153f80c781e360b93e097ff7d87d09235430fc48e7a67d97dda5fc3322
        archive_path: "."
        install_dir: auth_oidc
        config_files:
          - automations.yaml
          - scripts.yaml
          - scenes.yaml
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: home-assistant
  name: home-assistant
  namespace: home-assistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: home-assistant
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: home-assistant
    spec:
      containers:
        - env:
            - name: TZ
              value: America/Los_Angeles
            - name: SETUP_PORT
              value: "8124"
          image: ghcr.io/home-assistant/home-assistant:2026.9.2
          livenessProbe:
            httpGet:
              host: 127.0.0.1
              path: /
              port: backend
            initialDelaySeconds: 60
            periodSeconds: 30
          name: home-assistant
          ports:
            - containerPort: 8124
              name: backend
          readinessProbe:
            httpGet:
              host: 127.0.0.1
              path: /
              port: backend
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - mountPath: /config
              name: config
            - mountPath: /config/configuration.yaml
              name: configuration
              readOnly: true
              subPath: configuration.yaml
        - env:
            - name: METRICS_TOKEN
              valueFrom:
                secretKeyRef:
                  key: password
                  name: home-assistant-metrics-token
          image: caddy:2.11.4-alpine
          name: caddy
          ports:
            - containerPort: 8123
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
          resources:
            limits:
              memory: 128Mi
            requests:
              cpu: 20m
              memory: 32Mi
          volumeMounts:
            - mountPath: /etc/caddy/Caddyfile
              name: caddy-config
              readOnly: true
              subPath: Caddyfile
      dnsPolicy: ClusterFirstWithHostNet
      hostNetwork: true
      imagePullSecrets:
        - name: forgejo-images-creds
      initContainers:
        - command:
            - /homeassistant/provisioner/components/install_bin
          env:
            - name: HOME_ASSISTANT_COMPONENT_INSTALLER_CONFIG_FILE
              value: /etc/provisioner/settings.yaml
          image: git.allegedly.works/ducktape-ci/homeassistant-component-installer:unset
          imagePullPolicy: Always
          name: provision-components
          volumeMounts:
            - mountPath: /config
              name: config
            - mountPath: /etc/provisioner
              name: installer-settings
              readOnly: true
      nodeSelector:
        kubernetes.io/hostname: optiplex
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: home-assistant-config
        - configMap:
            name: home-assistant-configuration
          name: configuration
        - configMap:
            name: home-assistant-component-installer
          name: installer-settings
        - configMap:
            name: home-assistant-caddy
          name: caddy-config
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: home-assistant
  name: home-assistant
  namespace: home-assistant
spec:
  ports:
    - name: http
      port: 8123
      targetPort: http
  selector:
    app.kubernetes.io/name: home-assistant
---
apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    kustomize.toolkit.fluxcd.io/force: enabled
    reloader.stakater.com/auto: "false"
  name: home-assistant-onboarding
  namespace: home-assistant
spec:
  backoffLimit: 3
  template:
    metadata:
      annotations:
        home-assistant.allegedly.works/bootstrap-revision: "5"
      labels:
        app.kubernetes.io/name: home-assistant-onboarding
    spec:
      containers:
        - command:
            - /homeassistant/provisioner/onboarding/onboard_bin
          env:
            - name: HOME_ASSISTANT_ONBOARDING_CONFIG_FILE
              value: /etc/provisioner/settings.yaml
            - name: HOME_ASSISTANT_ONBOARDING_OWNER_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: home-assistant-break-glass
            - name: HOME_ASSISTANT_ONBOARDING_CORE_CONFIG__LOCATION__LATITUDE
              valueFrom:
                secretKeyRef:
                  key: latitude
                  name: home-assistant-location
                  optional: true
            - name: HOME_ASSISTANT_ONBOARDING_CORE_CONFIG__LOCATION__LONGITUDE
              valueFrom:
                secretKeyRef:
                  key: longitude
                  name: home-assistant-location
                  optional: true
          image: git.allegedly.works/ducktape-ci/homeassistant-onboarding:unset
          imagePullPolicy: Always
          name: onboarding
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 20m
              memory: 64Mi
          volumeMounts:
            - mountPath: /etc/provisioner
              name: settings
              readOnly: true
      imagePullSecrets:
        - name: forgejo-images-creds
      restartPolicy: OnFailure
      volumes:
        - configMap:
            name: home-assistant-onboarding
          name: settings
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: home-assistant-token-provisioner
  namespace: home-assistant
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: home-assistant-token-provisioner
  namespace: home-assistant
rules:
  - apiGroups:
      - ""
    resourceNames:
      - ha-mcp-home-assistant-token
      - agentplane-home-assistant-token
    resources:
      - secrets
    verbs:
      - get
      - update
      - patch
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - create
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: home-assistant-token-provisioner
  namespace: home-assistant
data:
  settings.yaml: |
    endpoint:
      url: http://home-assistant.home-assistant.svc.cluster.local:8123
      client_id: https://home.allegedly.works/
      redirect_uri: https://home.allegedly.works/
    owner_username: ha-local-admin
    tokens:
      - client_name: ha-mcp-cluster
        secret_name: ha-mcp-home-assistant-token
        secret_namespace: home-assistant
        description: Long-lived token of Home Assistant's local owner, which ha-mcp copies
      - client_name: agentplane-egress
        read_only_user: agentplane-reader
        secret_name: agentplane-home-assistant-token
        secret_namespace: home-assistant
        description: Long-lived token of Home Assistant's read-only agentplane-reader user, which agentplane-staging's egress proxy presents
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: home-assistant-token-provisioner
  namespace: home-assistant
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: home-assistant-token-provisioner
subjects:
  - kind: ServiceAccount
    name: home-assistant-token-provisioner
    namespace: home-assistant
---
apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    app.kubernetes.io/name: home-assistant-token-provisioner
  name: home-assistant-token-provisioner
  namespace: home-assistant
spec:
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      activeDeadlineSeconds: 600
      backoffLimit: 3
      template:
        metadata:
          labels:
            app.kubernetes.io/name: home-assistant-token-provisioner
        spec:
          automountServiceAccountToken: true
          containers:
            - command:
                - /homeassistant/provisioner/tokens/provision_bin
              env:
                - name: HOME_ASSISTANT_TOKEN_PROVISIONER_CONFIG_FILE
                  value: /etc/provisioner/settings.yaml
                - name: HOME_ASSISTANT_TOKEN_PROVISIONER_OWNER_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      key: password
                      name: home-assistant-break-glass
              image: git.allegedly.works/ducktape-ci/homeassistant-token-provisioner:unset
              imagePullPolicy: Always
              name: provision-tokens
              resources:
                limits:
                  memory: 256Mi
                requests:
                  cpu: 20m
                  memory: 64Mi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
              volumeMounts:
                - mountPath: /etc/provisioner
                  name: settings
                  readOnly: true
          imagePullSecrets:
            - name: forgejo-images-creds
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          serviceAccountName: home-assistant-token-provisioner
          volumes:
            - configMap:
                name: home-assistant-token-provisioner
              name: settings
  schedule: "*/15 * * * *"
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: home-assistant
  namespace: home-assistant
spec:
  hostnames:
    - home.allegedly.works
  parentRefs:
    - name: cluster-gateway
      namespace: gateway-system
  rules:
    - backendRefs:
        - name: home-assistant
          port: 8123
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: home-assistant-config-restic
  namespace: home-assistant
spec:
  restic:
    cacheAccessModes:
      - ReadWriteOnce
    cacheCapacity: 1Gi
    cacheStorageClassName: local-path-home-ssd
    copyMethod: Direct
    moverAffinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - optiplex
    moverPodLabels:
      app.kubernetes.io/name: home-assistant-config-restic
    moverResources:
      limits:
        cpu: "1"
        memory: 1Gi
      requests:
        cpu: 250m
        memory: 512Mi
    moverSecurityContext:
      runAsGroup: 0
      runAsUser: 0
      seccompProfile:
        type: RuntimeDefault
    pruneIntervalDays: 7
    repository: home-assistant-config-restic-tenant
    retain:
      daily: 7
      monthly: 6
      weekly: 4
  sourcePVC: home-assistant-config
  trigger:
    schedule: 17 */6 * * *
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: home-assistant-config-restic-egress
  namespace: home-assistant
spec:
  egress:
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
      to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
    - ports:
        - port: 8333
          protocol: TCP
      to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: seaweedfs
          podSelector:
            matchLabels:
              app.kubernetes.io/component: s3
              app.kubernetes.io/instance: seaweedfs
              app.kubernetes.io/managed-by: seaweedfs-operator
              app.kubernetes.io/name: seaweedfs
  podSelector:
    matchLabels:
      app.kubernetes.io/name: home-assistant-config-restic
  policyTypes:
    - Egress
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: home-assistant
  namespace: home-assistant
spec:
  endpoints:
    - authorization:
        credentials:
          key: password
          name: home-assistant-metrics-token
        type: Bearer
      path: /api/prometheus
      port: http
  selector:
    matchLabels:
      app.kubernetes.io/name: home-assistant
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  labels:
    release: kube-prometheus-stack
  name: home-assistant
  namespace: home-assistant
spec:
  groups:
    - name: home-assistant
      rules:
        - alert: HomeAssistantUnavailable
          annotations:
            description: Prometheus has been unable to scrape Home Assistant for 10 minutes.
            summary: Home Assistant is unavailable
          expr: up{namespace="home-assistant", service="home-assistant"} == 0
          for: 10m
          labels:
            severity: warning
