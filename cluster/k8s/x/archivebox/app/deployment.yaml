apiVersion: apps/v1
kind: Deployment
metadata:
  name: archivebox
  labels:
    app.kubernetes.io/name: archivebox
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: archivebox
  template:
    metadata:
      labels:
        app.kubernetes.io/name: archivebox
    spec:
      # seaweedfs-ovh is mounted only on the OVH nodes. This also keeps the
      # local SQLite PVC and its single writer in the same storage locality.
      nodeSelector:
        topology.kubernetes.io/zone: hil-ovh
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 911
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      # ArchiveBox's RemoteUser backend creates a local Django user on the
      # first successful Authentik login, but that user is not a staff user.
      # Bootstrap the intended Authentik principal declaratively instead of
      # relying on an operator to run createsuperuser in a live Pod.
      initContainers:
        - name: bootstrap-admin
          image: archivebox/archivebox@sha256:1a5a37331091d9df865ead2b9c231aa5a892fc26fe0422ce6140d9e2d9532327
          command: ["/bin/sh", "-ec"]
          args:
            - |
              archivebox init --quick
              archivebox manage shell -c "from django.contrib.auth import get_user_model; import os; User = get_user_model(); user, _ = User.objects.get_or_create(username=os.environ['ARCHIVEBOX_ADMIN_USERNAME']); user.is_active = True; user.is_staff = True; user.is_superuser = True; user.set_unusable_password(); user.save()"
          env:
            - name: ARCHIVEBOX_ADMIN_USERNAME
              value: agentydragon
          envFrom:
            - configMapRef:
                name: archivebox-config
          securityContext:
            runAsNonRoot: true
            runAsUser: 911
            runAsGroup: 911
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          volumeMounts:
            - name: data
              mountPath: /data
            - name: archive
              mountPath: /data/archive
            - name: tmp
              mountPath: /tmp
      containers:
        - name: archivebox
          # Pinned official stable multi-architecture v0.7.4 image index.
          image: archivebox/archivebox@sha256:1a5a37331091d9df865ead2b9c231aa5a892fc26fe0422ce6140d9e2d9532327
          # Override the image's root-requiring Docker entrypoint. The image
          # already owns its browser runtime as UID/GID 911; fsGroup makes both
          # PVCs writable without granting the workload Linux capabilities.
          command: ["archivebox"]
          args: ["server", "--quick-init", "0.0.0.0:8000"]
          envFrom:
            - configMapRef:
                name: archivebox-config
          ports:
            - name: http
              containerPort: 8000
          securityContext:
            runAsNonRoot: true
            runAsUser: 911
            runAsGroup: 911
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 3Gi
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
          volumeMounts:
            - name: data
              mountPath: /data
            - name: archive
              mountPath: /data/archive
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: archivebox-data
        - name: archive
          persistentVolumeClaim:
            claimName: archivebox-archive
        - name: tmp
          emptyDir:
            sizeLimit: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: archivebox
  labels:
    app.kubernetes.io/name: archivebox
spec:
  selector:
    app.kubernetes.io/name: archivebox
  ports:
    - name: http
      port: 8000
      targetPort: http
