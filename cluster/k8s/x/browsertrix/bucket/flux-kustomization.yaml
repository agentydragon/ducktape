apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  # Retain the existing resource name so moving its source path does not delete
  # the live Bucket before its explicit retirement.
  name: seaweedfs-browsertrix-bucket
  namespace: ducktape-flux
spec:
  suspend: true
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  path: ./cluster/k8s/x/browsertrix/bucket
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: ducktape
    namespace: ducktape-flux
  dependsOn:
    - name: seaweedfs-cluster
      namespace: ducktape-flux
    - name: seaweedfs-secrets
      namespace: ducktape-flux
  healthChecks:
    - apiVersion: seaweed.seaweedfs.com/v1
      kind: Bucket
      name: browsertrix
      namespace: seaweedfs
