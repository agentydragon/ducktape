apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: harbor-agent-rbac
  namespace: ducktape-flux
  annotations:
    description: Agent RBAC for harbor namespace. Lightweight — only RoleBindings. Depends on namespace existing + agent RBAC base for ClusterRoles.
spec:
  suspend: true
  interval: 10m
  path: ./cluster/k8s/x/harbor/agent-rbac
  prune: true
  sourceRef:
    kind: GitRepository
    name: ducktape
    namespace: ducktape-flux
  timeout: 2m
  dependsOn:
    - name: harbor-namespace
      namespace: ducktape-flux
    - name: claude-rbac
      namespace: ducktape-flux
