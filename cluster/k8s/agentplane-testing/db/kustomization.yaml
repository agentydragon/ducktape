apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Role password Secrets before the Cluster that references them.
  - role-secrets.yaml
  - postgres-cluster.yaml
  - databases.yaml
