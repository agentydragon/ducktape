apiVersion: v1
kind: Service
metadata:
  name: agentplane-mcp-everything
  namespace: agentplane-testing
spec:
  selector:
    app.kubernetes.io/name: agentplane-mcp-everything
  ports:
    - name: http
      port: 3001
      targetPort: http
