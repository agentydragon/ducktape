apiVersion: v1
kind: Service
metadata:
  name: agentplane-ssh-mcp
  namespace: agentplane-testing
spec:
  selector:
    app.kubernetes.io/name: agentplane-ssh-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
