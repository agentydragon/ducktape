apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - postgres-cluster.yaml
  - readonly-secret.sops.yaml
  - readonly-role-provisioner-job.yaml
  - forgejo-images-creds-eso.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - rolebinding-secrets-reader.yaml
configMapGenerator:
  - name: study-casino-db-readonly-sql
    namespace: study-casino
    files:
      - readonly-role.sql
