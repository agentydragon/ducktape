apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdk8s-generated (cluster/cdk8s/study_casino/app.py).
  - study-casino.k8s.yaml
  - readonly-secret.sops.yaml
  # Hand-written: its image and STUDY_CASINO_IMAGE_TAG carry Flux image-automation markers.
  - deployment.yaml
configMapGenerator:
  - name: study-casino-db-readonly-sql
    namespace: study-casino
    files:
      - readonly-role.sql
