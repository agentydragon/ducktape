apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - schema-job.yaml

configMapGenerator:
  - name: clickhouse-aiquota-schema
    namespace: clickhouse
    files:
      - schema.sql
