apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - clickhouse-schema.k8s.yaml
configMapGenerator:
  - name: clickhouse-aiquota-schema
    namespace: clickhouse
    files:
      - schema.sql
