apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: wayback-archive
  namespace: flux-system
spec:
  image: ghcr.io/agentydragon/wayback-archive
  interval: 5m
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: wayback-archive
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: wayback-archive
  filterTags:
    pattern: '^devel-\d{14}-[0-9a-f]{7}$'
  policy:
    alphabetical:
      order: asc
