---
# Harbor CI robot credentials for Flux ImageRepository authentication.
# Robot account created by terraform/gitops/harbor-ci.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-ci-robot
  namespace: flux-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: harbor-ci-robot
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: kubernetes.io/dockerconfigjson
      engineVersion: v2
      data:
        .dockerconfigjson: |
          {"auths":{"registry.allegedly.works":{"username":"{{ .username }}","password":"{{ .password }}","auth":"{{ printf "%s:%s" .username .password | b64enc }}"}}}
  data:
    - secretKey: username
      remoteRef:
        key: kv/harbor/ci-robot
        property: username
    - secretKey: password
      remoteRef:
        key: kv/harbor/ci-robot
        property: password
