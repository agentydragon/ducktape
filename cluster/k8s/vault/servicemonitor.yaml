apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: vault
  namespace: vault
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: vault
  endpoints:
    - port: api-port
      path: /v1/sys/metrics
      params:
        format: ["prometheus"]
      interval: 30s
