# Grant approved agent identities access in explicitly opted-in namespaces.
# The namespace labels are GitOps-owned; they are access grants, not merely
# metadata.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: generate-agent-diagnostics-readers
  annotations:
    policies.kyverno.io/title: Generate agent diagnostics readers
    policies.kyverno.io/category: Access Control
    policies.kyverno.io/severity: high
    policies.kyverno.io/subject: Namespace, RoleBinding
    policies.kyverno.io/description: >-
      Generates namespaced, read-only diagnostics RoleBindings when a
      GitOps-managed Namespace opts in with the corresponding RBAC label.
spec:
  background: true
  rules:
    - name: generate-agent-namespace-diagnostics-reader
      match:
        any:
          - resources:
              kinds:
                - Namespace
              selector:
                matchLabels:
                  rbac.ducktape.io/agent-namespace-diagnostics-reader: "true"
      generate:
        generateExisting: true
        synchronize: true
        apiVersion: rbac.authorization.k8s.io/v1
        kind: RoleBinding
        name: agent-namespace-diagnostics-reader
        namespace: "{{request.object.metadata.name}}"
        data:
          metadata:
            labels:
              rbac.ducktape.io/managed-by: kyverno
              rbac.ducktape.io/access: agent-namespace-diagnostics-reader
          roleRef:
            apiGroup: rbac.authorization.k8s.io
            kind: ClusterRole
            name: namespace-diagnostics-reader
          subjects:
            - kind: Group
              name: oidc-ksbx-groups:haku
              apiGroup: rbac.authorization.k8s.io
            - kind: ServiceAccount
              name: haku
              namespace: haku-sandbox
            - kind: ServiceAccount
              name: haku-claude
              namespace: haku-claude-sandbox
            - kind: Group
              name: oidc-ksbx-groups:kubectl-sandbox-users
              apiGroup: rbac.authorization.k8s.io
