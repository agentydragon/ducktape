apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdk8s-generated (cluster/cdk8s/forgejo/app.py).
  - forgejo.k8s.yaml
  # Hand-written: no CiliumEnvoyConfig CRD binding exists for cdk8s.
  - ciliumenvoyconfig.yaml
  - forgejo-admin-password.sops.yaml
# OAuth OIDC credentials provisioned by tf/gitops/sso-providers/ as a k8s Secret.
