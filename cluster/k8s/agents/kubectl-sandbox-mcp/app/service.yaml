apiVersion: v1
kind: Service
metadata:
  name: kubectl-sandbox-mcp
  namespace: kubectl-sandbox-mcp
spec:
  selector:
    app: kubectl-sandbox-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
