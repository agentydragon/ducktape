apiVersion: v1
kind: Service
metadata:
  name: ha-mcp
  namespace: ha-mcp
  labels:
    app.kubernetes.io/name: ha-mcp
spec:
  selector:
    app.kubernetes.io/name: ha-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  type: ClusterIP
