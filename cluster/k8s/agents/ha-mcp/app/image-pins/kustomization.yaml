apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
images:
  # The `:tag` suffix on both entries tells Setters to write back only the bare tag
  # value -- see cluster/k8s/litellm/app/image-pins/kustomization.yaml for why the
  # bare (full-reference) marker form is wrong here.
  - name: git.allegedly.works/ducktape-ci/mcp-oauth-facade
    newTag: devel-20260917082758-5647a0e # {"$imagepolicy": "flux-system:mcp-oauth-facade:tag"}
  - name: git.allegedly.works/ducktape-ci/ha-mcp-token-provisioner
    newTag: devel-20260915084053-a8b1ac6 # {"$imagepolicy": "flux-system:ha-mcp-token-provisioner:tag"}
