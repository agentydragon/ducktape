apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ha-mcp
  namespace: ha-mcp
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ha-mcp
  endpoints:
    - port: metrics
      interval: 30s
