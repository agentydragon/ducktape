apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-images-creds
  namespace: plaid-mcp
spec:
  dataFrom:
    - extract:
        key: forgejo-images-creds
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: kubernetes-forgejo-images-secret-store
  target:
    name: forgejo-images-creds
    template:
      mergePolicy: Merge
      type: kubernetes.io/dockerconfigjson
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: plaid-mcp-config
  namespace: plaid-mcp
data:
  PLAID_MCP_INVESTMENT_TRANSACTION_DAYS: "730"
  PLAID_MCP_PLAID_ENV: production
  PLAID_MCP_PUBLIC_BASE_URL: https://plaid-mcp.allegedly.works
  PLAID_MCP_TRANSACTION_DAYS: "730"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: plaid-mcp
  namespace: plaid-mcp
imagePullSecrets:
  - name: forgejo-images-creds
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: plaid-mcp-secret-manager
  namespace: plaid-mcp
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - get
      - list
      - create
      - update
      - patch
      - delete
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: plaid-mcp-secret-manager
  namespace: plaid-mcp
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: plaid-mcp-secret-manager
subjects:
  - kind: ServiceAccount
    name: plaid-mcp
    namespace: plaid-mcp
---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    description: Plaid self-contained link web UI. Authentik proxy outpost protects browser access; no bespoke Plaid MCP tools are exposed in v0. The app writes access-token Secrets and syncs linked Items into the plaid-mcp Postgres database.
    reloader.stakater.com/auto: "true"
  labels:
    app.kubernetes.io/name: plaid-mcp
  name: plaid-mcp
  namespace: plaid-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: plaid-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: plaid-mcp
    spec:
      containers:
        - env:
            - name: PLAID_MCP_PLAID_ENV
              valueFrom:
                configMapKeyRef:
                  key: PLAID_MCP_PLAID_ENV
                  name: plaid-mcp-config
            - name: PLAID_MCP_PUBLIC_BASE_URL
              valueFrom:
                configMapKeyRef:
                  key: PLAID_MCP_PUBLIC_BASE_URL
                  name: plaid-mcp-config
            - name: PLAID_MCP_TRANSACTION_DAYS
              valueFrom:
                configMapKeyRef:
                  key: PLAID_MCP_TRANSACTION_DAYS
                  name: plaid-mcp-config
            - name: PLAID_MCP_INVESTMENT_TRANSACTION_DAYS
              valueFrom:
                configMapKeyRef:
                  key: PLAID_MCP_INVESTMENT_TRANSACTION_DAYS
                  name: plaid-mcp-config
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  key: uri
                  name: plaid-mcp-db-app
            - name: PLAID_MCP_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  key: client_id
                  name: plaid-client-credentials
            - name: PLAID_MCP_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  key: client_secret
                  name: plaid-client-credentials
          image: git.allegedly.works/ducktape-ci/plaid-mcp-server:unset
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
          name: plaid-web
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 50m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsGroup: 1000
            runAsNonRoot: true
            runAsUser: 1000
      imagePullSecrets:
        - name: forgejo-images-creds
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: plaid-mcp
---
apiVersion: batch/v1
kind: CronJob
metadata:
  annotations:
    description: v0 synchronous full-refresh Plaid sync. Keeps Postgres fresh within 12 hours; no real-time balance endpoint calls.
  name: plaid-mcp-sync
  namespace: plaid-mcp
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app.kubernetes.io/name: plaid-mcp-sync
        spec:
          containers:
            - env:
                - name: PLAID_MCP_PLAID_ENV
                  valueFrom:
                    configMapKeyRef:
                      key: PLAID_MCP_PLAID_ENV
                      name: plaid-mcp-config
                - name: PLAID_MCP_PUBLIC_BASE_URL
                  valueFrom:
                    configMapKeyRef:
                      key: PLAID_MCP_PUBLIC_BASE_URL
                      name: plaid-mcp-config
                - name: PLAID_MCP_TRANSACTION_DAYS
                  valueFrom:
                    configMapKeyRef:
                      key: PLAID_MCP_TRANSACTION_DAYS
                      name: plaid-mcp-config
                - name: PLAID_MCP_INVESTMENT_TRANSACTION_DAYS
                  valueFrom:
                    configMapKeyRef:
                      key: PLAID_MCP_INVESTMENT_TRANSACTION_DAYS
                      name: plaid-mcp-config
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      key: uri
                      name: plaid-mcp-db-app
                - name: PLAID_MCP_CLIENT_ID
                  valueFrom:
                    secretKeyRef:
                      key: client_id
                      name: plaid-client-credentials
                - name: PLAID_MCP_CLIENT_SECRET
                  valueFrom:
                    secretKeyRef:
                      key: client_secret
                      name: plaid-client-credentials
              image: git.allegedly.works/ducktape-ci/plaid-mcp-sync:unset
              imagePullPolicy: Always
              name: sync
              resources:
                limits:
                  cpu: 500m
                  memory: 512Mi
                requests:
                  cpu: 50m
                  memory: 128Mi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - ALL
                runAsGroup: 1000
                runAsNonRoot: true
                runAsUser: 1000
          restartPolicy: Never
          securityContext:
            seccompProfile:
              type: RuntimeDefault
          serviceAccountName: plaid-mcp
  schedule: 17 */12 * * *
  successfulJobsHistoryLimit: 3
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: plaid-mcp
  name: plaid-mcp
  namespace: plaid-mcp
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: plaid-mcp
  type: ClusterIP
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  annotations:
    description: Default-deny ingress for plaid-mcp pods. Only the Authentik embedded proxy outpost can reach the v0 web UI.
  name: plaid-mcp-ingress
  namespace: plaid-mcp
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: plaid-mcp
  ingress:
    - fromEndpoints:
        - matchLabels:
            app.kubernetes.io/name: authentik
            k8s:io.kubernetes.pod.namespace: authentik
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
