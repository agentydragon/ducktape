apiVersion: v1
kind: Service
metadata:
  name: plaid-mcp
  namespace: plaid-mcp
  labels:
    app.kubernetes.io/name: plaid-mcp
spec:
  selector:
    app.kubernetes.io/name: plaid-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
