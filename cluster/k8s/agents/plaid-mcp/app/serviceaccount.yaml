apiVersion: v1
kind: ServiceAccount
metadata:
  name: plaid-mcp
  namespace: plaid-mcp
  annotations:
    description: "SA for the Plaid MCP pod. No k8s permissions — Plaid creds and per-item access tokens arrive as reflected Secrets from the airlock namespace."
