apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - plaid-mcp-db.k8s.yaml
configMapGenerator:
  - name: plaid-mcp-db-readonly-sql
    namespace: plaid-mcp
    files:
      - readonly-role.sql
