apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: plaid-db-mcp
  namespace: plaid-mcp
spec:
  endpoints:
    - path: /metrics
      port: metrics
      scrapeTimeout: 10s
  selector:
    matchLabels:
      app.kubernetes.io/name: plaid-db-mcp
