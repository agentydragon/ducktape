apiVersion: v1
kind: Service
metadata:
  name: grocy-mcp-oidc-server
  namespace: grocy-mcp-oidc
  labels:
    app: grocy-mcp-oidc
    component: server
spec:
  selector:
    app: grocy-mcp-oidc
    component: server
  ports:
    - name: http
      port: 8765
      targetPort: 8765
      protocol: TCP
  type: ClusterIP
