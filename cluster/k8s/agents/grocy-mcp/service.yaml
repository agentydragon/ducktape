apiVersion: v1
kind: Service
metadata:
  name: grocy-mcp
  namespace: grocy-mcp
spec:
  selector:
    app: grocy-mcp
  ports:
    - name: mcp
      port: 3000
      targetPort: 3000
      protocol: TCP
