apiVersion: v1
kind: Service
metadata:
  name: haku-sandbox-iron-proxy
  namespace: haku-egress-proxy
spec:
  selector:
    app.kubernetes.io/name: haku-sandbox-iron-proxy
  ports:
    - name: proxy
      port: 8182
      targetPort: proxy
    - name: metrics
      port: 9090
      targetPort: metrics
