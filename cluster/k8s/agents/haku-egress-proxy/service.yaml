apiVersion: v1
kind: Service
metadata:
  name: haku-egress-proxy
  namespace: haku-egress-proxy
spec:
  selector:
    app.kubernetes.io/name: haku-egress-proxy
  ports:
    - name: proxy
      port: 8080
      targetPort: 8080
