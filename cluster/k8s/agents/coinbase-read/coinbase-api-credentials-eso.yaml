apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coinbase-api-credentials
  namespace: coinbase-read
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-external-creds-secret-store
    kind: ClusterSecretStore
  target:
    name: coinbase-read-api-credentials
    creationPolicy: Owner
  data:
    - secretKey: api_key
      remoteRef:
        key: coinbase-api-credentials
        property: api_key
    - secretKey: api_secret
      remoteRef:
        key: coinbase-api-credentials
        property: api_secret
