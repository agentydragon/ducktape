apiVersion: v1
kind: Service
metadata:
  name: authentik-mcp-poc-server
  namespace: authentik-mcp-poc
  labels:
    app: authentik-mcp-poc
    component: server
spec:
  selector:
    app: authentik-mcp-poc
    component: server
  ports:
    - name: http
      port: 8765
      targetPort: 8765
      protocol: TCP
  type: ClusterIP
