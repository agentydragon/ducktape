apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdk8s-generated (cluster/cdk8s/authentik_jwt_rotation.py) -- see cluster/docs/cdk8s.md.
  - authentik-jwt-rotation.k8s.yaml
components:
  - ./image-pins
configMapGenerator:
  - name: authentik-jwt-rotations-config
    namespace: agents-infra
    files:
      - rotations.yaml
