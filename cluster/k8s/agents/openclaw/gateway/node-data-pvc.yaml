apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-node-data
  namespace: openclaw-gateway
spec:
  accessModes:
    - ReadWriteOnce
  # CLEANUP(added 2026-04-02): Migrate to local-path-proxmox
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
