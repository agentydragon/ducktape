# Airlock writes oura-access-token into the `airlock` namespace; ESO mirrors it
# into the Claude sandbox where consumers read it.
apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: oura-access-token
spec:
  namespaces:
    - claude-sandbox
  externalSecretSpec:
    refreshInterval: 1m
    secretStoreRef:
      name: kubernetes-airlock-secret-store
      kind: ClusterSecretStore
    target:
      name: oura-access-token
    dataFrom:
      - extract:
          key: oura-access-token
