apiVersion: v1
kind: Service
metadata:
  name: firecracker-manager
  namespace: claude-sandbox
spec:
  selector:
    app: firecracker-manager
  ports:
    - name: api
      port: 8080
      targetPort: 8080
