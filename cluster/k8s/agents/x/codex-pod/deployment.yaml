apiVersion: apps/v1
kind: Deployment
metadata:
  name: codex-pod
  namespace: codex-pod
  labels:
    app.kubernetes.io/name: codex-pod
spec:
  replicas: 1
  strategy:
    type: Recreate # single RWO workspace PVC — don't run two pods against it
  selector:
    matchLabels:
      app.kubernetes.io/name: codex-pod
  template:
    metadata:
      labels:
        app.kubernetes.io/name: codex-pod
    spec:
      automountServiceAccountToken: false
      # Private Forgejo package — ducktape-ci credential reflected into this
      # namespace from cluster/k8s/forgejo-images/.
      imagePullSecrets:
        - name: forgejo-images-creds
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: codex
          # Flux image-automation setter: the marker MUST be a trailing comment on
          # the image line (kyaml Setters ignores an own-line comment above it).
          image: git.allegedly.works/ducktape-ci/codex-pod:devel-20260903195439-312bb2d # {"$imagepolicy": "flux-system:codex-pod"}
          # HOME (/home/codex) + all static config (incl. sshd_config +
          # authorized_keys) are baked into the image by home-manager. Runtime
          # steps: (1) plant the outbound SSH private key (secret → 0600; a mount
          # can't satisfy ssh's ownership/perms); (2) ensure a persistent host key
          # on the PVC; (3) run sshd as the long-lived process — it keeps the
          # container alive and serves inbound `ssh codex-pod` over `kubectl exec`
          # (127.0.0.1 only; see the ProxyCommand matchBlock in nix/home/home.nix).
          # `kubectl exec` still works too. tini is PID 1 (reaps zombies orphaned by
          # exec/ssh sessions — sshd wouldn't; forwards signals for clean shutdown).
          command: ["/bin/tini", "--", "/bin/bash", "-c"]
          args:
            - |
              install -D -m600 /run/codex-secret/id_ed25519 "$HOME/.ssh/id_ed25519"
              install -d -m700 /workspace/.sshd
              [ -f /workspace/.sshd/ssh_host_ed25519_key ] \
                || ssh-keygen -q -t ed25519 -N "" -f /workspace/.sshd/ssh_host_ed25519_key
              # sshd strips env; expose the secret env to ssh sessions via
              # ~/.ssh/environment (sshd_config: PermitUserEnvironment yes). kubectl
              # exec already inherits the container env, so this is ssh-only.
              install -m600 /dev/null "$HOME/.ssh/environment"
              for v in LITELLM_API_KEY BUILDBUDDY_API_KEY CODEX_LITELLM_KEY; do
                [ -n "${!v:-}" ] && printf '%s=%s\n' "$v" "${!v}" >>"$HOME/.ssh/environment"
              done
              # sshd re-execs itself for privsep, so it refuses a PATH-resolved
              # invocation ("sshd requires execution with an absolute path").
              exec /bin/sshd -D -e -f "$HOME/.ssh/sshd_config"
          env:
            # Shared BuildBuddy key (bbr reads BUILDBUDDY_API_KEY), reflected into
            # this namespace from shared-secrets/buildbuddy-api-key. Optional so a
            # brief pre-reflection window doesn't block pod start.
            - name: BUILDBUDDY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: buildbuddy-api-key
                  key: api-key
                  optional: true
            # LiteLLM virtual key (`chatgpt/oai-responses/*` models) minted by
            # tf/gitops/litellm-keys and reflected into this namespace. Codex's
            # baked config.toml points its `litellm` model_provider at this via
            # env_key=LITELLM_API_KEY, so codex routes to LiteLLM instead of a
            # ChatGPT sign-in. Optional during the pre-reflection window.
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm-key-codex-pod
                  key: api-key
                  optional: true
            # Codex-claude LiteLLM virtual key (codex-* models via LiteLLM → CLIProxyAPI),
            # minted by tf/gitops/litellm-keys and reflected into this namespace.
            - name: CODEX_LITELLM_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm-codex-clients-key
                  key: CODEX_LITELLM_KEY
                  optional: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: identity
              mountPath: /run/codex-secret
              readOnly: true
            - name: forgejo-tea
              mountPath: /home/codex/.config/tea
              readOnly: true
          resources:
            requests:
              cpu: "500m"
              memory: 1Gi
              ephemeral-storage: 2Gi
            limits:
              cpu: "4"
              memory: 8Gi
              ephemeral-storage: 20Gi
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: codex-workspace
        - name: identity
          secret:
            secretName: codex-bootstrap-identity
            defaultMode: 0444
        - name: forgejo-tea
          secret:
            secretName: forgejo-tea
            defaultMode: 0444
