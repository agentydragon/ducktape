apiVersion: v1
kind: Namespace
metadata:
  name: codex-nix-image-pod
  annotations:
    description: "Manual x/ spike for running a Codex-like environment as a pod with an image-backed Nix store."
  labels:
    app.kubernetes.io/name: codex-nix-image-pod
