apiVersion: v1
kind: Service
metadata:
  name: haku-mitmproxy
  namespace: haku-mitmproxy
spec:
  selector:
    app.kubernetes.io/name: haku-mitmproxy
  ports:
    - name: proxy
      port: 8080
      targetPort: 8080
    - name: web
      port: 8081
      targetPort: 8081
