apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - github-pat.yaml
  - serviceaccount.yaml
  - cronjob.yaml

configMapGenerator:
  - name: attic-rotators-config
    namespace: nix-cache
    files:
      - rotators.yaml
