apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: postscanmail-mcp
  namespace: postscanmail-mcp
  annotations:
    description: "Authentik-gated PostScan Mail MCP facade. Only agentydragon is allowed at the OAuth consent step (group binding in tf/gitops/agent-machine-access)."
spec:
  parentRefs:
    - name: cluster-gateway
      namespace: gateway-system
  hostnames:
    - "postscanmail-mcp.allegedly.works"
  rules:
    - backendRefs:
        - name: postscanmail-mcp
          port: 8765
      timeouts:
        request: "60s"
        backendRequest: "60s"
