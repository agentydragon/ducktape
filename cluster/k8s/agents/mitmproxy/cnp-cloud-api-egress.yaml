# Allow egress to cloud LLM API providers.
# Additive with the operator's default-deny NetworkPolicy.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-cloud-api-egress
  namespace: agents-mitmproxy
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: mitmproxy
  egress:
    # Cilium FQDN egress rules need the proxy pod to query CoreDNS first.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: ANY
          rules:
            dns:
              - matchPattern: "*"
    # The shared build-registry bucket: public package, source and toolchain
    # registries, granted all-or-none. Pinned as one set by
    # //cluster/validation:test_egress_allowlists (BUILD_REGISTRIES) -- add a host
    # there and here together, or the test fails.
    - toFQDNs:
        - matchName: bcr.bazel.build
        - matchName: cache.nixos.org
        - matchName: channels.nixos.org
        - matchName: code.forgejo.org
        - matchName: codeload.github.com
        - matchName: data.forgejo.org
        - matchName: files.pythonhosted.org
        - matchName: ftp.gnu.org
        - matchName: ghcr.io
        - matchName: github.com
        - matchName: nixos.org
        - matchName: nodejs.org
        - matchName: objects.githubusercontent.com
        - matchName: pkg-containers.githubusercontent.com
        - matchName: pypi.org
        - matchName: raw.githubusercontent.com
        - matchName: registry.npmjs.org
        - matchName: release-assets.githubusercontent.com
        - matchName: releases.bazel.build
        - matchName: snapshot.debian.org
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # Model provider APIs reached directly from sandbox pods.
    - toFQDNs:
        - matchName: generativelanguage.googleapis.com
        - matchName: api.openai.com
        - matchName: api.anthropic.com
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # Allow forwarding to any in-cluster Service. Sandbox bench Jobs and
    # other workloads keep mitmproxy in-path even when targeting cluster
    # endpoints (e.g. `ollama.ollama:11434`); without this rule mitmproxy
    # was the bottleneck because it couldn't reach the in-cluster
    # destination. NO_PROXY in the inject policy still permits explicit
    # bypass for Service FQDNs that already match (`*.svc.cluster.local`
    # and `10.0.0.0/8`).
    - toEntities:
        - cluster
      toPorts:
        - ports:
            - port: "11434"
              protocol: TCP
            - port: "80"
              protocol: TCP
            - port: "443"
              protocol: TCP
            - port: "8000"
              protocol: TCP
            - port: "8080"
              protocol: TCP
    # The loom/gym eval driver (in claude-sandbox) reaches the docker-ci DinD by
    # its public name through this proxy, which raw-tunnels it (ignore_hosts in
    # deployment.yaml) so docker mTLS passes end-to-end. Allow mitmproxy to reach
    # the daemon's mTLS port to establish that CONNECT tunnel.
    #
    # SUSPECTED DEAD RULE (2026-08-04, unverified): docker-ci.allegedly.works is
    # a TLSRoute on the hostNetwork Gateway, so it resolves to node IPs carrying
    # reserved:remote-node — which toFQDNs cannot select
    # (cluster/docs/cilium_network_policy.md). Unlike the *.allegedly.works names
    # in agents/haku-egress-proxy, port 2376 is NOT in the `toEntities: cluster`
    # port list above, so nothing else admits it and this path should be timing
    # out. Either the eval driver is not currently exercising it, or it reaches
    # the daemon some other way. Diagnose before "fixing": if the path is wanted,
    # the fix is adding 2376 to the toEntities rule, not editing this block.
    - toFQDNs:
        - matchName: docker-ci.allegedly.works
      toPorts:
        - ports:
            - port: "2376"
              protocol: TCP
