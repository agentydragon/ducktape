apiVersion: v1
kind: Service
metadata:
  name: mitmproxy
  namespace: agents-mitmproxy
spec:
  selector:
    app.kubernetes.io/name: mitmproxy
  ports:
    - name: proxy
      port: 8080
      targetPort: 8080
    - name: web
      port: 8081
      targetPort: 8081
