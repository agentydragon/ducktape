apiVersion: v1
kind: Service
metadata:
  name: plaid-db-mcp
  namespace: plaid-mcp
  labels:
    app.kubernetes.io/name: plaid-db-mcp
spec:
  selector:
    app.kubernetes.io/name: plaid-db-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
  type: ClusterIP
