apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: plaid-db-mcp
  namespace: plaid-mcp
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: plaid-db-mcp
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
