apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: logs-configmaps-reader
rules:
  - apiGroups: [""]
    resources:
      - pods/log
      - configmaps
    verbs: ["get", "list", "watch"]
