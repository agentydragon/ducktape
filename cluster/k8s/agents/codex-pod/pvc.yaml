apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: codex-home
  namespace: codex-pod
  annotations:
    description: "Persistent home + workspace for the codex pod."
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: seaweedfs-ovh
  resources:
    requests:
      storage: 80Gi
