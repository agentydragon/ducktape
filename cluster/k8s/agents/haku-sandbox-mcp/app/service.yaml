apiVersion: v1
kind: Service
metadata:
  name: haku-sandbox-mcp
  namespace: haku-sandbox-mcp
  labels:
    app.kubernetes.io/name: haku-sandbox-mcp
spec:
  selector:
    app.kubernetes.io/name: haku-sandbox-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
  type: ClusterIP
