apiVersion: v1
kind: ServiceAccount
metadata:
  name: claude-jwt-rotator
  namespace: agents-infra
  annotations:
    description: "Runs the claude-jwt-rotation CronJob. No cluster-level perms — mounts client_credentials Secret, pushes JWT to git."
