apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: claude-token-rotation
  namespace: flux-system
spec:
  interval: 10m
  path: ./cluster/k8s/agents/claude-token-rotation
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: claude-rbac
    - name: github-secrets-sync-secrets
  timeout: 2m
