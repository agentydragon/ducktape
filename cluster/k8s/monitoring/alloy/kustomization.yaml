apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  # cdk8s-generated (cluster/cdk8s/monitoring/alloy.py).
  - alloy.k8s.yaml
configMapGenerator:
  - name: alloy-config
    options:
      disableNameSuffixHash: true
    files:
      - config.alloy
