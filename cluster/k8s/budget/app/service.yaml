apiVersion: v1
kind: Service
metadata:
  name: fava
  namespace: budget
  labels:
    app.kubernetes.io/name: fava
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fava
  ports:
    - name: http
      port: 5000
      targetPort: http
      protocol: TCP
