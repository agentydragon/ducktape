apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: homeassistant-proxy-ha-token
  namespace: homeassistant-proxy
spec:
  encryptedData:
    token: FILL_WITH_KUBESEAL
  template:
    metadata:
      name: homeassistant-proxy-ha-token
      namespace: homeassistant-proxy
