apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdk8s-generated Flux Kustomization graph.
  - kustomizations.k8s.yaml

  # Hand-written: the Flux bootstrap.
  - flux-system/
  # cdk8s-generated: the ducktape-flux namespace and upstream sources.
  - ducktape-flux/
  - sources/git-repositories.k8s.yaml
