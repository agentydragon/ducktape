apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ibkr
resources:
  - paper-trading-credentials.sops.yaml
  - forgejo-images-creds-eso.yaml
  - postgres-cluster.yaml
  - service.yaml
  - deployment.yaml
  - httproute.yaml
configMapGenerator:
  - name: ibkr-mcp-config
    files:
      - config.yaml
