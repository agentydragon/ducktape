apiVersion: v1
kind: Service
metadata:
  name: atuin-server
  namespace: atuin
spec:
  selector:
    app.kubernetes.io/name: atuin
  ports:
    - name: http
      port: 8888
      targetPort: 8888
      protocol: TCP
  type: ClusterIP
