apiVersion: v1
kind: Service
metadata:
  name: agentplane-llm-ingress
  namespace: agentplane-staging
spec:
  selector:
    app.kubernetes.io/name: agentplane-llm-ingress
  ports:
    - name: http
      port: 8080
      targetPort: http
