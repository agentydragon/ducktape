apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
images:
  # The `:tag` suffix tells Setters to write back only the bare tag value -- see
  # cluster/k8s/litellm/app/image-pins/kustomization.yaml for why the bare
  # (full-reference) marker form is wrong here.
  - name: git.allegedly.works/ducktape-ci/agentplane-llm-ingress
    newTag: devel-20260917092052-c42b848 # {"$imagepolicy": "flux-system:agentplane-llm-ingress:tag"}
