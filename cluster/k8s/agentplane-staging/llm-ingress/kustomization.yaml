apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - clusterrole-token-reviewer.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: agentplane-llm-ingress-settings
    namespace: agentplane-staging
    files:
      - settings.yaml
