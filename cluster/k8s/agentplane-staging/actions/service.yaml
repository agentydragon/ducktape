apiVersion: v1
kind: Service
metadata:
  name: agentplane-actions
  namespace: agentplane-staging
spec:
  selector:
    app.kubernetes.io/name: agentplane-actions
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
