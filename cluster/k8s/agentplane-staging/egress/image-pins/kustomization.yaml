apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
images:
  # The `:tag` suffix on both entries tells Setters to write back only the bare tag
  # value -- see cluster/k8s/litellm/app/image-pins/kustomization.yaml for why the
  # bare (full-reference) marker form is wrong here.
  - name: git.allegedly.works/ducktape-ci/agentplane-egress
    newTag: devel-20260917104409-99b584a # {"$imagepolicy": "flux-system:agentplane-egress:tag"}
  - name: git.allegedly.works/ducktape-ci/agentplane-egress-migrate
    newTag: devel-20260917104411-99b584a # {"$imagepolicy": "flux-system:agentplane-egress-migrate:tag"}
