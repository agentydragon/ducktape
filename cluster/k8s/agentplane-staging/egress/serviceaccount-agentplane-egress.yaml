# The egress proxy's identity: it reads policy from the API server, verifies sandbox tokens
# against it, and writes binding status. Never mounted into a sandbox.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agentplane-egress
  namespace: agentplane-staging
