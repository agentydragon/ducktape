apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  annotations:
    description: Complete Agentplane staging environment, including namespace, database, egress, LLM ingress, Actions, app, runner template, and operator RBAC.
  name: agentplane-staging
  namespace: ducktape-flux
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age-cluster-secrets
  deletionPolicy: Orphan
  dependsOn:
    - name: agentplane-crds
    - name: agent-sandbox-controller
    - name: cert-manager-environment
    - name: cert-manager-trust
    - name: claude-rbac
    - name: cnpg
    - name: external-creds
    - name: external-secrets-config
    - name: forgejo-images
    - name: gateway
    - name: litellm-keys-tf
    - name: local-path-provisioner
    - name: reflector
    - name: sso-providers-tf
    - name: ssh-mcp
  healthCheckExprs:
    - apiVersion: postgresql.cnpg.io/v1
      current: has(status.applied) && status.applied && has(status.observedGeneration) && status.observedGeneration == metadata.generation
      kind: Database
  healthChecks:
    - apiVersion: v1
      kind: Namespace
      name: agentplane-staging
    - apiVersion: postgresql.cnpg.io/v1
      kind: Cluster
      name: postgres
      namespace: agentplane-staging
    - apiVersion: postgresql.cnpg.io/v1
      kind: Database
      name: actions
      namespace: agentplane-staging
    - apiVersion: postgresql.cnpg.io/v1
      kind: Database
      name: egress
      namespace: agentplane-staging
    - apiVersion: apps/v1
      kind: Deployment
      name: agentplane-llm-ingress
      namespace: agentplane-staging
    - apiVersion: apps/v1
      kind: Deployment
      name: agentplane-egress
      namespace: agentplane-staging
    - apiVersion: apps/v1
      kind: Deployment
      name: agentplane-app
      namespace: agentplane-staging
    - apiVersion: apps/v1
      kind: Deployment
      name: agentplane-actions
      namespace: agentplane-staging
    - apiVersion: cert-manager.io/v1
      kind: Certificate
      name: agentplane-egress-root-ca
      namespace: agentplane-staging
    - apiVersion: trust.cert-manager.io/v1alpha1
      kind: Bundle
      name: agentplane-egress-ca
    - apiVersion: v1
      kind: ConfigMap
      name: agentplane-egress-ca
      namespace: agentplane-staging
  interval: 10m
  path: ./cluster/k8s/agentplane-staging
  prune: true
  retryInterval: 1m
  sourceRef:
    kind: ExternalArtifact
    name: agentplane-staging
    namespace: ducktape-flux
  timeout: 10m
