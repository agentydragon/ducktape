apiVersion: v1
kind: Service
metadata:
  name: litellm-chatgpt
  namespace: litellm
  labels:
    app.kubernetes.io/name: litellm-chatgpt
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: litellm-chatgpt
  ports:
    - name: http
      port: 4000
      targetPort: http
      protocol: TCP
