apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: litellm
resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml

configMapGenerator:
  - name: tana-litellm-config
    options:
      disableNameSuffixHash: true
    files:
      - config.yaml=proxy-config.yaml
      - custom_handler.py=custom_handler.py
