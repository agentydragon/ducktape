apiVersion: v1
kind: Service
metadata:
  name: tana-litellm
  namespace: litellm
  labels:
    app.kubernetes.io/name: tana-litellm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tana-litellm
  ports:
    - name: http
      port: 4000
      targetPort: http
      protocol: TCP
