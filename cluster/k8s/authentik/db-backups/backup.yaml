apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: authentik-db-ovh-before-authentik-2026-8
  namespace: authentik
  annotations:
    description: One-off physical backup before the Authentik 2026.8 chart upgrade.
spec:
  cluster:
    name: authentik-db-ovh
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
