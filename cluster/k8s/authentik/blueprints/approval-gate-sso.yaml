version: 1
metadata:
  name: SSO - Approval Gate
entries:
  - model: authentik_providers_proxy.proxyprovider
    id: approval-gate-provider
    state: present
    identifiers:
      name: approval-gate
    attrs:
      external_host: "https://approval-gate.allegedly.works"
      internal_host: "http://approval-gate.approval-gate.svc.cluster.local:8765"
      mode: proxy
      authentication_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      access_token_validity: "hours=24"
      # Operator JWTs include "decide" and "read" scopes so JWTVerifier
      # can enforce the same scope model as agent JWTs.
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [name, "approval-gate: decide"]]
        - !Find [authentik_providers_oauth2.scopemapping, [name, "approval-gate: read"]]

  - model: authentik_core.application
    id: approval-gate-app
    state: present
    identifiers:
      slug: approval-gate
    attrs:
      name: Approval Gate
      provider: !KeyOf approval-gate-provider
      meta_description: "MCP tool approval queue"
      meta_launch_url: "https://approval-gate.allegedly.works"
      open_in_new_tab: true

  # Only authentik Admins may access the approval gate UI.
  - model: authentik_policies.policybinding
    state: present
    identifiers:
      target: !KeyOf approval-gate-app
      group: !Find [authentik_core.group, [name, authentik Admins]]
      order: 0
