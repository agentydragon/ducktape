apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: authentik-blueprint-gitea-secret
  namespace: flux-system
spec:
  suspend: true
  retryInterval: 1m
  interval: 10m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./cluster/k8s/authentik/blueprints/gitea-secret
  prune: true
  wait: true
  # Health check ensures OIDC secret is synced from Vault before Gitea starts
  healthChecks:
    - apiVersion: external-secrets.io/v1
      kind: ExternalSecret
      name: gitea-oauth-client-secret
      namespace: gitea
  dependsOn:
    - name: gitea-namespace
    - name: authentik-blueprint-sso-secrets # Vault data exists
    - name: external-secrets-config # Ensure ClusterSecretStore exists
