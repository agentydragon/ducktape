version: 1
metadata:
  name: SSO - Claude Code Airlock (retired)
  labels:
    blueprints.goauthentik.io/description: >-
      Tombstone for the Claude Code client that proposed actions to the retired
      Airlock MCP tool-approval service.

# CLEANUP(added 2026-07-15): Delete this file and its configMapGenerator entry
#   after neither the claude-code-airlock application nor provider appears in
#   Authentik. The absent entries must reconcile before the blueprint is removed.
entries:
  - model: authentik_core.application
    state: absent
    identifiers:
      slug: claude-code-airlock

  - model: authentik_providers_oauth2.oauth2provider
    state: absent
    identifiers:
      name: claude-code-airlock
