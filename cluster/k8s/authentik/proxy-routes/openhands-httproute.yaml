# HTTPRoute for OpenHands via shared Authentik proxy outpost.
# Traffic flows: Gateway -> outpost (auth) -> openhands backend.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: openhands
  namespace: authentik
spec:
  parentRefs:
    - name: cluster-gateway
      namespace: gateway-system
  hostnames:
    - "openhands.allegedly.works"
  rules:
    - backendRefs:
        - name: authentik-server
          port: 80
