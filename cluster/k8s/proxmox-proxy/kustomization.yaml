apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - proxmox-proxy.k8s.yaml
configMapGenerator:
  - name: proxmox-proxy-config
    namespace: proxmox-proxy
    files:
      - config/nginx.conf
