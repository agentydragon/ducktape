apiVersion: v1
kind: Service
metadata:
  name: postscanmail-mcp
  namespace: postscanmail-mcp
  labels:
    app.kubernetes.io/name: postscanmail-mcp
spec:
  selector:
    app.kubernetes.io/name: postscanmail-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  type: ClusterIP
