apiVersion: v1
kind: Service
metadata:
  name: openhands
  namespace: openhands
spec:
  selector:
    app.kubernetes.io/name: openhands
  ports:
    - name: http
      port: 3000
      targetPort: 3000
