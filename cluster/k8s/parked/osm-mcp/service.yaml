apiVersion: v1
kind: Service
metadata:
  name: osm-mcp
  namespace: osm-mcp
  labels:
    app.kubernetes.io/name: osm-mcp
spec:
  selector:
    app.kubernetes.io/name: osm-mcp
  ports:
    - name: http
      port: 7082
      targetPort: http
      protocol: TCP
  type: ClusterIP
