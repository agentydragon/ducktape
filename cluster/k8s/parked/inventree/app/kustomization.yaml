apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# TODO: add SOPS secrets for the inventree admin + db passwords, and the
# sandbox-agent password the token-provisioner reads, before unsuspending.
resources:
  - helmrelease.yaml
  - httproute.yaml
