apiVersion: v1
kind: Service
metadata:
  name: manifold-mcp
  namespace: manifold-mcp
  labels:
    app.kubernetes.io/name: manifold-mcp
spec:
  selector:
    app.kubernetes.io/name: manifold-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  type: ClusterIP
