apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: manifold-mcp
  namespace: manifold-mcp
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: manifold-mcp
  endpoints:
    - port: metrics
      path: /metrics
      scrapeTimeout: 10s
