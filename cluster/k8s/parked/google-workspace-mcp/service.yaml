apiVersion: v1
kind: Service
metadata:
  name: google-workspace-mcp
  namespace: google-workspace-mcp
spec:
  selector:
    app.kubernetes.io/name: google-workspace-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
