apiVersion: v1
kind: Namespace
metadata:
  name: codex-nix-pod
  annotations:
    description: "Manual x/ spike for running a Codex-like Nix/direnv environment as a pod."
  labels:
    app.kubernetes.io/name: codex-nix-pod
