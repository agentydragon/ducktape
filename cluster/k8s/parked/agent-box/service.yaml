apiVersion: v1
kind: Service
metadata:
  name: agent-box-ssh
  namespace: agent-box
  labels:
    app.kubernetes.io/name: agent-box
spec:
  type: ClusterIP
  selector:
    kubevirt.io/domain: agent-box
  ports:
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: 22
