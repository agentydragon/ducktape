apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ultrafeeder-data
  namespace: sdr
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path-proxmox
  resources:
    requests:
      storage: 2Gi
