apiVersion: v1
kind: Service
metadata:
  name: rtl-tcp
  namespace: sdr
spec:
  selector:
    app.kubernetes.io/name: rtl-tcp
  ports:
    - name: iq-stream
      port: 1234
      targetPort: 1234
      protocol: TCP
  type: ClusterIP
