# Evaluator credentials for props backend (read-only DB access + agent launch).
# Seal with: scripts/seal-secret.sh props props-evaluator-credentials password <openssl rand -hex 24>
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: props-evaluator-credentials
  namespace: props
spec:
  encryptedData:
    password: REPLACE_ME
  template:
    metadata:
      name: props-evaluator-credentials
      namespace: props
