apiVersion: v1
kind: Service
metadata:
  name: homeassistant-proxy
  namespace: homeassistant-proxy
spec:
  type: ClusterIP
  selector:
    app: homeassistant-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
