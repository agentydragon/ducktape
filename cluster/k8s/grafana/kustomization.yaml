apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - datasource.yaml
  - dashboard.yaml

configMapGenerator:
  - name: aiquota-history-dashboard
    namespace: monitoring
    files:
      - dashboard.json

configurations:
  - kustomizeconfig
