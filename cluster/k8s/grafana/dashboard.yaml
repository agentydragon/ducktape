apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: aiquota-history
  namespace: monitoring
spec:
  folder: Analytics
  instanceSelector:
    matchLabels:
      dashboards: grafana
  configMapRef:
    name: aiquota-history-dashboard
    key: dashboard.json
