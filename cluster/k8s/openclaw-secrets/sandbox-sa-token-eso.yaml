apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sandbox-sa-token
  namespace: openclaw
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-openclaw-sandbox-secret-store
    kind: ClusterSecretStore
  target:
    name: sandbox-sa-token
  data:
    - secretKey: token
      remoteRef:
        key: sandbox-sa-token
        property: token
    - secretKey: ca.crt
      remoteRef:
        key: sandbox-sa-token
        property: ca.crt
    - secretKey: namespace
      remoteRef:
        key: sandbox-sa-token
        property: namespace
