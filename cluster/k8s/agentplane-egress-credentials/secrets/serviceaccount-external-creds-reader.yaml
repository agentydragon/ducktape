# Consumer-owned referent identity for source-approved external credentials
# (cluster/k8s/external-creds).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-creds-reader
  namespace: agentplane-egress-credentials
