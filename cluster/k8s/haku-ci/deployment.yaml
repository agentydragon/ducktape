apiVersion: apps/v1
kind: Deployment
metadata:
  name: haku-runner
  namespace: haku-ci
  labels:
    app.kubernetes.io/name: haku-runner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: haku-runner
  template:
    metadata:
      labels:
        app.kubernetes.io/name: haku-runner
    spec:
      automountServiceAccountToken: false
      # Pin to the ACTUAL OVH workers (ks_game_worker0/1), not the OVH control planes.
      # The OVH CPs are schedulable (allowSchedulingOnControlPlanes) and region=hil, and
      # are misleadingly keyed kimsufi_worker0/1 in tofu though their role is controlplane
      # — so a plain region selector landed this runner on a control plane. That's wrong
      # twice over: (1) this is agent-controlled build compute, which must stay off
      # control-plane nodes, and (2) the user.max_user_namespaces sysctl rootless dind
      # needs is set only on role==worker nodes (cluster/terraform/main/ovh-nodes.tf), so
      # the runner must land there. Exclude any node carrying the control-plane label.
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: topology.kubernetes.io/region
                    operator: In
                    values: ["hil"]
                  - key: node-role.kubernetes.io/control-plane
                    operator: DoesNotExist
      containers:
        # The Forgejo Actions runner: registers (ephemerally) with the haku-state
        # repo via the registration token, long-polls for jobs, and runs them in
        # docker-cli job containers against the dind sidecar.
        - name: runner
          image: code.forgejo.org/forgejo/runner:6
          command: ["sh", "-c"]
          # Re-register on each start (ephemeral; no persisted .runner file/PVC),
          # then run the daemon. The registration token is repo-scoped (haku-state).
          args:
            - |
              set -e
              forgejo-runner register --no-interactive \
                --instance "$FORGEJO_INSTANCE_URL" \
                --token "$(cat /secrets/token)" \
                --name "$RUNNER_NAME" \
                --labels "haku-ci:docker://catthehacker/ubuntu:act-latest" \
                --config /config/config.yaml
              exec forgejo-runner daemon --config /config/config.yaml
          env:
            - name: FORGEJO_INSTANCE_URL
              value: http://forgejo-http.forgejo:3000
            - name: RUNNER_NAME
              value: haku-ci
            - name: DOCKER_HOST
              value: tcp://localhost:2375
            # Route the runner's external egress (fetching `uses:` actions from
            # data.forgejo.org, etc.) through haku-egress-proxy — the sole external door
            # (ccnp-force-proxy-egress.yaml). NO_PROXY keeps in-cluster Forgejo
            # (git + registry, long-poll) and the localhost dind socket direct. The
            # haku-egress-proxy CA (mounted below) is trusted via the standard per-tool CA
            # env vars.
            - name: HTTP_PROXY
              value: http://haku-egress-proxy.haku-egress-proxy.svc.cluster.local:8080
            - name: HTTPS_PROXY
              value: http://haku-egress-proxy.haku-egress-proxy.svc.cluster.local:8080
            - name: NO_PROXY
              value: 127.0.0.1,localhost,host.docker.internal,forgejo-http.forgejo,forgejo-http.forgejo.svc.cluster.local,.forgejo,.svc.cluster.local,10.0.0.0/8
            - name: SSL_CERT_FILE
              value: /egress-proxy-ca/ca-certificates.crt
            - name: CURL_CA_BUNDLE
              value: /egress-proxy-ca/ca-certificates.crt
            - name: GIT_SSL_CAINFO
              value: /egress-proxy-ca/ca-certificates.crt
            - name: REQUESTS_CA_BUNDLE
              value: /egress-proxy-ca/ca-certificates.crt
            - name: NODE_EXTRA_CA_CERTS
              value: /egress-proxy-ca/ca-certificates.crt
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: token
              mountPath: /secrets
              readOnly: true
            - name: runner-data
              mountPath: /data
            - name: egress-proxy-ca
              mountPath: /egress-proxy-ca
              readOnly: true
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 1Gi
        # Docker daemon for the builds, listening only on localhost:2375 inside the pod.
        # privileged: true is the documented requirement for docker:dind-rootless — it
        # provides /dev (incl. /dev/net/tun for RootlessKit) and disables the mount masks
        # that otherwise hide those devices from RootlessKit's nested namespaces. The
        # non-privileged path cleared seccomp (PSA) + user.max_user_namespaces (the OVH
        # worker sysctl) but still couldn't create the rootless TAP because of the masks.
        # Crucially the DAEMON still runs ROOTLESS (UID 1000 via the -rootless image), so
        # this is strictly better than classic rootful dind; the privileged POD is the
        # price, contained by haku-ci being operator-only (no Haku RBAC), off control
        # planes, and egress-fenced. See README.
        - name: dind
          image: docker:27-dind-rootless
          args:
            - --host=tcp://0.0.0.0:2375
            - --tls=false
          # --tls=false alone isn't enough: the dind entrypoint defaults DOCKER_TLS_CERTDIR
          # to /certs and re-enables TLS on 2375, so the runner's plain-HTTP client hits
          # "HTTP request to an HTTPS server". Emptying it disables TLS so 2375 is plain HTTP
          # (localhost-only inside the pod). This is the last piece that lets the runner
          # connect to the rootless daemon.
          env:
            - name: DOCKER_TLS_CERTDIR
              value: ""
            # dockerd pulls base images through haku-egress-proxy. Docker >=23 reads these
            # proxy vars from the daemon environment for registry pulls; NO_PROXY
            # keeps the in-cluster Forgejo registry push direct. dockerd is Go, so
            # SSL_CERT_FILE points its registry-TLS trust at the haku-egress-proxy CA
            # bundle (a full bundle incl. system + cluster roots), so it validates
            # the intercepted registry TLS.
            - name: HTTP_PROXY
              value: http://haku-egress-proxy.haku-egress-proxy.svc.cluster.local:8080
            - name: HTTPS_PROXY
              value: http://haku-egress-proxy.haku-egress-proxy.svc.cluster.local:8080
            - name: NO_PROXY
              value: 127.0.0.1,localhost,host.docker.internal,forgejo-http.forgejo,forgejo-http.forgejo.svc.cluster.local,.forgejo,.svc.cluster.local,10.0.0.0/8
            - name: SSL_CERT_FILE
              value: /egress-proxy-ca/ca-certificates.crt
          securityContext:
            privileged: true
            runAsUser: 1000
            runAsGroup: 1000
          volumeMounts:
            - name: docker-data
              mountPath: /var/lib/docker
            - name: egress-proxy-ca
              mountPath: /egress-proxy-ca
              readOnly: true
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "3"
              memory: 6Gi
      volumes:
        - name: config
          configMap:
            name: haku-runner-config
        - name: token
          secret:
            # Provisioned out-of-band during paving (see README): a repo-scoped
            # runner registration token for haku-state. The pod stays pending until
            # this Secret exists.
            secretName: haku-ci-runner-token
        - name: runner-data
          emptyDir: {}
        - name: docker-data
          emptyDir: {}
        # haku-egress-proxy CA trust bundle, written into this namespace by the
        # trust-manager Bundle (agents/haku-egress-proxy/trust-bundle.yaml). Mounted into
        # both the runner and dind so intercepted external TLS validates.
        - name: egress-proxy-ca
          configMap:
            name: haku-egress-proxy-ca-cert
