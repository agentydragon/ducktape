apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vpa
  namespace: kube-system
spec:
  interval: 30m
  install:
    remediation:
      retries: 3
  chart:
    spec:
      chart: vpa
      version: "5.0.1"
      sourceRef:
        kind: HelmRepository
        name: fairwinds-stable
        namespace: flux-system
      interval: 12h
  values:
    recommender:
      enabled: true
      # Checkpoint and recommendation loops need not run every minute; the
      # longer interval reduces steady etcd write churn on the HDD control plane
      # while keeping recommendations reasonably fresh.
      extraArgs:
        recommender-interval: 15m
      resources:
        requests:
          cpu: 25m
          memory: 256Mi
        limits:
          cpu: 200m
          memory: 512Mi

    updater:
      enabled: true
      resources:
        requests:
          cpu: 25m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi

    admissionController:
      enabled: true
      resources:
        requests:
          cpu: 25m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
