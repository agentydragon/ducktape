apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: langfuse-log-reader
  namespace: langfuse
rules:
  - apiGroups: [""]
    resources:
      - pods
      - pods/log
      - services
      - configmaps
      - events
    verbs: ["get", "list", "watch"]
