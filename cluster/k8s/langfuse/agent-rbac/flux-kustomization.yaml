apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: langfuse-agent-rbac
  namespace: ducktape-flux
  annotations:
    description: Agent RBAC for langfuse namespace. Lightweight — only RoleBindings. Depends on namespace existing + agent RBAC base for ClusterRoles.
spec:
  interval: 10m
  path: ./cluster/k8s/langfuse/agent-rbac
  prune: true
  sourceRef:
    kind: ExternalArtifact
    name: langfuse-agent-rbac
    namespace: ducktape-flux
  timeout: 2m
  dependsOn:
    - name: langfuse-namespace
      namespace: ducktape-flux
    - name: claude-rbac
      namespace: ducktape-flux
