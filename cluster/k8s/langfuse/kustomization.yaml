apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdk8s-generated (cluster/cdk8s/langfuse/app.py).
  - langfuse.k8s.yaml
  # Hand-written: cdk8s drops the null Helm values it sets.
  - helmrelease.yaml
  - langfuse-secrets.sops.yaml
