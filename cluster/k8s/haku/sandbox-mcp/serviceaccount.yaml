# Dedicated identity for the reviewed in-process haku_sandbox tools. The Deployment keeps
# automountServiceAccountToken=false and projects this token only into its server container, so the
# static nginx sidecar receives no Kubernetes credential.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: haku-console-sandbox
  namespace: haku-console
automountServiceAccountToken: false
