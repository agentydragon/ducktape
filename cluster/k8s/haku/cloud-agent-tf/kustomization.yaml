apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-creds-reader.yaml
  - anthropic-api-key-eso.yaml
  # Seeded here; authentik-jwt-rotation overwrites it on each mint (rotations.yaml).
  - haku-kube-token.sops.yaml
  - terraform.yaml
