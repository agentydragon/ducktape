apiVersion: v1
kind: ServiceAccount
metadata:
  name: haku-scanner
  namespace: haku-sandbox
  annotations:
    description: "Identity for the Haku scanner CronJob (see ducktape haku/PLAN.md). Drives its own haku-sandbox namespace via the haku-sandbox-admin Role; nothing else."
