apiVersion: v1
kind: ServiceAccount
metadata:
  name: haku-scanner
  namespace: haku
  annotations:
    description: "Identity for the Haku scanner CronJob (see ducktape haku/PLAN.md). Reads its own namespace's secrets/configmaps via the haku-secret-reader Role; nothing else."
