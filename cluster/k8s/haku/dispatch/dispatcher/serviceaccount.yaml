# The ONLY principal that can create Jobs/Secrets in zone namespaces
# (rbac-zone-zai.yaml) — credential placement is the load-bearing part of the
# dispatch design: Haku holds only the dispatcher endpoint, never this SA.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dispatcher
  namespace: haku-dispatch
