apiVersion: v1
kind: Service
metadata:
  name: workers-litellm
  namespace: haku-dispatch
spec:
  selector:
    app.kubernetes.io/name: workers-litellm
  ports:
    - name: http
      port: 4000
      targetPort: 4000
