# The Haku console: reviewed FastAPI API code plus a baked nginx static frontend.
# The console is the trusted outer shell — it serves config + the capability tier
# (launch-routine) and frames Haku's own UI (haku-ui) as a sandboxed cross-origin
# iframe. It holds NO haku-state git credential (feedback/trace writes moved into
# haku-ui). nginx serves the bundled SPA image and proxies API calls to localhost.
# The app owns its auth: the operator browser logs in via Authentik OIDC
# (operator_oidc -> signed session cookie, guarded by OperatorSessionMiddleware) and
# agents authenticate to /mcp via MultiAuth (OIDCProxy DCR + static bearer). The
# forward-auth proxy outpost that used to gate haku.allegedly.works is retired; the
# HTTPRoute now points straight at this Service (single-user access is Authentik's
# application access policy on the console's OAuth2 providers).
#
# Trust boundary: runs in its OWN `haku-console` namespace, NOT haku-sandbox. As
# reviewed/released ducktape code it sits outside Haku's RBAC (Haku can't read its
# secrets/logs or patch it) and outside the haku-egress-proxy egress fence (which keys
# on the haku-sandbox namespace) — so it gets ordinary egress and can later hold
# secrets Haku may not read. See haku/PLAN.md → "The agent-authored console". It
# still runs locked down (uid/gid 1000, non-root, no service-account token, all
# capabilities dropped). readOnlyRootFilesystem is intentionally NOT set: the aspect
# py_binary launcher materializes its venv on the rootfs at startup (same as
# grocy-mcp), so the root fs must be writable.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: haku-console
  namespace: haku-console
  labels:
    app.kubernetes.io/name: haku-console
  annotations:
    # Roll the pod when the TF-minted OAuth client secrets rotate (haku-console-oidc).
    reloader.stakater.com/auto: "true"
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    # maxUnavailable: 0 means the new pod must be Ready before an old one is torn
    # down, so a deploy never drops to zero endpoints (unlike the single-replica
    # Recreate strategy this replaces, which always had a brief full outage).
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: haku-console
  template:
    metadata:
      labels:
        app.kubernetes.io/name: haku-console
    spec:
      automountServiceAccountToken: false
      # Don't inject the kubelet's {SVCNAME}_* service-link env vars: the haku-console
      # Service's HAKU_CONSOLE_PORT=tcp://<clusterIP>:8080 collides with the app's
      # HAKU_CONSOLE_ settings prefix (it parsed into the port:int field → crash).
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: server
          image: ghcr.io/agentydragon/haku-console:devel-20260712125643-2f7a60e # {"$imagepolicy": "flux-system:haku-console"}
          imagePullPolicy: Always
          ports:
            - name: api
              containerPort: 8080
          env:
            # Capability tier: the launch-routine action. The console builds both the
            # (public) fire URL and the claude.ai/code page deep-link from this routine
            # (trigger) id; the bearer comes from the haku-console-only secret so Haku
            # can't read it. See haku/PLAN.md → "The agent-authored console".
            - name: HAKU_CONSOLE_LAUNCH_ROUTINE__ROUTINE_ID
              value: trig_0158pCMU1XBhoALBWikwSyK4
            - name: HAKU_CONSOLE_LAUNCH_ROUTINE__TOKEN
              valueFrom:
                secretKeyRef:
                  name: haku-routine-launch-token
                  key: token
            # Free-form UI (Phase 1b): the Authentik-gated origin of Haku's own UI
            # service (haku-sandbox). The console embeds it in a sandboxed cross-origin
            # iframe and sets a CSP frame-src for it; it never renders Haku's UI itself.
            # Lights up once the haku-ui workload serves (cluster/k8s/haku/workloads +
            # the haku-ui Authentik route). See haku/console/docs/containment.md.
            - name: HAKU_CONSOLE_HAKU_UI_URL
              value: https://haku-ui.allegedly.works
            - name: HAKU_CONSOLE_AUTH_ORIGIN
              value: https://auth.allegedly.works
            - name: HAKU_CONSOLE_PUBLIC_BASE_URL
              value: https://haku.allegedly.works
            - name: HAKU_CONSOLE_CONFIG_FILE
              value: /etc/haku-console/config/config.yaml
            - name: HAKU_CONSOLE_DB_USER
              valueFrom:
                secretKeyRef:
                  name: haku-console-db-app
                  key: username
            - name: HAKU_CONSOLE_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: haku-console-db-app
                  key: password
            - name: HAKU_CONSOLE_DB_HOST
              valueFrom:
                secretKeyRef:
                  name: haku-console-db-app
                  key: host
            - name: HAKU_CONSOLE_DB_PORT
              valueFrom:
                secretKeyRef:
                  name: haku-console-db-app
                  key: port
            - name: HAKU_CONSOLE_DB_NAME
              valueFrom:
                secretKeyRef:
                  name: haku-console-db-app
                  key: dbname
            - name: HAKU_CONSOLE_DATABASE_URL
              value: "postgresql+psycopg://$(HAKU_CONSOLE_DB_USER):$(HAKU_CONSOLE_DB_PASSWORD)@$(HAKU_CONSOLE_DB_HOST):$(HAKU_CONSOLE_DB_PORT)/$(HAKU_CONSOLE_DB_NAME)"
            # Haku's own backend static agent: the `haku` entry in config.yaml's `static_agents`
            # references these two env vars — the bearer (below) and the operator subject it acts as
            # (further down). The agent id `haku` is the audit caller_principal and /mcp client_id.
            - name: HAKU_CONSOLE_AGENT_HAKU_TOKEN
              valueFrom:
                secretKeyRef:
                  name: haku-console-agent-api
                  key: token
            # CSRF double-submit signing key, shared across replicas (see
            # csrf-secret.sops.yaml) — without it each pod would sign/validate with
            # its own ephemeral secret and cross-pod requests would randomly 403.
            - name: HAKU_CONSOLE_CSRF_SECRET
              valueFrom:
                secretKeyRef:
                  name: haku-console-csrf-secret
                  key: secret
            # Two in-process MCP servers built from this one token: `gmail` (Gmail reads
            # mirroring the REST API, draft creation, thread-label changes, label CRUD — see
            # haku/console/tools/gmail.py) and `google_calendar` (event creation — google_calendar.py).
            # haku-console-google-access-token (access_token + expires_at) is written
            # by Airlock's haku_console_google provider into the airlock namespace and
            # mirrored here by ESO; re-read via google-auth's refresh_handler, no
            # restart needed on rotation.
            - name: HAKU_CONSOLE_GOOGLE_TOKEN_DIR
              value: /etc/haku-console-google-token
            # Operator-facing origin, used to build the deep-link `url` the /mcp promise +
            # read tools return (https://haku.allegedly.works/tool-calls/<id>, Authentik-gated).
            - name: HAKU_CONSOLE_UI_BASE_URL
              value: https://haku.allegedly.works
            # The operator the `haku` static agent acts as when it reaches an operator_oauth server:
            # reflecting grocy-sf/tana-rw/kubectl schemas and executing its own auto-approved
            # operator_oauth calls (a machine agent has no operator token of its own). The value is the
            # operator's opaque OIDC subject (the associations key); TF injects it from the Authentik
            # user id into haku-console-oidc, never a username.
            - name: HAKU_CONSOLE_AGENT_HAKU_OPERATOR
              valueFrom:
                secretKeyRef:
                  name: haku-console-oidc
                  key: operator_subject
            # Agent-facing MCP OAuth: an Authentik-backed OIDCProxy (DCR + PKCE) for claude.ai /
            # the `claude` CLI on /mcp, composed with the static agent bearer via MultiAuth. The
            # provider + client secret are minted by tf/gitops/agent-machine-access (application
            # slug `haku-console-mcp`, redirect https://haku.allegedly.works/mcp/auth/callback).
            # public_base_url is the console origin (root, not /mcp) — matching airlock; FastMCP's
            # OIDCProxy is mount-aware for its callback.
            - name: HAKU_CONSOLE_MCP_OAUTH__OIDC_ISSUER
              value: https://auth.allegedly.works/application/o/haku-console-mcp/
            - name: HAKU_CONSOLE_MCP_OAUTH__PUBLIC_BASE_URL
              value: https://haku.allegedly.works
            - name: HAKU_CONSOLE_MCP_OAUTH__OIDC_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: haku-console-oidc
                  key: mcp_client_id
            - name: HAKU_CONSOLE_MCP_OAUTH__OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: haku-console-oidc
                  key: mcp_client_secret
            # OIDCProxy dynamic-client-registration + token state, shared across the 2 replicas so
            # a claude.ai DCR registration survives a re-schedule (the file default would strand it
            # per-pod). Backed by the console's own Postgres (py-key-value's PostgreSQLStore
            # auto-creates its table), so there is no separate valkey to run. The URL is the asyncpg
            # form (plain postgresql://), NOT the SQLAlchemy postgresql+psycopg:// the ORM uses.
            - name: HAKU_CONSOLE_MCP_OAUTH_PERSISTENCE__KIND
              value: postgres
            - name: HAKU_CONSOLE_MCP_OAUTH_PERSISTENCE__URL
              value: "postgresql://$(HAKU_CONSOLE_DB_USER):$(HAKU_CONSOLE_DB_PASSWORD)@$(HAKU_CONSOLE_DB_HOST):$(HAKU_CONSOLE_DB_PORT)/$(HAKU_CONSOLE_DB_NAME)"
            # Operator browser login (Authentik OIDC), replacing the forward-auth proxy outpost:
            # the console authenticates the operator itself (authorization-code -> signed session
            # cookie) and OperatorSessionMiddleware guards /api/*. Provider + secrets minted by
            # tf/gitops/agent-machine-access (application slug `haku-console`, redirect
            # https://haku.allegedly.works/auth/callback). Setting this activates the app's own
            # auth — it lands together with the HTTPRoute swap that removes the outpost.
            - name: HAKU_CONSOLE_OPERATOR_OIDC__ISSUER
              value: https://auth.allegedly.works/application/o/haku-console/
            - name: HAKU_CONSOLE_OPERATOR_OIDC__CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: haku-console-oidc
                  key: operator_client_id
            - name: HAKU_CONSOLE_OPERATOR_OIDC__CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: haku-console-oidc
                  key: operator_client_secret
            - name: HAKU_CONSOLE_OPERATOR_OIDC__SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: haku-console-oidc
                  key: operator_session_secret
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            tcpSocket:
              port: api
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: api
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp # writable scratch (/tmp) for the app
              mountPath: /tmp
            - name: config
              mountPath: /etc/haku-console/config
              readOnly: true
            - name: haku-console-google-token
              mountPath: /etc/haku-console-google-token
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
        - name: static
          image: ghcr.io/agentydragon/haku-console-static:devel-20260712223509-1503d5e # {"$imagepolicy": "flux-system:haku-console-static"}
          imagePullPolicy: Always
          env:
            # The nginx entrypoint renders /etc/nginx/templates/default.conf.template
            # with these origins at startup, keeping frame-src environment-specific
            # while the console itself remains unframeable via frame-ancestors 'none'.
            - name: HAKU_CONSOLE_HAKU_UI_URL
              value: https://haku-ui.allegedly.works
            - name: HAKU_CONSOLE_AUTH_ORIGIN
              value: https://auth.allegedly.works
          ports:
            - name: http
              containerPort: 8081
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            # The SPA is baked into the image, but the nginx entrypoint still writes
            # the envsubst-rendered runtime config here before nginx starts.
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: tmp
          emptyDir: {}
        - name: config
          configMap:
            name: haku-console-config
        - name: haku-console-google-token
          # optional: true so a missing token (before the one-time operator OAuth
          # consent below) never blocks the whole console pod from starting — only
          # the in-process `gmail` + `google_calendar` MCP servers degrade
          # (google_token_dir empty -> their FastMCP instances never registered ->
          # those capability entries report `degraded`, everything else unaffected).
          secret:
            secretName: haku-console-google-access-token
            optional: true
        - name: nginx-conf
          emptyDir: {}
