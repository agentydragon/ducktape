apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: haku-console
  namespace: haku-console
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: haku-console
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
