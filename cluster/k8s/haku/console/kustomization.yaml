apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: haku-console

resources:
  - deployment.yaml
  - mcp-approval-store-pvc.yaml
  - routine-launch-token.sops.yaml
  - service.yaml

configMapGenerator:
  - name: haku-console-mcp-servers
    files:
      - mcp-servers.yaml=mcp-servers.txt
