# Hand-written: carries the Flux image-automation marker the generated manifests cannot
# (cluster/cdk8s/AGENTS.md § image-pins/kustomization.yaml). The same policy as the API
# Deployment's: the migration must run from the exact image that will serve.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
images:
  - name: git.allegedly.works/ducktape-ci/haku-console
    newTag: devel-20260918220217-f291352 # {"$imagepolicy": "flux-system:haku-console:tag"}
