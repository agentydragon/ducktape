apiVersion: v1
kind: Service
metadata:
  name: haku-console
  namespace: haku-sandbox
  labels:
    app.kubernetes.io/name: haku-console
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: haku-console
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
