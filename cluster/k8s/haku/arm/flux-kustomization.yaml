apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: haku-arm
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  path: ./cluster/k8s/haku/arm
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: haku-namespace
    - name: haku-rbac
    - name: haku-state # provisions haku-state-git-write in the haku-sandbox namespace
    - name: haku-mitmproxy
    - name: gateway
    - name: authentik
