apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # cdk8s-generated (cluster/cdk8s/github_exporter/app.py).
  - github-exporter.k8s.yaml
components:
  - ./image-pins

configMapGenerator:
  - name: github-exporter-dashboard
    namespace: monitoring
    files:
      - dashboard.json

configurations:
  - kustomizeconfig
