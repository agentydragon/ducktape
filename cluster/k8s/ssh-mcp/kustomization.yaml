apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ssh-mcp
resources:
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
configMapGenerator:
  - name: ssh-mcp-config
    files:
      - settings.yaml
      - known_hosts
