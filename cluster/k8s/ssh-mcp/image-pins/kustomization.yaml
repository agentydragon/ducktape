# Hand-written: carries the Flux image-automation marker the generated Deployment cannot.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
images:
  - name: git.allegedly.works/ducktape-ci/ssh-mcp
    newTag: unset # {"$imagepolicy": "flux-system:ssh-mcp:tag"}
