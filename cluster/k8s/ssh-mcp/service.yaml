apiVersion: v1
kind: Service
metadata:
  name: ssh-mcp
  namespace: ssh-mcp
spec:
  selector:
    app.kubernetes.io/name: ssh-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
