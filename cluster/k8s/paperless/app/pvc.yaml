apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-data
  namespace: paperless
spec:
  accessModes:
    - ReadWriteOnce
  # Replicated/distributed storage so the pod is not node-pinned and can
  # reschedule across OVH nodes (see cluster/AGENTS.md § Storage Selection).
  storageClassName: seaweedfs-ovh-hdd
  resources:
    requests:
      storage: 50Gi
