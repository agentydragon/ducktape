apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The Flux image-automation and image-reflector controllers are deployed as part
# of the flux-system bootstrap (gotk-components.yaml). This kustomization is a
# dependency marker so other kustomizations using ImagePolicy/ImageRepository/
# ImageUpdateAutomation can declare a dependsOn relationship.
resources: []
