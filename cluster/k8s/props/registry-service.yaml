apiVersion: v1
kind: Service
metadata:
  name: props-registry
  namespace: props
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app.kubernetes.io/name: props
    component: registry
