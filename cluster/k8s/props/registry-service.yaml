apiVersion: v1
kind: Service
metadata:
  name: props-registry
  labels:
    app: props-registry
    app.kubernetes.io/component: registry
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: props-registry
    app.kubernetes.io/component: registry
