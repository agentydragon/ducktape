apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: props-backup
  namespace: props
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: proxmox-csi-retain
