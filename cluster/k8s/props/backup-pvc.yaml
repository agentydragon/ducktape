apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: props-backup
  labels:
    app: props-backup
    app.kubernetes.io/component: backup
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-csi-retain
  resources:
    requests:
      storage: 5Gi
