apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - jwt-token.sops.yaml
  - bucket.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml

configMapGenerator:
  - name: attic-config
    namespace: nix-cache
    files:
      - server.toml
