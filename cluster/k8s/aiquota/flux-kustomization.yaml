apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: aiquota
  namespace: ducktape-flux
  annotations:
    description: aiquota API with Codex quota through the CLIProxyAPI integration.
spec:
  retryInterval: 1m
  interval: 10m
  path: ./cluster/k8s/aiquota
  prune: true
  sourceRef:
    kind: GitRepository
    name: ducktape
    namespace: ducktape-flux
  timeout: 5m
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age-cluster-secrets
  dependsOn:
    # Provides the shared namespace and the CLIProxyAPI integration Secret.
    - name: cli-proxy-api
      namespace: ducktape-flux
    - name: haku-egress-proxy
      namespace: ducktape-flux
