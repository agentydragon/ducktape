apiVersion: v1
kind: Service
metadata:
  name: inventree-postgres
  namespace: inventree
spec:
  selector:
    app: inventree-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  type: ClusterIP
