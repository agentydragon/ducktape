apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: inventree-postgres
  namespace: inventree
  labels:
    app: inventree-postgres
spec:
  serviceName: inventree-postgres
  replicas: 1
  selector:
    matchLabels:
      app: inventree-postgres
  template:
    metadata:
      labels:
        app: inventree-postgres
    spec:
      nodeSelector:
        topology.kubernetes.io/region: proxmox
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: inventree
            - name: POSTGRES_USER
              value: inventree
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: inventree-db-password
                  key: db_password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - inventree
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - inventree
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: postgres-storage
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: proxmox-csi-retain
        resources:
          requests:
            storage: 5Gi
