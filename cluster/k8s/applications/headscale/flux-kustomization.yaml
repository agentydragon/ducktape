apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: headscale
  namespace: flux-system
spec:
  interval: 10m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: "./cluster/k8s/applications/headscale"
  prune: true
  wait: true
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cert-manager-issuer-config
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: headscale
      namespace: headscale
  dependsOn:
    - name: cert-manager-issuer-config
    - name: headscale-namespace
    - name: headscale-secrets # headscale-db-values secret (DB password) must exist
    - name: gateway
    - name: cert-manager-environment
    - name: authentik # OIDC provider blueprint must be applied before headscale starts
    - name: authentik-blueprint-headscale-secret # OIDC client secret must exist before deploy
