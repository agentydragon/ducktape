apiVersion: v1
kind: Service
metadata:
  name: github-api-proxy
  namespace: github-api-proxy
spec:
  selector:
    app.kubernetes.io/name: github-api-proxy
  ports:
    - name: proxy-tls
      port: 443
      targetPort: proxy
      protocol: TCP
