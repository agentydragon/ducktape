apiVersion: v1
kind: Service
metadata:
  name: gecko-ssh
  namespace: gecko
  labels:
    app.kubernetes.io/name: gecko
spec:
  type: ClusterIP
  selector:
    kubevirt.io/domain: gecko
  ports:
    - name: ssh
      protocol: TCP
      port: 22
      targetPort: 22
