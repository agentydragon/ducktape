apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: agentplane-action-service
  namespace: flux-system
spec:
  image: git.allegedly.works/ducktape-ci/agentplane-action-service
  interval: 5m
  secretRef:
    name: forgejo-images-creds
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: agentplane-action-service
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: agentplane-action-service
  filterTags:
    pattern: '^devel-\d{14}-[0-9a-f]{7}$'
  policy:
    alphabetical:
      order: asc
