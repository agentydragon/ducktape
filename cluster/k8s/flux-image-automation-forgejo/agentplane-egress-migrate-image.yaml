apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: agentplane-egress-migrate
  namespace: flux-system
spec:
  image: git.allegedly.works/ducktape-ci/agentplane-egress-migrate
  interval: 5m
  secretRef:
    name: forgejo-images-creds
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: agentplane-egress-migrate
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: agentplane-egress-migrate
  filterTags:
    pattern: '^devel-\d{14}-[0-9a-f]{7}$'
  policy:
    alphabetical:
      order: asc
