apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: homeassistant-provisioner
  namespace: flux-system
spec:
  image: git.allegedly.works/ducktape-ci/homeassistant-provisioner
  interval: 5m
  secretRef:
    name: forgejo-images-creds
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: homeassistant-provisioner
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: homeassistant-provisioner
  filterTags:
    pattern: '^devel-\d{14}-[0-9a-f]{7}$'
  policy:
    alphabetical:
      order: asc
