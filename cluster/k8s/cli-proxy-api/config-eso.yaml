# CLIProxyAPI requires a single config file. ESO renders its API key from a
# dedicated SOPS-managed Secret, so this template remains safe to review and edit.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cli-proxy-api-config
  namespace: cli-proxy-api
  annotations:
    description: >-
      CLIProxyAPI config.yaml rendered from its dedicated API key Secret. Retry an upstream stream up
      to three times only before its first response byte reaches the caller.
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-cli-proxy-api-secret-store
    kind: ClusterSecretStore
  target:
    name: cli-proxy-api-config
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        config.yaml: |
          host: "0.0.0.0"
          port: 8317
          auth-dir: "/data/auth"
          api-keys:
            - "{{ .api_key }}"
          debug: false
          streaming:
            bootstrap-retries: 3
  data:
    - secretKey: api_key
      remoteRef:
        key: cli-proxy-api-config-key
        property: api-key
