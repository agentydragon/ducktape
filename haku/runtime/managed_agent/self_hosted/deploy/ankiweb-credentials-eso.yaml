# Copy the external-creds-managed AnkiWeb credentials from ducktape-flux into
# haku-sandbox without duplicating the credential values in git.
apiVersion: external-secrets.io/v1
kind: ClusterExternalSecret
metadata:
  name: haku-ankiweb-credentials
spec:
  namespaces:
    - haku-sandbox
  externalSecretSpec:
    refreshInterval: 1m
    secretStoreRef:
      name: kubernetes-external-creds-secret-store
      kind: ClusterSecretStore
    target:
      name: ankiweb-credentials
    data:
      - secretKey: email
        remoteRef:
          key: ankiweb-credentials
          property: email
      - secretKey: password
        remoteRef:
          key: ankiweb-credentials
          property: password
