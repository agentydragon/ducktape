# memory

Haku's durable memory — keep here whatever your future self needs: your model of the
operator, live situational awareness, per-source bookmarks, a research/ideas backlog,
and the base-sync pin. It is **yours to structure** (prose is fine, not
machine-readable); these files are only a starting scaffold — rename, split, or
replace them. The durable contract for what belongs here is `instructions.md` →
_Continuity_.
