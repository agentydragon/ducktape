load("//devinfra/python:defs.bzl", "py_library")

# The hostexec wire surface: RunAsUser (shared with the console tool) and the hostexecd HTTP body.
py_library(
    name = "wire",
    srcs = ["wire.py"],
    visibility = ["//visibility:public"],
    deps = ["@pypi//pydantic"],
)
