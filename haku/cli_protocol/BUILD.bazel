load("//devinfra/python:defs.bzl", "py_test")

py_test(
    name = "test_compaction_capture",
    srcs = ["test_compaction_capture.py"],
    data = ["testdata/compaction_session.jsonl"],
    deps = [
        "//:conftest",
        "//util/bazel:runfiles",
        "@pypi//pytest",
        "@pypi//pytest_bazel",
    ],
)
