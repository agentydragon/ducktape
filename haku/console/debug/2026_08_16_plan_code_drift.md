# The chat-runtime plans, read against the code

A day of parallel landings (#4104 through #4137) moved the runtime faster than the documents
describing it. This is the audit: every substantive claim the chat-runtime plans make about how the
code is **shaped**, checked against `devel` at `fc1f28afc`, with a verdict on which side was wrong.

The discrimination that matters is the verdict column. A plan is a specification, so where the code
diverged from a decision the plan had already argued through, the **code** is the thing that moves —
rewriting the plan to describe whatever was built destroys the only record of what was intended.
Where the plan simply described the world and the world changed, the plan is burned down.

## The table

| #   | What the plan says                                                                                                                                                                                | What the code does                                                                                                                                                                              | Which is right                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | <../../plans/chat_runtime_projection.md> § The shape: a fold `project(state, frame) -> (state, effects)` read from a per-session cursor, so **live and recovery are one code path**               | `x/claude_projection.py` (arriving with #4134) is `project(frames) -> Projection`: stateless over a whole sequence, cross-frame state private to the call and discarded. Nothing on `devel` yet | **Plan.** Re-runnable is not resumable, and a projection that cannot resume from a cursor leaves recovery a second implementation. Known; being fixed on `claude/haku-projection-reducer`, stacked on #4134 |
| 2   | Same file § The projection is not a one-way door: "That is #4105", written as pending                                                                                                             | Landed. Migration `0045` adds `session_messages.source_{first,last}_frame_seq`, surfaced on `SessionMessageView`                                                                                | Plan stale — burned down                                                                                                                                                                                    |
| 3   | Same §: `ADD CONSTRAINT … CHECK (…) NOT VALID` requiring a range on new and updated rows, "when provenance lands"                                                                                 | `0045` shipped only the ordering check (`first ≤ last` when both are present). A row with no range is still writable                                                                            | **Plan.** The requirement stands and is unbuilt; marked as owed rather than quietly dropped                                                                                                                 |
| 4   | Same §: backfill "falls out of the reprojection tool rather than being its own archaeology"                                                                                                       | `0045` backfills by joining the agent's own `message.id` against `assistant` frames                                                                                                             | **Both, and they compose.** The migration filled exactly the unambiguous case; the rows with no agent id — the population the neutral message exists to fix — are still the reprojection tool's job         |
| 5   | Same file § stage 1: "One thing it leaves owed: **measure the delta cost**" before stage 2 commits to a row per delta                                                                             | Measured, by the production census (#4114, <frame_shape_census.md>)                                                                                                                             | Plan stale — burned down, citing the census rather than copying its numbers into plan prose                                                                                                                 |
| 6   | Same file § 5 "What it does not close": the abort drain discarding a produced reply (E3) and a batch acknowledged at enqueue (I3) are open                                                        | E3 fixed (#4109), I3 fixed (#4117). <message_drops.md> is clear apart from I2's residual windows, which it records as re-deliveries and never skips                                             | Plan stale — burned down                                                                                                                                                                                    |
| 7   | Same file § 4, reading as unstarted work                                                                                                                                                          | In flight on two branches — #4127 (neutral vocabulary + Claude adapter) and #4134 (turn loop on the projection). All four interpreters the section counts are still on `devel`                  | Plan stale — status line added; the section's content is unchanged and still the target                                                                                                                     |
| 8   | <../plans/session_channels.md> parity table: console live updates are "**no** — one fetch on mount"                                                                                               | #4132: `SessionChangedEvent` plus `x/session_live_updates.py`, coalesced per session; list and detail both re-read                                                                              | Plan stale — burned down. The file's § 4 already said "done"; the table two screens above it did not, which is drift internal to one document                                                               |
| 9   | <../plans/one_read_api.md> Stage 3: add `SessionChangedEvent`, then retire `/api/sessions/{id}/stream`                                                                                            | First half landed (#4132). The SSE route is still mounted and `/chat` still holds it, so there are two live-update mechanisms                                                                   | Plan stale in status only — marked half-landed, with the deletion gated on the page merge                                                                                                                   |
| 10  | Same file § 3: `list_conversations`' MCP payload is "`+ error`, no aggregates"                                                                                                                    | Both `Conversation` (MCP) and `ConversationSessionSummary` (REST) carry `error`                                                                                                                 | **Plan wrong** — corrected. `error` is not a difference; the aggregates are                                                                                                                                 |
| 11  | Same § 3: MCP orders "newest first"                                                                                                                                                               | MCP orders `created_at DESC`; REST orders `updated_at DESC, session_id DESC`                                                                                                                    | **Plan imprecise** — corrected. Two different meanings of newest, and a running session sorts differently under each                                                                                        |
| 12  | Same § 3: neither side has a cursor                                                                                                                                                               | True on `devel`. #4137 (open) adds a keyset cursor to `list_conversations`                                                                                                                      | Plan right today — pointer to the open PR added                                                                                                                                                             |
| 13  | <../../plans/matrix_chat_runtime.md> Phase 5 item 4: two tools, "both tools in the Haku agent's unconditional auto-approval set"                                                                  | Four: `list_conversations`, `read_rollout`, `list_turns`, `read_frame` (#4116). `haku_recall_reads` lists all four, plus `haku_index`'s two                                                     | Plan stale — burned down                                                                                                                                                                                    |
| 14  | Same file R11.6 [v1]: a late reply is "marked as possibly duplicated"                                                                                                                             | The outbox (#4104) retries; the marking is deliberately absent, because every row is sent under its own stable transaction id and a late redelivery is refused rather than duplicated           | Plan stale — re-marked `[built, minus the marking]` with the reason and the condition that brings it back (a channel with no idempotency key)                                                               |
| 15  | Same file, Phase 5 payoff: "a replacement session today gets the last N messages of **its own** transcript"                                                                                       | #4136: `matrix_session.RoomTranscript` reads by **room**, across every session that served it, out of our own record and excluding the new session's own rows                                   | Plan stale — burned down                                                                                                                                                                                    |
| 16  | <../../plans/chat_runtime_cleanup.md> § Anytime: the whole-session re-read is "paid only while the SPA is streaming"                                                                              | Still true, and #4132 gave the same read a second consumer: every invalidation refetches the whole conversation, per open tab                                                                   | Plan incomplete — extended. The operator's stream-the-increment direction is recorded as future work, not implemented                                                                                       |
| 17  | Same § "Split `claude_chat.py` further", implying the file is being reduced                                                                                                                       | 2,233 lines, up from ~1,925 after the leaf split — provenance and the outbox seam put it back                                                                                                   | Plan incomplete — note added; it is an argument for the store/service split, not against it                                                                                                                 |
| 18  | `next_month.md` § Where each module lands (PR #4078) — the `haku/console/x/` inventory                                                                                                            | Predates `matrix_outbox.py` (#4104) and `session_live_updates.py` (#4132), and lists `synapse_container.py` / `stub_claude.py` at `x/` where both already sit in `x/testing/`                   | **Plan**, and out of scope to edit: #4078 is explicitly not a merge target. Reported for whoever runs the reorg                                                                                             |
| 19  | Same file: the prose settles the harness directory on **`claude_code/`** and argues explicitly against `claude/`; the table one screen later routes `claude_chat.py`'s frame `match` to `claude/` | n/a — the file contradicts itself                                                                                                                                                               | **Plan.** The prose is the reasoned half; the table row is the leftover. The reorg should follow `claude_code/`                                                                                             |

## Two things this deliberately did not chase

**The layout reorg is in flight.** An agent is moving `x/` modules into `claude_code/` and
`channels/matrix/` on `claude/haku-runtime-layout`. Rows 18 and 19 are about the _plan's_ table, not
about where the files should end up; correcting module paths while they are moving would be racing a
branch for no gain.

**Row 1 is not a plan edit.** The projection plan's § The shape is untouched apart from a note
recording the divergence and where it is being fixed. Editing it to describe
`project(frames) -> Projection` would have deleted the argument for the cursor, which is the whole
reason the stage exists.
