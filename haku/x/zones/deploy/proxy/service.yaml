apiVersion: v1
kind: Service
metadata:
  name: haku-zones-mitmproxy
  namespace: haku-zones-mitmproxy
spec:
  selector:
    app.kubernetes.io/name: haku-zones-mitmproxy
  ports:
    - name: proxy
      port: 8080
      targetPort: 8080
    - name: web
      port: 8081
      targetPort: 8081
