apiVersion: v1
kind: Service
metadata:
  name: dispatcher
  namespace: haku-dispatch
  labels:
    app.kubernetes.io/name: dispatcher
spec:
  selector:
    app.kubernetes.io/name: dispatcher
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
