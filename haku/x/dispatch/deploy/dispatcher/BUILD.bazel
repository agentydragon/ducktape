# The Job template is data for haku/x/dispatch's stamping parity test.
exports_files(
    [
        "job-template.yaml",
        "zones.yaml",
    ],
    visibility = ["//haku/x/dispatch:__pkg__"],
)

# Deployment manifests for cluster/validation's focused dispatch render test.
filegroup(
    name = "manifests",
    srcs = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//cluster/validation:__pkg__"],
)
