# TODO(mpokorny): Once nested pyprojects are gone, move this Ruff config into
# the single pyproject.toml at repo root and delete ruff.toml.

# Prefer one-line formatting when code fits
line-length = 120

[format]
# Ignore trailing commas when deciding whether to collapse to one line
# (Google-style formatting: pack code densely when it fits)
skip-magic-trailing-comma = true

[lint]
select = [
    # Base
    "E", "F",
    # Pylint families
    "PLC",  # convention
    "PLE",  # errors
    "PLR",  # refactor
    "PLW",  # warnings
    # Modernization / quality
    "UP",       # pyupgrade
    "FURB",     # refurb
    # Imports (isort replacement)
    "I",
    # Extras
    "YTT",
    "ASYNC",
    "B",        # bugbear
    "COM",      # commas
    "C4",       # comprehensions
    "LOG",      # logging
    "PIE",
    "PT",       # pytest
    "RSE",
    "RET",      # returns
    "SIM",      # simplify
    "SLOT",
    "PTH",      # pathlib
    "NPY",
    "PD",       # pandas
    "N",        # naming
    "RUF"       # ruff-specific
]
# Keep these off globally; handled case-by-case or via constants
ignore = [
    "PLR2004",  # magic value comparisons
    "PLR0912",  # too many branches
    "PLR0915",  # too many statements
    "PLR0911",  # too many returns
    "PLR0913",  # too many arguments - often leads to annoying unhelpful refactors
    "B904",     # raise from e
    "E501"      # line length (formatter handles wrapping where needed)
]
# Avoid conflicts between ruff-format and trailing-comma rules
extend-ignore = ["COM812"]

[lint.isort]
# Teach Ruff which packages are first-party so sectioning stays stable
known-first-party = ["adgn", "tana", "wt", "ducktape", "claude_hooks", "homeassistant", "llm"]
force-sort-within-sections = true
combine-as-imports = true
# Compatible with format.skip-magic-trailing-comma
split-on-trailing-comma = false

[lint.per-file-ignores]
"experimental/webhook_inbox/webhook_inbox.py" = ["RUF003"]  # Ambiguous unicode characters in comments
"trilium/search_hack.py" = ["N806", "PD901", "PTH110", "PTH123"]  # Various style issues
"trilium/papers/*.py" = ["N806", "PTH123"]  # Naming and pathlib issues
"finance/*.py" = ["N802"]  # Function naming conventions
