"""Integration test infrastructure.

This package provides hermetic services for integration tests using rules_itest.

Currently available:
- rules_itest and rules_postgresql are added to MODULE.bazel

TODO: Add postgres service for pure DB tests (no Docker needed)
- itest_service for postgres using rules_postgresql binaries
- service_test wrappers for props/db tests
- Migration task for schema initialization

For now, props tests use testcontainers which requires Docker.
Pure DB tests can be migrated to rules_itest for fully hermetic execution.
"""
