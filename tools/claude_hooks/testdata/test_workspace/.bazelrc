# Noop quiet/ai_agent configs — the per-session bazelrc activates --config=ai_agent,
# so the workspace must define it (and quiet, which ai_agent references).
# In the real repo these live in the root .bazelrc.
common:quiet --curses=no
common:quiet --show_progress_rate_limit=30
common:ai_agent --config=quiet
