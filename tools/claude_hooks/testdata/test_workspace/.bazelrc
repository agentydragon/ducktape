# Noop ai_agent config — the per-session bazelrc activates --config=ai_agent,
# so the workspace must define it. In the real repo this lives in the root .bazelrc.
common:ai_agent --curses=no
common:ai_agent --show_progress_rate_limit=30
