load("@rules_python//python:defs.bzl", "py_binary")

# Yamllint tool for YAML linting in Bazel

# Yamllint binary via py_binary entry point
py_binary(
    name = "yamllint",
    main_module = "yamllint",
    visibility = ["//visibility:public"],
    deps = ["@pypi//yamllint"],
)
