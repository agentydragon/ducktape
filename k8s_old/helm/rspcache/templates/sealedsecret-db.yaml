{{- if and .Values.sealedSecrets.db.enabled .Values.sealedSecrets.db.encryptedData }}
{{- $name := .Values.sealedSecrets.db.name | default "rspcache-db" -}}
{{ include "common.sealedSecret" (dict
  "context" .
  "namespace" .Release.Namespace
  "name" $name
  "component" "database"
  "encryptedData" .Values.sealedSecrets.db.encryptedData
) }}
{{- end }}
