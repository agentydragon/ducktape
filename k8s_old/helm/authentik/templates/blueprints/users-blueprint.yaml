apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints-users
  namespace: authentik
  labels:
{{- include "common.blueprintLabels" . | nindent 4 }}
data:
  users.yaml: |
{{ tpl (.Files.Get "blueprints/users.yaml") . | indent 4 }}
