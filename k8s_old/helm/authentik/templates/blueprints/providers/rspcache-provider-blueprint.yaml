apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprints-rspcache
  namespace: {{ .Release.Namespace }}
  labels:
{{- include "common.blueprintLabels" . | nindent 4 }}
data:
  rspcache.yaml: |
{{ tpl (.Files.Get "blueprints/rspcache.yaml") . | indent 4 }}
