apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "atuin.serverName" . }}-config
  labels:
{{ include "atuin.serverLabels" . | indent 4 }}
data:
  server.toml: |
{{ include "atuin.serverConfig" . | indent 4 }}
