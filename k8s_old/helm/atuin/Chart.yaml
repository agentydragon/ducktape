apiVersion: v2
name: atuin
description: Atuin shell history sync server
type: application
version: 0.1.0
appVersion: "latest"

dependencies:
  - name: common-lib
    version: ^0.1.1
    repository: file://../common-lib
