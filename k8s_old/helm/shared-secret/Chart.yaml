apiVersion: v2
name: shared-secret
description: Generate or reuse a shared Kubernetes Secret with consistent key values.
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: common-lib
    version: 0.1.0
    repository: file://../common-lib
