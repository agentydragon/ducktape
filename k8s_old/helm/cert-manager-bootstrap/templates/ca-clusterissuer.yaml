apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.caClusterIssuer.name }}
  labels:
{{ toYaml .Values.labels | indent 4 }}
spec:
  ca:
    secretName: {{ .Values.caClusterIssuer.secretName }}
