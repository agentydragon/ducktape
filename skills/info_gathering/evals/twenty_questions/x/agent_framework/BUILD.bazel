# CLEANUP(added 2026-09-07): agent_framework_core has no release compatible with mcp-sdk >=2.0's
#   InitializeResult shape (latest checked: 1.17.0 still reads the removed camelCase
#   `protocolVersion` attribute in its MCP span tracing, agent_framework/_mcp.py -- raises
#   AttributeError on every MCP-backed connection), which blocked the #5786 mcp-sdk/fastmcp
#   migration. Restore these targets (and drop the `# gazelle:exclude agent_framework` in
#   ../BUILD.bazel) once agent_framework_core ships a compatible release, then re-run gazelle.
#   Source files in this directory are otherwise untouched.
