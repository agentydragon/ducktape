# CLEANUP(added 2026-09-07): pydantic-ai-slim's fastmcp toolset has no release compatible with
#   mcp-sdk >=2.0 -- `pydantic_ai.toolsets.fastmcp` unconditionally imports the removed
#   `mcp.server.fastmcp` (renamed to `mcp.server.mcpserver.MCPServer` in mcp-sdk v2; see
#   https://py.sdk.modelcontextprotocol.io/v2/migration/#fastmcp-renamed-to-mcpserver), which
#   blocked the #5786 mcp-sdk/fastmcp migration. Restore these targets (and drop the
#   `# gazelle:exclude pydantic_ai` in ../BUILD.bazel) once pydantic-ai-slim ships a compatible
#   release, then re-run gazelle. Source files in this directory are otherwise untouched.
