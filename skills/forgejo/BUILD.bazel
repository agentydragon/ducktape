load("//skills:defs.bzl", "skill_mapping", "skill_package")

skill_package(
    name = "forgejo",
    contents = [
        skill_mapping(
            srcs = ["SKILL.md"],
            preserve_paths = True,
        ),
        skill_mapping(
            srcs = ["//skills/forgejo/scripts:forgejo.py"],
            prefix = "scripts",
        ),
    ],
)
