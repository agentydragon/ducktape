---
- name: Configure wyrm machine
  hosts: wyrm
  vars_files:
    - vars/common_vars.yml
  vars:
    # Use system Python for modules requiring python3-debian (deb822_repository)
    ansible_python_interpreter: /usr/bin/python3
  remote_user: "{{ my_user }}"

  roles:
    - role: gui
