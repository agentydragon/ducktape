# SPICE proxy for Proxmox VMs
# Forwards atlas.agentydragon.com:3128 → atlas:3128 via Tailscale

upstream atlas_spice {
    server atlas.vps:3128;
}

server {
    listen 3128;
    listen [::]:3128;
    proxy_pass atlas_spice;
    proxy_connect_timeout 15s;
    proxy_timeout 300s;
}
