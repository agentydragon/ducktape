---
- name: Restart nebula
  ansible.builtin.systemd:
    name: nebula@config
    state: restarted
    daemon_reload: true

- name: Restart dnsmasq
  ansible.builtin.systemd:
    name: dnsmasq
    state: restarted
