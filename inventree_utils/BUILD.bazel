# gazelle:exclude rai_plugin
# gazelle:exclude labels
# rai_plugin uses InvenTree's plugin framework (not in @pypi)

load("//devinfra/python:defs.bzl", "py_binary", "py_library")

# Samplebooks import (standalone script)

py_binary(
    name = "import_samplebooks",
    main_module = "import_samplebooks2",
    deps = ["//inventree_utils/samplebooks_import:import_samplebooks2"],
)

py_library(
    name = "tasks",
    srcs = ["tasks.py"],
    visibility = ["//:__subpackages__"],
    deps = [
        "//inventree_utils/beautifier:config",
        "@pypi//inventree",
        "@pypi//invoke",
    ],
)
