diff --git a/lint/ruff.bzl b/lint/ruff.bzl
index 0b48de5..7aa6e2a 100644
--- a/lint/ruff.bzl
+++ b/lint/ruff.bzl
@@ -108,8 +108,9 @@ def _ruff_action(ctx, executable, srcs, configs, stdout, patch, exit_code):
             command = "{ruff} $@ >{stdout}; echo $? >" + exit_code.path
             outputs.append(exit_code)
         else:
-            # Create empty file on success, as Bazel expects one
-            command = "{ruff} $@ && touch {stdout}"
+            # Create empty file on success without relying on external touch.
+            # Ruff actions set their own env and may not have PATH.
+            command = "{ruff} $@ && : > {stdout}"

         ctx.actions.run_shell(
             inputs = inputs,
diff --git a/lint/eslint.bzl b/lint/eslint.bzl
index 591640e..a4fd8d7 100644
--- a/lint/eslint.bzl
+++ b/lint/eslint.bzl
@@ -162,7 +162,7 @@ def eslint_action(ctx, executable, srcs, stdout, exit_code = None, format = "stylish", env = {}, patch = None, target = None, args = []):
                 outputs = [stdout],
                 tools = [executable._eslint],
                 arguments = [action_args],
-                command = executable._eslint.path + " $@ && touch " + stdout.path,
+                command = executable._eslint.path + " $@ && : > " + stdout.path,
                 env = dict(env, **{
                     "BAZEL_BINDIR": ctx.bin_dir.path,
                 }),
diff --git a/lint/private/lint_aspect.bzl b/lint/private/lint_aspect.bzl
index 95ba409..1df0e58 100644
--- a/lint/private/lint_aspect.bzl
+++ b/lint/private/lint_aspect.bzl
@@ -192,8 +192,8 @@ def noop_lint_action(ctx, outputs):
         outputs: struct returned from output_files or patch_and_output_files
     """
     commands = []
-    commands.append("touch {}".format(outputs.human.out.path))
-    commands.append("touch {}".format(outputs.machine.out.path))
+    commands.append(": > {}".format(outputs.human.out.path))
+    commands.append(": > {}".format(outputs.machine.out.path))

     outs = [outputs.human.out, outputs.machine.out]

@@ -207,7 +207,7 @@ def noop_lint_action(ctx, outputs):
         outs.append(outputs.machine.exit_code)

     if hasattr(outputs, "patch"):
-        commands.append("touch {}".format(outputs.patch.path))
+        commands.append(": > {}".format(outputs.patch.path))
         outs.append(outputs.patch)

     ctx.actions.run_shell(
