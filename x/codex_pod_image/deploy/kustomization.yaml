apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: codex-pod
generatorOptions:
  disableNameSuffixHash: true
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
# The two SOPS Secret manifests remain parked under cluster/k8s/parked/codex-pod/.
# Replace them with runtime-managed credentials before reactivating this experiment.
