apiVersion: v1
kind: Namespace
metadata:
  name: codex-pod
  annotations:
    description: "Parked Codex pod image experiment (see x/codex_pod_image/deploy/README.md)."
  labels:
    app.kubernetes.io/name: codex-pod
    goldilocks.fairwinds.com/enabled: "true"
