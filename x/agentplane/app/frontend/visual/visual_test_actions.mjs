// Render-health and PR visual for the operator ActionRequest review surface.
import { main } from "../../../../../util/testing/frontend_visual/visual-test-lib.mjs";

await main("actions", { element: "#app", viewport: { width: 1200, height: 1100 } });
