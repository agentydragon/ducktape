# Native harness protocol roster

What each harness binary exposes on its stdio protocol, against what the scripted tests in
`x/agentplane/harness_tests/{claude,codex}/` exercise. Pinned builds: Claude Code 2.1.252
(`--input-format stream-json --output-format stream-json --permission-prompt-tool stdio`) and
Codex app-server 0.152.0 (`codex app-server --listen stdio://`, JSON-RPC v2 protocol). Codex rows
come from `codex-rs/app-server-protocol/src/protocol/v2/` and the generated schema bundle; Claude
Code ships no source, so rows marked "(inferred)" come from `strings`/JS fragments in the binary
rather than `--help`. `Covered` names the test; "partial" means the harness uses the surface but no
assertion pins it, or only one branch is exercised.

## Claude Code 2.1.252

Driver: `x/agentplane/native/claude/{driver,scenarios}.py`. Every test runs `launch_handshake`
(`control_request` `initialize` → `control_response`), sends `user` frames, and auto-allows
`can_use_tool` in `harness_tests/claude/harness.py`.

### Session lifecycle

| Feature                    | Wire surface                                                                                                                                                                  | Covered                                                  | Note                                                                                                                            |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Handshake                  | `control_request` `{subtype: "initialize"}` → `control_response` with `commands`, `agents`, `models`, `output_style`, `pid`, `pending_permission_requests` (inferred)         | partial, every test                                      | Harness sends an empty request; `hooks`, `sdkMcpServers`, `jsonSchema`, `systemPrompt`, `agents`, `title` payload fields unused |
| Session start frame        | `system` `init` (`tools`, `mcp_servers`, `model`, `permissionMode`, `slash_commands`, `skills`, `plugins`, `claude_code_version`)                                             | no                                                       | Bridge would read the advertised tool and model roster here                                                                     |
| Resume                     | `--resume <id>` (id from `result.session_id`)                                                                                                                                 | yes, `test_idle_resume_replays_the_transcript_from_disk` | Transcript replayed to upstream verbatim                                                                                        |
| Fork / continue / fixed id | `--fork-session`, `--continue`, `--session-id <uuid>`                                                                                                                         | no                                                       | Fork needs a new id surfaced to the bridge                                                                                      |
| Truncating resume          | `--resume-session-at <message id>`, `--resume-drops-turn <uuid>`, `--rewind-files <uuid>` (inferred, hidden flags); `rewind_conversation`, `rewind_files` subtypes (inferred) | no                                                       | Rewind is the natural "undo a turn" surface                                                                                     |
| Persistence off            | `--no-session-persistence`                                                                                                                                                    | no                                                       | Would break resume; harness persists to `CLAUDE_CONFIG_DIR`                                                                     |
| Rename / title             | `--name`, `rename_session`, `generate_session_title` subtypes (inferred)                                                                                                      | partial                                                  | `--name` set to suppress the title model call; not asserted                                                                     |
| End of session             | `end_session` subtype (inferred); stdin EOF                                                                                                                                   | partial                                                  | Tests close stdin via `NativeProcess`; no explicit end frame                                                                    |
| Liveness                   | `keep_alive` output frame (inferred)                                                                                                                                          | no                                                       | Bridge idle detection                                                                                                           |

### Turn input

| Feature                          | Wire surface                                                                                                                                                                                                                                                     | Covered                                                               | Note                                                                 |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Text turn                        | `user` frame `{message: {role, content}, parent_tool_use_id: null, uuid}`                                                                                                                                                                                        | yes, `test_baseline_turn`                                             | String content only; content-block arrays (images, documents) unused |
| Input echo                       | `--replay-user-messages` → `user` frame with `isReplay: true` (inferred)                                                                                                                                                                                         | no                                                                    | Gives the bridge an ack per queued message                           |
| Queue lifecycle                  | `command_lifecycle` frame keyed by the inbound `uuid` (inferred)                                                                                                                                                                                                 | no                                                                    | Only emitted for uuid-bearing messages; the harness supplies uuids   |
| Mid-turn input                   | second `user` frame while a tool runs                                                                                                                                                                                                                            | yes, `test_second_input_during_a_tool_arrives_inside_the_tool_result` | Delivered appended to the running tool result, not as a user message |
| Interrupt                        | `control_request` `interrupt` `{reason, cancel_queued}`                                                                                                                                                                                                          | partial, `test_interrupt_aborts_the_in_flight_model_call`             | `cancel_queued=False` only; `terminal_reason: aborted_streaming`     |
| Cancel a pending control request | `control_cancel_request {request_id}` (inferred)                                                                                                                                                                                                                 | no                                                                    | Withdraws e.g. a `can_use_tool` after interrupt                      |
| Structured output                | `--json-schema`, `result.structured_output`, `error_max_structured_output_retries` (inferred)                                                                                                                                                                    | no                                                                    |                                                                      |
| Runtime knobs                    | `set_model`, `set_permission_mode`, `set_max_thinking_tokens`, `apply_flag_settings`, `set_cwd`, `add_directory` subtypes (inferred)                                                                                                                             | no                                                                    | Mid-session model/mode switches without a restart                    |
| Side channels                    | `side_question`, `submit_feedback`, `message_rated`, `stop_task`, `background_tasks`, `get_context_usage`, `get_session_cost`, `get_usage`, `list_models`, `read_file`, `file_suggestions`, `get_workspace_diff`, `get_plan`, `get_settings` subtypes (inferred) | no                                                                    | Read-only queries a bridge UI could serve                            |

### Tool execution

| Feature          | Wire surface                                                                                                            | Covered                                                             | Note                                                                                                             |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Shell            | `Bash` tool; `assistant` `tool_use` → `user` frame with `tool_use_result`                                               | yes, `test_parallel_shell_tools_report_both_streams_and_exit_codes` | stdout, stderr, exit code, `is_error`; two parallel uses                                                         |
| File read/edit   | `Read`, `Edit` tools                                                                                                    | yes, `test_file_edit_round_trip_changes_the_workspace`              | `Write`, `Glob`, `Grep`, `NotebookEdit`, `WebFetch`, `WebSearch` excluded by `--tools`                           |
| Streaming        | `stream_event` (`--include-partial-messages`): `content_block_start/stop`, deltas                                       | yes, `frames.assert_tool_lifecycles`                                | Text deltas not asserted individually                                                                            |
| Thinking         | `thinking` blocks in `assistant`; signature echoed upstream                                                             | yes, `test_parallel_shell_tools_report_both_streams_and_exit_codes` | Thinking stays on; `--thinking`, `--max-thinking-tokens` hidden flags, `MAX_THINKING_TOKENS` (inferred) untested |
| Progress         | `tool_progress` (`elapsed_time_seconds`, `heartbeat`), `tool_use_summary` (inferred)                                    | no                                                                  | Long-running tool liveness                                                                                       |
| Subagents        | `Task`/`Agent` tool, `parent_tool_use_id`, `--forward-subagent-text`, `--agents`, `system` `task_*` subtypes (inferred) | no                                                                  | Deliberately excluded; see below                                                                                 |
| Background tasks | `background_tasks` subtype, `system` `background_tasks_changed` (inferred)                                              | no                                                                  |                                                                                                                  |

### Approvals and permissions

| Feature               | Wire surface                                                                                                                                                                                                      | Covered                  | Note                                                                                                             |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| Permission prompt     | `control_request` `can_use_tool` `{tool_name, input, tool_use_id, permission_suggestions, blocked_path, decision_reason, decision_reason_type}` (inferred) → `control_response` `{behavior: allow, updatedInput}` | partial, every tool test | Auto-allowed in `harness.py`; no test asserts the request or exercises `deny`, `updatedPermissions`, `interrupt` |
| Permission modes      | `--permission-mode` (`acceptEdits`, `auto`, `bypassPermissions`, `manual`, `dontAsk`, `plan`), `--dangerously-skip-permissions`, `set_permission_mode`                                                            | no                       | Default mode plus stdio prompt tool                                                                              |
| Tool allow/deny lists | `--allowedTools`, `--disallowedTools`, `--tools`                                                                                                                                                                  | partial                  | `--tools Bash,Edit,Read` asserted via upstream `tool_names`                                                      |
| Denial notice         | `system` `permission_denied`, `result.permission_denials` (inferred)                                                                                                                                              | no                       |                                                                                                                  |
| User dialogs          | `request_user_dialog` subtype, `system` `elicitation_complete` (inferred)                                                                                                                                         | no                       | `AskUserQuestion`-style prompts routed to the host                                                               |
| Hooks                 | `initialize.hooks`, `hook_callback` subtype, `--include-hook-events` (`hook_started`, `hook_progress`, `hook_response`)                                                                                           | no                       | Off under `--safe-mode`; see below                                                                               |

### Model and upstream

| Feature                 | Wire surface                                                                                                                                  | Covered                                                                            | Note                                                                                                                               |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Endpoint and auth       | `ANTHROPIC_BASE_URL`, `ANTHROPIC_AUTH_TOKEN`                                                                                                  | yes, every test                                                                    | `/v1/messages?beta=true`, streaming on                                                                                             |
| Model                   | `--model` (routed id, family suffix resolves the context window)                                                                              | yes, `test_baseline_turn`                                                          | `--effort`, `--betas`, `--fallback-model` untested                                                                                 |
| Retry                   | `CLAUDE_CODE_MAX_RETRIES`; `system` `api_retry` frame with `attempt`                                                                          | yes, `test_stream_lost_before_content_is_retried_without_streaming`                | The retry is a `stream: false` request with no `api_retry` notice (`CLAUDE_CODE_DISABLE_NONSTREAMING_FALLBACK` inferred, untested) |
| Loss after visible text | retried as a `stream: false` request                                                                                                          | yes, `test_stream_lost_after_visible_text_is_retried_without_streaming`            | The streamed partial text is dropped from the transcript                                                                           |
| Retry exhaustion        | `result` `is_error: true`, `terminal_reason: api_error`; `system` `api_retry` per attempt                                                     | yes, `test_retry_exhaustion_fails_the_turn_and_the_process_accepts_the_next_input` | Process survives and the next turn succeeds                                                                                        |
| Model fallback          | `--fallback-model`, `system` `model_fallback`, `model_refusal_fallback`, `model_consent_fallback`, `CLAUDE_CODE_NO_MODEL_FALLBACK` (inferred) | no                                                                                 |                                                                                                                                    |
| Limits                  | `--max-budget-usd`, `--max-turns` (hidden), `--task-budget` (hidden); `result` subtypes `error_max_budget_usd`, `error_max_turns` (inferred)  | no                                                                                 | Bridge-side budget enforcement                                                                                                     |
| Context                 | `--autocompact`, `DISABLE_AUTO_COMPACT`, `system` `compact_boundary` (inferred), `CLAUDE_CODE_MAX_OUTPUT_TOKENS`, `API_TIMEOUT_MS` (inferred) | no                                                                                 | Compaction changes what resume replays                                                                                             |

### Observability

| Feature            | Wire surface                                                                                                                                | Covered                                         | Note                                                                          |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------- |
| Terminal frame     | `result` (`is_error`, `stop_reason`, `terminal_reason`, `result`, `session_id`)                                                             | yes, `frames.assert_success` / `assert_failure` | `usage`, `modelUsage`, `total_cost_usd`, `num_turns`, `duration_*` unasserted |
| Status frames      | `system` `status`, `turn_starting`, `turn_duration`, `thinking_tokens`, `notification`, `informational`, `session_state_changed` (inferred) | no                                              |                                                                               |
| Rate limits        | `rate_limit_event` (inferred)                                                                                                               | no                                              |                                                                               |
| Prompt suggestions | `--prompt-suggestions`, `prompt_suggestion` frame                                                                                           | no                                              | Configured off; see below                                                     |
| Telemetry off      | `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`, `DISABLE_TELEMETRY` (inferred)                                                                  | yes, `test_baseline_turn`                       | One turn is exactly one upstream request                                      |

### Configuration

| Feature            | Wire surface                                                                                                                        | Covered                   | Note                                                                                  |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------- |
| System prompt      | `--system-prompt`, `--append-system-prompt`, `--exclude-dynamic-system-prompt-sections`                                             | yes, `test_baseline_turn` | Asserts `system_text` ends with the prompt and stays under 1000 chars                 |
| Settings isolation | `--setting-sources=`, `--settings`, `CLAUDE_CONFIG_DIR`                                                                             | partial, every test       | Empty source list; `--settings` JSON unused                                           |
| Minimal modes      | `--safe-mode`, `--bare`, `--restricted`, `--disable-slash-commands`                                                                 | partial                   | `--safe-mode` + `--disable-slash-commands` used; `--bare` and `--restricted` untested |
| MCP                | `--mcp-config`, `--strict-mcp-config`, `sdkMcpServers` + `mcp_message`, `mcp_status`, `mcp_set_servers`, `mcp_reconnect` (inferred) | partial                   | `--strict-mcp-config` with no config; no server ever loaded                           |
| Working set        | `--add-dir`, `set_cwd` (inferred)                                                                                                   | no                        |                                                                                       |
| Plugins / skills   | `--plugin-dir`, `--plugin-url`, `reload_plugins`, `reload_skills` (inferred)                                                        | no                        | Off; see below                                                                        |

## Codex app-server 0.152.0

Driver: `x/agentplane/native/codex/{driver,scenarios}.py`. Config is passed as `-c key=value`
overrides; the model id `agentplane-test-model` is deliberately absent from Codex's catalog
(`models-manager/models.json`), because catalog entries carry `tool_mode: code_mode_only`, which
swaps the upstream wire shape for one JS `exec` tool the model scripts against. An unknown or routed
id keeps the classic `exec_command` function-call shape the tests assert.

### Session lifecycle

| Feature                        | Wire surface                                                                                                                      | Covered                                              | Note                                                                                        |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Handshake                      | `initialize` (`clientInfo`, `capabilities`) → `initialized`                                                                       | yes, every test                                      | `capabilities: null`; `experimental_raw_events` and opt-in notification capabilities unused |
| Thread start                   | `thread/start` (`cwd`, `approvalPolicy`, `sandbox`, `model`, `baseInstructions`, `config`, `ephemeral`) → `thread/started`        | yes, every test                                      | `developerInstructions`, `dynamicTools`, `personality`, `historyMode`, `serviceTier` unused |
| Resume                         | `thread/resume {threadId}` after a new process                                                                                    | yes, `test_idle_resume_replays_the_thread_from_disk` | `history`, `path`, `excludeTurns`, `initialTurnsPage` params unused                         |
| Fork                           | `thread/fork` (`lastTurnId`, `beforeTurnId`)                                                                                      | no                                                   | Branch a thread at a turn                                                                   |
| Rollback / revert              | `thread/rollback`, `thread/revert` → `thread/reverted`                                                                            | no                                                   | Drop trailing turns in place                                                                |
| Listing and reading            | `thread/list`, `thread/read`, `thread/turns/list`, `thread/items/list`, `thread/loaded/list`                                      | no                                                   | History pagination for a bridge UI                                                          |
| Archive / delete / unsubscribe | `thread/archive`, `thread/unarchive`, `thread/delete`, `thread/unsubscribe`; `thread/archived`, `thread/deleted`, `thread/closed` | no                                                   | Unsubscribe stops notifications without unloading                                           |
| Naming and goals               | `thread/name/set`, `thread/goal/{set,get,clear}`, `thread/metadata/update`; `thread/name/updated`, `thread/goal/*`                | no                                                   |                                                                                             |
| Status                         | `thread/status/changed` (`notLoaded`, `idle`, `systemError`, `active {activeFlags}`)                                              | no                                                   | `waitingOnApproval` flag is the bridge's "blocked" signal                                   |
| Compaction                     | `thread/compact/start` → `thread/compacted`; `contextCompaction` item                                                             | no                                                   | Changes what resume replays                                                                 |
| History injection              | `thread/inject_items`                                                                                                             | no                                                   | Client-authored transcript items                                                            |

### Turn input

| Feature             | Wire surface                                                                                                                                                 | Covered                                                         | Note                                                            |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------- | --------------------------------------------------------------- |
| Text turn           | `turn/start {threadId, input: [{type: text, text, text_elements}]}` → `turn/started` … `turn/completed`                                                      | yes, `test_baseline_turn`                                       | `image`, `localImage`, `audio` inputs unused                    |
| Per-turn overrides  | `turn/start` `cwd`, `approvalPolicy`, `sandboxPolicy`, `model`, `effort`, `summary`, `personality`, `outputSchema`, `collaborationMode`, `additionalContext` | no                                                              | `outputSchema` is the structured-output surface                 |
| Input during a turn | second `turn/start` while active → same `turn.id`                                                                                                            | yes, `test_second_input_during_a_tool_joins_the_running_turn`   | Joins the running turn as a user message after the tool output  |
| Steer               | `turn/steer {threadId, turnId, input}`                                                                                                                       | yes, `test_steer_during_a_tool_joins_the_running_turn`          | `expectedTurnId` mismatch path untested                         |
| Queue               | `thread/queue/changed`                                                                                                                                       | no                                                              | Not observed; the second `turn/start` joined rather than queued |
| Interrupt           | `turn/interrupt {threadId, turnId}` → `turn/completed` `status: interrupted`                                                                                 | yes, `test_interrupt_aborts_the_in_flight_model_call`           |                                                                 |
| Turn statuses       | `completed`, `interrupted`, `failed`, `inProgress`                                                                                                           | yes, across `test_active_turn.py` and `test_connection_loss.py` |                                                                 |
| Review              | `review/start`; `enteredReviewMode`, `exitedReviewMode` items                                                                                                | no                                                              |                                                                 |
| Tool output turns   | `turn/start.toolOutput` (`item/tool/call` reply path)                                                                                                        | no                                                              | Needed only with `dynamicTools`                                 |

### Tool execution

| Feature                   | Wire surface                                                                                                            | Covered                                                          | Note                                                                     |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Shell                     | `exec_command` function call → `commandExecution` item (`command`, `status`, `exitCode`, `aggregatedOutput`)            | yes, `test_parallel_shell_commands_report_output_and_exit_codes` | Output goes upstream as `function_call_output` with "exited with code N" |
| Interactive stdin         | `write_stdin` tool (exposed to the model), `item/commandExecution/terminalInteraction`                                  | no                                                               | Advertised in every request; never invoked                               |
| Output streaming          | `item/commandExecution/outputDelta`, `command/exec/outputDelta`                                                         | no                                                               | Live output for long commands                                            |
| File edits                | `apply_patch` → `fileChange` item, `item/fileChange/{outputDelta,patchUpdated}`, `turn/diff/updated`                    | no                                                               | The round trip uses shell redirection instead                            |
| File round trip via shell | `exec_command` `cat` / `printf >`                                                                                       | yes, `test_file_edit_round_trip_changes_the_workspace`           | Asserts workspace content and upstream outputs                           |
| Client-run shell          | `command/exec`, `command/exec/{write,terminate,resize}`, `process/*`                                                    | no                                                               | App-driven, not model-driven                                             |
| Client-hosted tools       | `thread/start.dynamicTools` → `item/tool/call` server request, `dynamicToolCall` item                                   | no                                                               | The Codex analogue of SDK MCP servers                                    |
| MCP                       | `mcpServerStatus/list`, `mcpServer/tool/call`, `mcpServer/resource/read`, `config/mcpServer/reload`, `mcpToolCall` item | no                                                               |                                                                          |
| Filesystem API            | `fs/{readFile,writeFile,createDirectory,getMetadata,readDirectory,remove,copy,watch,unwatch}`, `fs/changed`             | no                                                               | Client-side file access through the server                               |
| Plan                      | `turn/plan/updated`, `item/plan/delta`, `plan` item                                                                     | no                                                               |                                                                          |
| Web / image / collab      | `webSearch`, `imageView`, `imageGeneration`, `collabAgentToolCall`, `subAgentActivity` items                            | no                                                               | Off; see below                                                           |

### Approvals and permissions

| Feature               | Wire surface                                                                                                                            | Covered             | Note                                                                 |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------- |
| Approval policy       | `approval_policy` (`untrusted`, `on-request`, `never`, granular), `sandbox_mode` (`read-only`, `workspace-write`, `danger-full-access`) | partial, every test | `never` + `danger-full-access` only                                  |
| Command approval      | `item/commandExecution/requestApproval` → `accept`, `acceptForSession`, `acceptWithExecpolicyAmendment`, `decline`, `cancel`            | no                  | Never requested under `never`                                        |
| File-change approval  | `item/fileChange/requestApproval`                                                                                                       | no                  |                                                                      |
| Permission escalation | `item/permissions/requestApproval` (`request_permissions` tool)                                                                         | no                  |                                                                      |
| User input            | `request_user_input` tool (exposed to the model) → `item/tool/requestUserInput`                                                         | no                  | Advertised in every request; a bridge must answer or the turn blocks |
| MCP elicitation       | `mcpServer/elicitation/request`                                                                                                         | no                  |                                                                      |
| Guardian              | `item/autoApprovalReview/*`, `autoApprovalReview/strictReviewRequired`, `thread/approveGuardianDeniedAction`, `guardianWarning`         | no                  |                                                                      |
| Legacy approvals      | `applyPatchApproval`, `execCommandApproval`                                                                                             | no                  | v1-era server requests                                               |

### Model and upstream

| Feature                  | Wire surface                                                                                             | Covered                                                                           | Note                                                                      |
| ------------------------ | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| Provider                 | `model_providers.<name>` (`base_url`, `env_key`, `wire_api: responses`), `OPENAI_BASE_URL`               | yes, every test                                                                   | `/v1/responses`, streaming on                                             |
| Retry                    | `request_max_retries`, `stream_max_retries`; `error` notification `{error, willRetry, threadId, turnId}` | yes, `test_stream_lost_before_content_is_retried`                                 | `features.unbounded_connection_retries` off                               |
| Loss after visible text  | retried with the partial text dropped                                                                    | yes, `test_stream_lost_after_visible_text_is_retried_and_the_thread_continues`    | Same shape as Claude, but streaming stays on                              |
| Retry exhaustion         | `turn/completed` `status: failed`, final `error` with `willRetry: false`                                 | yes, `test_retry_exhaustion_fails_the_turn_and_the_thread_accepts_the_next_input` | Failed turn's input persists in the thread                                |
| Reasoning                | `model_reasoning_effort`, `reasoning` items with `encrypted_content` echoed upstream                     | yes, `test_baseline_turn`, `test_idle_resume_replays_the_thread_from_disk`        | `item/reasoning/{summaryTextDelta,summaryPartAdded,textDelta}` unasserted |
| Request metadata         | `prompt_cache_key = threadId`, `responsesapi_client_metadata` (`thread_id`, `turn_id`)                   | yes, `test_baseline_turn`                                                         |                                                                           |
| Model catalog            | `model/list`, `modelProvider/capabilities/read`, `model/rerouted`, `model/verification`                  | no                                                                                | Catalog ids trigger code mode                                             |
| Accounts                 | `account/*`, `account/chatgptAuthTokens/refresh`, `modelProvider/authRecovery*`                          | no                                                                                | API-key auth only                                                         |
| Budgets                  | `features.token_budget`, `rollout_budget`, `thread/tokenUsage/updated`                                   | no                                                                                |                                                                           |
| Fast mode / service tier | `service_tier`, `features.fast_mode`                                                                     | no                                                                                |                                                                           |

### Observability

| Feature                    | Wire surface                                                      | Covered                              | Note                                                    |
| -------------------------- | ----------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------- |
| Item lifecycle             | `item/started`, `item/completed`                                  | yes, `frames.assert_item_lifecycles` | `userMessage`, `commandExecution`, `agentMessage` types |
| Text deltas                | `item/agentMessage/delta`                                         | no                                   | Final text asserted from `item/completed` only          |
| Server request bookkeeping | `serverRequest/resolved`                                          | no                                   |                                                         |
| Warnings                   | `warning`, `deprecationNotice`, `configWarning`                   | no                                   |                                                         |
| Hooks                      | `hooks/list`, `hook/started`, `hook/completed`, `hookPrompt` item | no                                   |                                                         |
| Realtime / remote control  | `thread/realtime/*`, `remoteControl/status/changed`               | no                                   | Out of scope for a stdio bridge                         |

### Configuration

| Feature                      | Wire surface                                                                                                                                                             | Covered                   | Note                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Overrides                    | `-c key=value` (TOML literals), `CODEX_HOME`                                                                                                                             | yes, every test           | Per-key `config/read`, `config/value/write`, `config/batchWrite`, `configRequirements/read` untested       |
| Prompt trimming              | `include_permissions_instructions`, `include_apps_instructions`, `include_collaboration_mode_instructions`, `include_environment_context`, `skills.include_instructions` | yes, `test_baseline_turn` | Asserts the `<skills_instructions>`, `<permissions instructions>`, `<apps_instructions>` blocks are absent |
| Base instructions            | `thread/start.baseInstructions`                                                                                                                                          | yes, `test_baseline_turn` | Asserts `instructions == BASE_INSTRUCTIONS`                                                                |
| Feature flags                | `features.*` (`Feature` enum in `codex-rs/features/src/lib.rs`)                                                                                                          | partial                   | `multi_agent`, `view_image`, `unbounded_connection_retries` off; `web_search = disabled`                   |
| Skills and plugins           | `skills/*`, `plugin/*`, `marketplace/*`, `skills/changed`                                                                                                                | no                        | `skills.bundled.enabled = false`                                                                           |
| Environments / projects      | `thread/environment/*`, `project/changed`, `environments` params                                                                                                         | no                        |                                                                                                            |
| External agent config import | `externalAgentConfig/*`                                                                                                                                                  | no                        |                                                                                                            |
| Fuzzy file search            | `fuzzyFileSearch`, `fuzzyFileSearch/*`                                                                                                                                   | no                        | UI helper                                                                                                  |

## What the runner consumes

The runner in `x/agentplane/runner/` translates these surfaces into its protocol; everything else
stays evidence in `Native` events. Observed with the pinned builds while writing it:

- Claude: `--session-id` fixes a fresh session's id, so the runner never waits for one;
  `--replay-user-messages` also emits `command_lifecycle` frames (`queued`, `started`,
  `completed`) per user frame uuid, and `queued` arrives on receipt even for an input the harness
  later delivers inside a running tool's result; `--effort` is accepted; `stream_event`
  `content_block_start`/deltas map to item lifecycle and `assistant` frames complete each block;
  `can_use_tool` is answered with allow, every other control request with an error; the transcript
  is written on clean exit, so a harness killed outright loses the conversation since its last
  write.
- Codex: `turn/start` during a turn returns the running turn's id; `turn/started`,
  `item/started`, `item/*/delta`, `item/completed`, and `turn/completed` drive the lifecycle;
  server requests (approvals, user input, elicitation) are answered with a JSON-RPC error; a
  `commandExecution` item that outlives its first read completes with `aggregatedOutput: null`;
  `turn/interrupt` completes the turn as `interrupted` but leaves an in-flight streamed model
  connection open.

## Deliberately unsupported

Configured off in `scenarios.command`/`scenarios.config` so recorded model requests contain only
the scenario under test; a bridge that turns any of these on must widen the roster above.

- **Skills and slash commands**: Claude `--disable-slash-commands`; Codex `skills.bundled.enabled = false`,
  `skills.include_instructions = false`. They add a catalog block to every prompt.
- **Plugins, hooks, settings files**: Claude `--safe-mode`, `--setting-sources=`; Codex hooks unconfigured.
  Hooks would require answering `hook_callback` / `hook/*` and change tool timing; what each
  harness offers is in [hooks](../../docs/hooks.md).
- **MCP servers**: Claude `--strict-mcp-config` with no `--mcp-config`; Codex none configured. Tool
  rosters must stay the fixed lists the tests assert (`Bash,Edit,Read`; `exec_command`, `write_stdin`,
  `request_user_input`).
- **Multi-agent / subagents**: Claude `--tools` omits `Task`; Codex `features.multi_agent = false`.
  `parent_tool_use_id` and `subAgentActivity` stay unexercised.
- **Web search and image viewing**: Codex `web_search = "disabled"`, `features.view_image = false`;
  Claude `--tools` omits `WebSearch`, `WebFetch`.
- **Code mode**: avoided by using a model id outside Codex's catalog (`MODEL = "agentplane-test-model"`);
  a catalog id would replace the function-call tools with one JS `exec` tool.
- **Session title generation**: Claude `--name` preset suppresses the separate title model call;
  Codex threads start `ephemeral` unless a test resumes.
- **Prompt suggestions**: Claude `--prompt-suggestions=false` suppresses the post-turn model call.
- **Nonessential traffic**: Claude `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1`; Codex
  `include_environment_context = false`. The baseline tests count upstream requests per turn.
- **Thinking stays on** for both providers: Claude `thinking.type == "enabled"` and signature echo,
  Codex `model_reasoning_effort` and encrypted reasoning echo are asserted, since replaying them
  correctly across resume is part of the contract.
