# claude_commands_old (archived snapshot)

Historical snapshot of an old set of Claude Code slash-command prompts. These
are **superseded** by the live `.claude/commands` directory and the `skills/`
tree, which are the maintained homes for commands and agent skills.

Kept only as a reference of earlier prompt wording. Do not wire these into the
current setup or treat them as active — consult `.claude/commands` and
`skills/` for anything in use.
