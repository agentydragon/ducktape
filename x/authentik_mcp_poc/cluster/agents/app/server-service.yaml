apiVersion: v1
kind: Service
metadata:
  name: authentik-mcp-poc-server
  namespace: authentik-mcp-poc
  labels:
    app.kubernetes.io/name: authentik-mcp-poc
    app.kubernetes.io/component: server
spec:
  selector:
    app.kubernetes.io/name: authentik-mcp-poc
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 8765
      targetPort: 8765
      protocol: TCP
  type: ClusterIP
