diff -urN old/litert_lm_cli/commands/openai_handler.py new/litert_lm_cli/commands/openai_handler.py
--- old/litert_lm_cli/commands/openai_handler.py
+++ new/litert_lm_cli/commands/openai_handler.py
@@ -881,6 +881,9 @@
           max_num_tokens=spec.max_num_tokens,
           vision_backend=vision_backend,
           audio_backend=audio_backend,
+          enable_speculative_decoding=getattr(
+              self.server, "enable_speculative_decoding", None
+          ),
       )
     except FileNotFoundError as e:
       self.send_error(404, "".join(traceback.format_exception_only(e)))
diff -urN old/litert_lm_cli/commands/serve.py new/litert_lm_cli/commands/serve.py
--- old/litert_lm_cli/commands/serve.py
+++ new/litert_lm_cli/commands/serve.py
@@ -24,6 +24,7 @@
 import click

 import litert_lm
+from litert_lm_cli import common
 from litert_lm_cli import help_formatter
 from litert_lm_cli.commands import gemini_handler
 from litert_lm_cli.commands import openai_handler
@@ -35,6 +36,7 @@
     port: int,
     handler_class: type[http.server.BaseHTTPRequestHandler],
     api_name: str,
+    enable_speculative_decoding: bool | None,
 ) -> None:
   """Starts the HTTP server.

@@ -43,10 +45,12 @@
     port: Port to listen on.
     handler_class: The HTTP handler class to use.
     api_name: The API protocol name (e.g., "OpenAI", "Gemini").
+    enable_speculative_decoding: Whether to force speculative decoding.
   """
   server_address = (host, port)
   try:
     with serve_util.LiteRTLMServer(server_address, handler_class) as server:
+      server.enable_speculative_decoding = enable_speculative_decoding
       click.echo(
           click.style(
               f"Starting {api_name}-compatible API server on {host}:{port}...",
@@ -85,7 +89,24 @@
     help="The API protocol to use.",
 )
 @click.option("--verbose", is_flag=True, help="Enable verbose logging")
-def serve(host: str, port: int, *, api: str, verbose: bool) -> None:
+@click.option(
+    "--enable-speculative-decoding",
+    type=click.Choice(["true", "false"], case_sensitive=False),
+    default="false",
+    callback=common.parse_speculative_decoding,
+    help=(
+        "Force speculative decoding for served engines. Use true for Gemma 4"
+        " MTP-capable GPU serving."
+    ),
+)
+def serve(
+    host: str,
+    port: int,
+    *,
+    api: str,
+    verbose: bool,
+    enable_speculative_decoding: bool,
+) -> None:
   """Starts a local HTTP server speaking the OpenAI or Gemini API protocol.

   Args:
@@ -93,6 +114,7 @@
     port: Port to listen on.
     api: The API protocol to use (openai or gemini).
     verbose: Whether to enable verbose logging.
+    enable_speculative_decoding: Whether to force speculative decoding.
   """
   if verbose:
     litert_lm.set_min_log_severity(litert_lm.LogSeverity.VERBOSE)
@@ -107,7 +129,9 @@
   else:
     raise click.BadParameter(f"Unsupported API: {api}")

-  run_server(host, port, handler_class, api_name)
+  run_server(
+      host, port, handler_class, api_name, enable_speculative_decoding
+  )


 def register(cli: click.Group) -> None:
diff -urN old/litert_lm_cli/commands/serve_util.py new/litert_lm_cli/commands/serve_util.py
--- old/litert_lm_cli/commands/serve_util.py
+++ new/litert_lm_cli/commands/serve_util.py
@@ -168,6 +168,7 @@
     max_num_tokens: int | None = None,
     vision_backend: litert_lm.Backend | None = None,
     audio_backend: litert_lm.Backend | None = None,
+    enable_speculative_decoding: bool | None = None,
 ) -> litert_lm.Engine:
   """Retrieves the persistent server engine or initializes it on first request.

@@ -187,6 +188,7 @@
     max_num_tokens: The maximum number of tokens. If None, uses model default.
     vision_backend: The hardware backend to use for vision encoding.
     audio_backend: The hardware backend to use for audio encoding.
+    enable_speculative_decoding: Whether to force speculative decoding.

   Returns:
     The shared LiteRT-LM Engine context object.
@@ -233,12 +235,18 @@
   click.echo(
       click.style(f"Initializing engine for model: {m.model_path}", fg="cyan")
   )
+  engine_kwargs = {}
+  if enable_speculative_decoding is not None:
+    engine_kwargs["enable_speculative_decoding"] = (
+        enable_speculative_decoding
+    )
   engine = litert_lm.Engine(
       m.model_path,
       backend=backend,
       max_num_tokens=max_num_tokens,
       vision_backend=vision_backend,
       audio_backend=audio_backend,
+      **engine_kwargs,
   )
   engine.__enter__()
   server.litert_lm_engine = engine
