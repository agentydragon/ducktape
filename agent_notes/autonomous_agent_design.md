# Autonomous Agent Design Notes

## Communication

- Should be able to talk to me via messengers like Telegram or Matrix

## Permissions

- Graduated, tweakable permissions (like the approval-gate MCP server we've been building)

## Current State (OpenClaw in devel)

OpenClaw is wired up in devel. Its exec tool only knows three modes:

- Sandbox by Docker
- Execution in gateway, unsandboxed
- Execution in node, unsandboxed

No graduated permission model — it's either fully sandboxed (Docker) or fully unsandboxed.

## Infrastructure

- Agent should have its own k8s namespace
- A bunch of tokens it can use autonomously
- Some services available only with approvals (e.g., Gmail: read often OK, write only with approval)

## OpenClaw Pros/Cons

### Good

- Good support for messengers

### Problems

- MCP support is **not fully native** despite earlier assessment. `@modelcontextprotocol/sdk` is in the dependency tree and there's internal MCP infrastructure (`mcporter` tool), but the key PR (#5121) adding user-configurable MCP client was closed. Issue #4834 (native MCP) closed as "not planned." Open requests #8188, #29053 still ask for it. Community projects `openclaw-mcp` / `openclaw-mcp-server` exist but go the wrong direction — they expose OpenClaw _as_ an MCP server for other clients, not help OpenClaw consume MCP tool servers.
- Approval workflow is **feasible** (corrected from earlier assessment):
  - `runtime.system.enqueueSystemEvent()` + `runtime.system.requestHeartbeatNow()` are both exposed to plugins (PR #19464)
  - This is the same code path `/hooks/wake` uses internally (`server/hooks.ts:28-34`)
  - A plugin can enqueue "approval granted" and force immediate heartbeat — no 30min wait
  - **Limitation**: this starts a new agent turn via `getReplyFromConfig()`, not a seamless resume of the blocked tool call. The agent gets the approval message in context and must retry the tool call. Workable but not as clean as n8n's Wait node or IronClaw's routines which natively suspend/resume

## Evaluation Dimensions

When comparing frameworks (OpenClaw, ZeroClaw, others), evaluate along these axes:

### Message Channels

- Telegram support
- Matrix support

### Triggers

- Cron support
- Generic trigger mechanism (e.g., webhooks)

### MCP Support

- Can you connect MCP servers to the agent to let it use their tools?
- Native or via workaround?

### Execution Environment

- What does the agent's shell execution environment look like?
- Can you inject environment variables (per session / per agent run / from plugin logic)?
- Where do commands execute? Same container? Can you run them in another k8s pod? What's the interface?
- Where does the agent store files? Can we sanely use a PVC?

### Asynchronous Execution / Tool Use

- Can the agent start a long-running call, be told it's running in background, take other actions meanwhile, and receive a wake-up/notification on finish?
- Is this only for shell execution, or more general (e.g., any tool call)?

### Extensibility

- Can you define custom tools?
- Can you unregister/register them dynamically, or disable/enable at runtime?
- Can your custom code/plugin deliver system notifications that either:
  - Get injected into agent context at next opportunity (if already running), or
  - Wake the agent up (if idle)?

### Basics

- Web UI that shows conversation(s)

### Nice to Have

- OAuth / Authentik integration

### Notes

Not expecting any single solution to have all of these. The subset that's available determines what's possible. Open to writing extension code but prefer minimal custom work.

## Candidates to Evaluate

### Tier 1: "Claw" Family (personal AI agent platforms)

These are all in the same direct lineage / competitive space as OpenClaw — self-hosted personal AI agents with messenger integrations and tool use.

- **OpenClaw** (current — see pros/cons above). TypeScript, 247k stars. The 800-pound gorilla. Telegram, Matrix (via `@openclaw/matrix` plugin, E2EE via Rust crypto SDK), WhatsApp, Slack, Discord, Signal, iMessage, etc. 5,700+ community skills via ClawHub. MCP SDK in dep tree but user-configurable MCP client not shipped (community bridges as workaround). Plugin API for runtime tool registration (no unregister API — disable plugin or use `tools.deny`). Sub-agent architecture for background tasks. Had a critical RCE (CVE-2026-25253). Creator joining OpenAI, project moving to foundation.
- **ZeroClaw** — Rust, single 3.4MB binary, sub-10ms cold start, <5MB RAM. Telegram, Discord, Slack, WhatsApp, iMessage, Matrix, webhooks. Deny-by-default channel policies. Optional Docker sandboxing for shell. 22+ AI providers.
- **NanoClaw** — ~500 lines TypeScript. Built on Anthropic Agent SDK. Runs agents in Linux containers (Apple Container / Docker). Telegram, WhatsApp, Slack, Discord, Gmail. Agent swarms. "Fork and customize" philosophy — not a framework, more a template.
- **IronClaw** — Rust, by NEAR AI (Llion Jones, Transformer co-author). Capability-based security (seL4-inspired). Every skill runs in WASM sandbox. MCP support. Dynamic tool building. Encrypted credential vault. Traffic inspection on outbound. 890 skills. TEE deployment on NEAR AI Cloud. 11.8k stars.
- **PicoClaw** — Go, single binary, runs on $10 RISC-V boards. Telegram, Discord, QQ, DingTalk. Cron scheduling. MCP support (v0.1.4+). Multi-agent. 12k stars. Ultra-lightweight but limited channel support (no Matrix).
- **Nanobot** (HKUDS) — ~4k lines Python. Telegram (recommended), Discord, WhatsApp, Slack, Matrix (added 2026-02-25), Email, etc. MCP support (v0.1.4+). Cron via natural language. 17.8k stars. Memory via MEMORY.md files. Very lightweight (45MB, 0.8s startup).

### Tier 2: Workflow/orchestration platforms (not agent-first, but capable)

- **n8n** — Visual workflow automation, 177k stars. AI Agent nodes + Telegram/Slack/Discord triggers. MCP client support. Human-in-the-loop approval for tool calls (new in 2026). Not an "agent" per se but can build agent-like flows. Self-hosted. Has a "Manager-Executor" pattern community template ("Agent One") for autonomous agents via Telegram.
- **Kagent** — Kubernetes-native agent framework by Solo.io (CNCF sandbox). Built on AutoGen. MCP support + A2A. Focused on DevOps/cloud-native tasks (Argo, Helm, Istio, k8s, Prometheus). Not a personal assistant — more an infra agent. Already partially deployed in our cluster.

### Tier 3: Memory / complementary (not standalone agents)

- **memU** — Not an agent itself, but a memory framework for always-on agents. Knowledge graph from conversations. Could complement any of the above.

### Not considered (too low-level)

- OpenAI Agents SDK, Anthropic Agent SDK, LangGraph, CrewAI, AutoGen, Google ADK — these are SDKs/frameworks for building agents from scratch, not batteries-included platforms.
- Botpress, LangBot, Flowise, Dify — more chatbot/flow builders than autonomous agent platforms.

## Detailed Evaluation by Dimension

### Message Channels

| Framework | Telegram                                                                                                                | Matrix                                                                                                                                                                                             | Other notable                                                                                                     |
| --------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✓ (full, including groups)                                                                                              | ✓ (via `@openclaw/matrix` plugin, E2EE via Rust crypto SDK, long-polling, no public URL needed)                                                                                                    | WhatsApp, Signal, Slack, Discord, iMessage, Teams, IRC, LINE, 20+ total                                           |
| ZeroClaw  | ✓ (long-polling Bot API, streaming, in-chat model switching, deny-by-default allowlist)                                 | ✓ (sync-based via `matrix-sdk` Rust lib, E2EE, no public endpoint needed, crypto state in `~/.zeroclaw/state/matrix/`, compiled via Cargo feature flag)                                            | 17+ total: WhatsApp, Signal, Slack, Discord, iMessage, Mattermost, IRC, Lark, DingTalk, QQ, Nostr, Email, webhook |
| NanoClaw  | Via `/add-telegram` skill (not built-in, must be added)                                                                 | ✗ (MicroClaw fork has it)                                                                                                                                                                          | WhatsApp (native), Slack, Discord, Gmail                                                                          |
| IronClaw  | ✓ (native WASM channel, webhook mode, pairing codes)                                                                    | ✗ — not natively supported. Available only via managed hosting (ironclaw.app) or OpenClaw bridges.                                                                                                 | Slack (WASM channel), HTTP webhooks, REPL/TUI (Ratatui), web gateway                                              |
| PicoClaw  | ✓ (long-polling, voice transcription via Groq Whisper)                                                                  | ✗                                                                                                                                                                                                  | Discord, QQ, DingTalk, LINE, WeCom                                                                                |
| Nanobot   | ✓ — first-class, recommended. `python-telegram-bot` long polling. Media groups, typing indicators, voice transcription. | ✓ (v0.1.4.post3, 2026-02-25). `matrix-nio` with E2EE, media uploads, typing keepalive, Markdown-to-HTML. Requires optional extras (`nio`, `nh3`, `mistune`). Sync loop as background asyncio task. | Discord, WhatsApp, Slack, Feishu, Email, 8+ total. Cross-channel messaging via `MessageTool`.                     |
| n8n       | ✓ (trigger node, but one webhook per bot limit)                                                                         | ✓ — built-in Matrix node (send messages, invite users, trigger on events)                                                                                                                          | Slack, Discord, email, SMS, 400+ service triggers                                                                 |

### Triggers

| Framework | Cron                                                                                                                                                                                                                                                                                                                                                                                                                 | Webhooks                                                                                                                                                                                                                                                    | Other                                                                                                                                                                                                                                                                                                                                                                      |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✓ — persistent jobs in `~/.openclaw/cron/`, at/interval/cron expressions, wakeMode "now" or "next-heartbeat", isolated or main session                                                                                                                                                                                                                                                                               | ✓ — `/hooks/wake` endpoint, Bearer or `x-openclaw-token` auth, SSRF-protected                                                                                                                                                                               | Heartbeat (default 30min)                                                                                                                                                                                                                                                                                                                                                  |
| ZeroClaw  | ✓ — cron expressions, RFC3339 timestamps, fixed intervals, one-shot delays. Persisted jobs.                                                                                                                                                                                                                                                                                                                          | ✓ — gateway at `127.0.0.1:3000` with `/hooks/wake` and `/hooks/agent`, Bearer token auth. Hookdeck integration for secure tunnels with retries.                                                                                                             | Heartbeat (30min default, reads HEARTBEAT.md). Daemon auto-restart with exponential backoff.                                                                                                                                                                                                                                                                               |
| NanoClaw  | ✓ — `task-scheduler.ts` for cron, interval, one-shot                                                                                                                                                                                                                                                                                                                                                                 | Host orchestrator handles IPC                                                                                                                                                                                                                               | Container-per-group isolation                                                                                                                                                                                                                                                                                                                                              |
| IronClaw  | ✓ — Routines Engine, cron schedules, event triggers, webhook handlers, AI-mediated (agent reasons about trigger)                                                                                                                                                                                                                                                                                                     | ✓ — HTTP webhook channel                                                                                                                                                                                                                                    | Routines run without active user session                                                                                                                                                                                                                                                                                                                                   |
| PicoClaw  | ✓ — one-time, recurring, cron expressions, jobs execute in agent's security context                                                                                                                                                                                                                                                                                                                                  | Single shared gateway HTTP server (`127.0.0.1:18790`) for webhook-based channels                                                                                                                                                                            | Heartbeat via HEARTBEAT.md                                                                                                                                                                                                                                                                                                                                                 |
| Nanobot   | ✓ — **verified (2026-03-05).** Three schedule types: `at` (one-shot), `every` (interval), `cron` (cron expr + timezone via `croniter`). Jobs persisted in `~/.nanobot/data/cron/jobs.json`. Agent can self-schedule via `CronTool`. Cron jobs call `agent.process_direct()` — full agent loop with tools. Results deliverable to specific channels. Safety: recursive cron scheduling blocked during cron execution. | ✗ — **verified: no HTTP server at all in gateway.** `MessageBus` is two `asyncio.Queue` instances (inbound/outbound), no HTTP endpoint. Adding a webhook would be trivial (~20 lines: HTTP server publishing `InboundMessage` to bus), but not implemented. | Heartbeat every 30min (configurable). Two-phase: Phase 1 reads `HEARTBEAT.md`, lightweight LLM call decides skip/run; Phase 2 runs full agent loop only if tasks found. Subagent spawning via `spawn` tool — asyncio tasks with own tool registry (no message/spawn tools to prevent recursion), results injected back to main agent via bus as `system` channel messages. |
| n8n       | ✓ — Schedule trigger node, cron expressions                                                                                                                                                                                                                                                                                                                                                                          | ✓ — first-class Webhook trigger node                                                                                                                                                                                                                        | Any of 400+ service triggers (GitHub, Stripe, etc.)                                                                                                                                                                                                                                                                                                                        |

### MCP Support

| Framework | Native MCP?                                   | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✗ — SDK in dep tree but not user-configurable | `@modelcontextprotocol/sdk` exists in codebase and `mcporter` tool uses MCP internally, but user-configurable MCP client (point at server, use tools) was never merged. PR #5121 closed, issue #4834 closed "not planned." Open requests #8188, #29053. Community projects (`openclaw-mcp`, `openclaw-mcp-server`) expose OpenClaw _as_ an MCP server — wrong direction, doesn't help consume MCP tools.                                                                                                                                                   |
| ZeroClaw  | ✗ — not implemented                           | **Verified by cloning repo (2026-03-05).** Zero MCP references in all 246 Rust source files — no `McpServer`, `McpClient`, `mcp_`, or `rmcp` crate in `Cargo.toml`. The only "MCP" mention in the entire repo is in `docs/providers-reference.md` describing **Osaurus** (a third-party provider), not ZeroClaw itself. Tool system is a custom Rust `Tool` trait (`src/tools/traits.rs`) with built-in registry — no external tool protocol support. Extension points are WASM modules and Composio (closed-source SaaS for OAuth integrations, not MCP). |
| NanoClaw  | ✓ — native via Agent SDK                      | Built on Anthropic Agent SDK with built-in MCP client. Config in `mcpServers` object in `src/index.ts` or `.mcp.json`. Supports stdio and HTTP transports. Can create custom in-process MCP servers via `createSdkMcpServer`. `/customize` skill for interactive MCP setup.                                                                                                                                                                                                                                                                                |
| IronClaw  | ✓ — first-class                               | `ironclaw tool install` supports both WASM tools (sandboxed) and MCP servers (any language, no sandbox). Both are first-class. Can also dynamically build tools from natural language descriptions.                                                                                                                                                                                                                                                                                                                                                        |
| PicoClaw  | ✗ — planned (issue #290)                      | Open GitHub issue #290 requests MCP support. Roadmap mentions it to transform PicoClaw into "AIEOS hub." Third-party `picoclaw-mcp-server` exists (LobeHub) but not native client support.                                                                                                                                                                                                                                                                                                                                                                 |
| Nanobot   | ✓ — **verified (2026-03-05)**                 | Native MCP client in `agent/tools/mcp.py`. Supports stdio (`StdioServerParameters`) and streamable HTTP (`streamable_http_client` with `httpx`). Tools auto-discovered via `session.list_tools()`, namespaced as `mcp_{server}_{tool}`. Lazy connection on first message via `AsyncExitStack`. Config in `tools.mcpServers` — Claude Desktop-compatible format. Configurable `toolTimeout` (default 30s).                                                                                                                                                  |
| n8n       | ✓ — native MCP client node                    | MCP Client tool node connects to MCP servers via SSE/streamable HTTP. Can also expose n8n workflows as MCP servers (MCP Server Trigger node). **Limitation**: no stdio transport — stdio-based MCP servers (like Claude Desktop tools) need a proxy like `mcp-remote`.                                                                                                                                                                                                                                                                                     |

### Execution Environment

| Framework | Shell model                                                                                                                                                                                                                                                       | Env var injection                                                                                                                                                                                                                                                      | Where commands run                                                                                                                                                       | File storage                                                                                                                                                                                                     |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | `sh -lc` (login shell)                                                                                                                                                                                                                                            | Global via `~/.openclaw/.env`, per-sandbox via `agents.defaults.sandbox.docker.env` (known bug #17923: injected at `docker exec` not `docker create`), `${VAR}` interpolation, `SecretRef` for external secrets. `OPENCLAW_HOME` overrides `$HOME` for full isolation. | Same host (unsandboxed) or ephemeral Docker container (sandboxed, no network by default, read-only root, 1GB mem limit, non-root user). No native k8s pod execution.     | `~/.openclaw/` for config/memory, `~/openclaw/workspace/` for agent workspace. In Docker: `/data` PV with `.openclaw/` + `workspace/`.                                                                           |
| ZeroClaw  | Two-container Docker: `zeroclaw-dev` (agent) + `sandbox` (exec). 60s timeout, 1MB output cap. Command allowlist (default: git, npm, cargo). Argument sanitizer blocks `--exec=`, metacharacters, backticks, pipes. High-risk (curl, wget) require approval.       | Env cleared before exec, only safe vars re-injected (PATH, HOME, TERM, LANG). `credential_profiles` inject auth headers from env. Config precedence: CLI > env > config.toml > defaults.                                                                               | Sandbox container by default (`tools.exec.host = sandbox`). If sandboxing off, runs on gateway host. `workspace_only = true` blocks `/etc`, `/root`, `~/.ssh`, `~/.aws`. | `~/.zeroclaw/workspace/`. Docker uses `zeroclaw-data` named volume for persistence.                                                                                                                              |
| NanoClaw  | Full shell inside container (Claude Code runs in container)                                                                                                                                                                                                       | Via container environment                                                                                                                                                                                                                                              | Isolated Linux container per group (Apple Container on macOS, Docker on Linux). Host orchestrator manages lifecycle.                                                     | Each group gets own mounted directory inside container                                                                                                                                                           |
| IronClaw  | Three-tier shell restriction: BLOCKED_COMMANDS (8 forbidden), DANGEROUS_PATTERNS (10, require `allow_dangerous`), NEVER_AUTO_APPROVE (10, force per-invocation approval). WASM sandbox for untrusted tools.                                                       | Encrypted vault (AES-256-GCM), injected at host boundary ms before execution, never in LLM context                                                                                                                                                                     | WASM sandbox for tools. MCP servers run as separate processes. PostgreSQL for persistent state.                                                                          | PostgreSQL with AES-256-GCM encrypted secrets                                                                                                                                                                    |
| PicoClaw  | Sandboxed to workspace by default, `deny_patterns`/`allow_patterns` for command filtering                                                                                                                                                                         | Via config                                                                                                                                                                                                                                                             | Same process, workspace-scoped                                                                                                                                           | `~/.picoclaw/workspace/` (sessions, memory, state, cron, skills)                                                                                                                                                 |
| Nanobot   | **Verified (2026-03-05).** `ExecTool` (`agent/tools/shell.py`) runs subprocesses directly. `restrict_to_workspace` confines to workspace. Max 40 tool iterations (configurable). Config is `config.json` (pydantic-settings, env prefix `NANOBOT_`, nested `__`). | Env vars via pydantic-settings `NANOBOT_PROVIDERS__*`. `path_append` in `tools.exec` extends PATH. MCP server configs pass env to stdio server processes.                                                                                                              | Same process/container as nanobot. No remote sandbox. No Firecracker/microVM. Global `_processing_lock` — one message at a time, but subagents run concurrently.         | `~/.nanobot/` (config.json, sessions as JSONL, cron jobs.json). `~/.nanobot/workspace/` (SOUL.md, USER.md, AGENTS.md, memory/MEMORY.md, skills/). Memory consolidation writes summaries to MEMORY.md/HISTORY.md. |
| n8n       | Execute Command node (disabled by default in n8n 2.0, must re-enable via `NODES_EXCLUDE="[]"`), Code node (JS/Python, isolated task runners, module imports blocked by default)                                                                                   | Via workflow variables, environment variables. `N8N_BLOCK_ENV_ACCESS_IN_NODE=true` by default blocks Code node from reading env vars.                                                                                                                                  | Inside n8n container/process. Queue mode for async workers. For custom tools not in Alpine image, build custom Docker image.                                             | n8n database (SQLite/PostgreSQL), binary data in filesystem, external storage (enterprise)                                                                                                                       |

### Asynchronous Execution / Tool Use

| Framework | Async/background support                                                                                                                                                                                                                                                                                                                                                                                                              | Scope                                                                 | Wake-up mechanism                                                                                                                                                                                             |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✓ — sub-agents for background/parallel work. `async-task start` returns immediately. Wake triggers can notify on completion. Lane-based command queue processes ops serially within a session, but multiple sessions/sub-agents run concurrently. Plugins can force immediate heartbeat via `runtime.system.requestHeartbeatNow()` (#19464).                                                                                          | General — sub-agents can run any tools. Multiple concurrent sessions. | `enqueueSystemEvent()` + `requestHeartbeatNow()` exposed to plugins — same code path as `/hooks/wake`. Starts new agent turn (not seamless tool-call resume).                                                 |
| ZeroClaw  | Tokio async runtime. `subagent_spawn`/`subagent_list`/`subagent_manage` tools with `max_concurrent = 24` and load-balancing. Each subsystem in own tokio task. **But**: per-tool-call background-with-callback not documented — shell exec appears synchronous within 60s timeout. Subagent delegation is agent-level, not per-tool-call.                                                                                             | Agent-level via subagents. Individual tool calls appear synchronous.  | Webhooks (`/hooks/wake`) can wake agent. Heartbeat reads HEARTBEAT.md every 30min. mpsc channel pipeline for internal events.                                                                                 |
| NanoClaw  | Container-per-group provides isolation. Host orchestrator manages lifecycle.                                                                                                                                                                                                                                                                                                                                                          | Per-container                                                         | IPC between host and container                                                                                                                                                                                |
| IronClaw  | ✓ — parallel jobs in isolated contexts, priority-aware scheduler. Self-repair for stuck operations.                                                                                                                                                                                                                                                                                                                                   | General (any tool/routine)                                            | Automatic detection and recovery of stuck operations                                                                                                                                                          |
| PicoClaw  | ✓ — subagent spawning for long tasks. Heartbeat reads HEARTBEAT.md, spawns subagent that works independently.                                                                                                                                                                                                                                                                                                                         | Subagent has access to all tools, can message user independently      | Subagent communicates back via message tool                                                                                                                                                                   |
| Nanobot   | **Verified (2026-03-05).** `SubagentManager` spawns `asyncio.Task` objects with own `ToolRegistry` (filesystem, shell, web — no message/spawn to prevent recursion). Max 15 iterations per subagent. Results injected back to main agent via `bus.publish_inbound(InboundMessage(channel="system"))`. **But**: no per-tool-call background-with-callback. Tool calls within each loop iteration are synchronous (call → wait → next). | Agent-level via subagents. Individual tool calls synchronous.         | Subagent results arrive as `system` channel `InboundMessage`. Heartbeat polls every 30min. Cron fires on schedule. No event-driven wake-up from idle. No webhook-triggered wake — gateway has no HTTP server. |
| n8n       | ✓ — Wait node (offloads to DB for waits >65s, frees execution slot), async sub-workflows (Execute Workflow without waiting, callback via `$execution.resumeUrl`), "Async Portal" pattern (pause at checkpoints, external events resume via session ID). Human-in-the-loop approval for AI Agent tool calls. Chat node "send message and wait for response."                                                                           | General — any workflow step                                           | Webhook callback, manual approval node, Wait node resume, polling                                                                                                                                             |

### Extensibility

| Framework | Custom tools                                                                                                                                                                                                                                                                                                                                                                                                     | Dynamic registration                                                                                                                                                                                           | System notifications / wake-up                                                                                                                                                                                                                                                                                                                                                |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✓ — Skills (markdown, 5,700+ on ClawHub) + Plugins (TS/JS, run in Gateway). Plugins can `api.registerTool()`, `api.registerProvider()`, `api.registerHttpHandler()`, add CLI commands. Tool allow/deny lists via `tools.allow`/`tools.deny`.                                                                                                                                                                     | `api.registerTool()` at runtime — but **no `unregisterTool()`**. To remove tools: disable the plugin entirely, or use `tools.deny` config. No hot-reload of individual tools. Skills loaded at startup.        | Plugins get `runtime.system.enqueueSystemEvent()` + `runtime.system.requestHeartbeatNow()` (#19464) — same primitives as `/hooks/wake`. Can force immediate agent turn with injected context.                                                                                                                                                                                 |
| ZeroClaw  | Rust `Tool` trait + WASM tools (Cargo feature flag) + FFI (C ABI via `cbindgen`). Skill packs via TOML manifests. Tool allowlisting. Security audit on install. Python SDK (`zeroclaw` pip) with `create_agent`/`ainvoke()`.                                                                                                                                                                                     | Config-driven registration, hot-reloadable (zero-downtime). **But**: dynamic runtime tool injection via API not documented — appears config-only.                                                              | Pushover (outbound). Webhooks `/hooks/wake` (inbound). mpsc channel pipeline. **But**: no documented API for injecting arbitrary system messages into agent context mid-conversation.                                                                                                                                                                                         |
| NanoClaw  | ✓ — Skills as Claude Code commands (e.g., `/add-telegram`). Fork-and-customize model.                                                                                                                                                                                                                                                                                                                            | Not dynamic — you modify the codebase                                                                                                                                                                          | Host-to-container IPC                                                                                                                                                                                                                                                                                                                                                         |
| IronClaw  | ✓ — WASM tools (sandboxed) + MCP servers + dynamic tool building from natural language                                                                                                                                                                                                                                                                                                                           | Tools installable at runtime via `ironclaw tool install`. Routines Engine for event-driven execution.                                                                                                          | Routines Engine handles event triggers. Priority-aware scheduler.                                                                                                                                                                                                                                                                                                             |
| PicoClaw  | ✓ — Skills in `skills/` directory, tool descriptions in `TOOLS.md`                                                                                                                                                                                                                                                                                                                                               | Loaded from workspace                                                                                                                                                                                          | Heartbeat mechanism via HEARTBEAT.md                                                                                                                                                                                                                                                                                                                                          |
| Nanobot   | **Verified (2026-03-05).** `Tool` ABC with `name`, `description`, `parameters` (JSON Schema), `execute(**kwargs)`. `ToolRegistry` is a simple `dict[str, Tool]`. Skills as markdown (`skills/*/SKILL.md`). MCP servers add tools without code changes. Default tools: `read_file`, `write_file`, `edit_file`, `list_dir`, `exec`, `web_search`, `web_fetch`, `message`, `spawn`, `cron`. ~4k lines Python total. | MCP tools lazy-loaded on first message. `ContextBuilder` re-reads workspace files (SOUL.md, AGENTS.md, MEMORY.md) every prompt — writing to these is indirect context injection. Skills modifiable at runtime. | `MessageBus` is two `asyncio.Queue` instances — trivially simple. Cron uses `process_direct()` (bypasses bus). Heartbeat also uses `process_direct()`. Subagents inject via `bus.publish_inbound()`. **No external API for injecting events** — no HTTP server, no webhook. Any code with bus access can call `publish_inbound()` to inject, but there's no network exposure. |
| n8n       | ✓ — any n8n node can be a tool (400+ integrations). Custom code nodes (JS/Python). MCP client. Sub-workflows as tools.                                                                                                                                                                                                                                                                                           | Workflows are the unit of composition — enable/disable at will.                                                                                                                                                | Human-in-the-loop approval node. Webhook callbacks. Chat triggers.                                                                                                                                                                                                                                                                                                            |

### Web UI

| Framework | Web UI       | Details                                                                                                                                                                                                           |
| --------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✓            | Vite + Lit SPA at `:18789/`. Chat, sessions, config, nodes, logs, skills, canvas (agent-driven dashboards). Device pairing approval. Community dashboards (cost tracking, cron mgmt).                             |
| ZeroClaw  | ✓            | Built-in React frontend at `127.0.0.1:3000` via `zeroclaw gateway`. Also: third-party desktop app (ZeroClaw UI) with streaming logs, doctor diagnostics, channel health, chat, config editor for all 29 sections. |
| NanoClaw  | ✗            | CLI + messenger-based interaction only.                                                                                                                                                                           |
| IronClaw  | ✓            | Web gateway with SSE/WebSocket streaming. Browser-based chat UI.                                                                                                                                                  |
| PicoClaw  | ✗            | CLI + messenger-based interaction only.                                                                                                                                                                           |
| Nanobot   | ✗ (official) | CLI + messenger-based only. Third-party community project `nanobot-ui` (SunnyChopper) exists but not maintained by HKUDS.                                                                                         |
| n8n       | ✓            | Full workflow editor, execution history, credential management, AI agent chat interface. Most polished UI of all candidates.                                                                                      |

### OAuth / Authentik

| Framework | OAuth/OIDC support                                                                                                                                                                                                     |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenClaw  | ✗ — no native OIDC for gateway auth (open request #4642). Workaround: trusted proxy auth (nginx + oauth2-proxy, Traefik + forward auth, Caddy, Pomerium). OAuth for model providers (token refresh, multi-account).    |
| ZeroClaw  | ⚠ Composio integration for 1000+ OAuth app integrations (closed-source SaaS backend — SDK is open but server is proprietary, no self-host option). Auth profiles stored encrypted. No OIDC for platform auth.          |
| NanoClaw  | ✗                                                                                                                                                                                                                      |
| IronClaw  | ✗ — encrypted credential vault, no documented OIDC/Authentik integration.                                                                                                                                              |
| PicoClaw  | ✗                                                                                                                                                                                                                      |
| Nanobot   | ⚠ Outbound OAuth only: provider login (`nanobot provider login`), MCP OAuth 2.0 with PKCE (auto on 401). No inbound OIDC for user auth — uses `allowFrom` lists with platform user IDs. Token persistence bug (#1023). |
| n8n       | ✓ — native OIDC/SAML SSO (but requires Startup license at $400/mo). Free alternative: `n8n-oidc` plugin works with Authentik, Keycloak, etc. via env vars.                                                             |

## Summary / Shortlist

**For our use case** (Telegram + Matrix, MCP tools, approval workflow with wake-up, k8s deployment, Authentik):

- **IronClaw** — strongest on MCP (native, first-class), security (WASM sandbox), and async (parallel jobs, self-repair, routines engine with event triggers). Weakest on Matrix (unconfirmed). Routines Engine could solve the approval wake-up problem.
- **Nanobot** — **verified by source audit (2026-03-05).** ~4k lines Python, very readable. Has both Telegram and Matrix, confirmed MCP (stdio + streamable HTTP, Claude Desktop-compatible config, lazy connection). Background processing: cron (3 schedule types, agent can self-schedule, results deliverable to channels), heartbeat (two-phase: LLM decides skip/run, then full agent loop), subagents (asyncio tasks, results injected back via bus). **But**: no HTTP server at all in gateway (no webhooks — adding one is ~20 lines but not there today), no approval workflow, no per-tool-call async callbacks, weak security (workspace sandboxing only), no inbound OIDC, no official web UI. Easiest codebase to fork and extend.
- **ZeroClaw** — has both Telegram and Matrix (with E2EE), two-container sandbox model, subagent system (max 24 concurrent), hot-reloadable config, React web UI + desktop app. **No MCP support** (verified by source audit 2026-03-05 — custom `Tool` trait only, extension via WASM + Composio closed-source SaaS). Rust codebase harder to extend (though Python SDK exists). No API for mid-conversation context injection.
- **n8n** — best UI, native MCP (SSE only, no stdio), human-in-the-loop approval (exactly what we need!), Authentik via `n8n-oidc`, built-in Matrix node. But it's a workflow engine, not an autonomous agent — building an agent-like experience requires significant workflow design.
- **OpenClaw** — broadest channel support, rich plugin API (register but no unregister), sub-agents for background work. MCP not actually user-configurable (community bridges only). Approval workflow **feasible**: plugins can `enqueueSystemEvent()` + `requestHeartbeatNow()` (same path as `/hooks/wake`, PR #19464) — starts new agent turn, not seamless tool-call resume. Gateway auth is token-only (OIDC via reverse proxy workaround).
