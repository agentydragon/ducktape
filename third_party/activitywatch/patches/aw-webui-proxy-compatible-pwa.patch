diff --git a/vite.config.js b/vite.config.js
index 1d2e63a..8a5e119 100644
--- a/vite.config.js
+++ b/vite.config.js
@@ -48,6 +48,10 @@ export default defineConfig(({ mode }) => {
       autoInject(),
       vue(),
       VitePWA({
+        // The UI is behind an Authentik proxy, so its manifest needs the session cookie.
+        useCredentials: true,
+        // aw-server exposes stable root assets; avoid a hash-named Workbox sidecar.
+        workbox: { inlineWorkboxRuntime: true },
         devOptions: {
           enabled: true,
         },
@@ -86,7 +90,7 @@ export default defineConfig(({ mode }) => {
     },
     define: {
       PRODUCTION,
       AW_SERVER_URL: process.env.AW_SERVER_URL,
-      COMMIT_HASH: process.env.COMMIT_HASH,
+      COMMIT_HASH: JSON.stringify(process.env.COMMIT_HASH),
       'process.env.VUE_APP_ON_ANDROID': process.env.VUE_APP_ON_ANDROID,
     },
   };
