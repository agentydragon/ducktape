Probably should have:

- Teasers
- MathJax

Maybe one day:

- Public resume
- Blog post: "Running a multi-master git forge on cheap OVH boxes" — write up the
  git-storage latency & HA benchmark (Forgejo RWX vs GitLab Gitaly Cluster across
  rook/CephFS and SeaweedFS on SSD/HDD). Source + data: `debug/git_storage_latency_and_ha.md`.
