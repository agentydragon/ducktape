diff -u a/dist/extensions/memory-core/manager-runtime.js b/dist/extensions/memory-core/manager-runtime.js
--- a/dist/extensions/memory-core/manager-runtime.js
+++ b/dist/extensions/memory-core/manager-runtime.js
@@ -326,10 +326,13 @@
 const RETRYABLE_MEMORY_EMBEDDING_SERVICE_ERROR_RE = /\b5\d\d\b|cloudflare/i;
 const RETRYABLE_MEMORY_EMBEDDING_TRANSPORT_ERROR_RE = /(fetch failed|other side closed|ECONNRESET|ECONNREFUSED|ETIMEDOUT|EPIPE|UND_ERR_|socket hang up|socket terminated|network error|read ECONN|timed out|connection (?:reset|refused|aborted|timed out)|EHOSTUNREACH|ENETUNREACH|ECONNABORTED|EAI_AGAIN)/i;
 const SPLITTABLE_MEMORY_EMBEDDING_BATCH_ERROR_RE = /(request_headers_too_large|request header fields too large|other side closed|ECONNRESET|EPIPE|UND_ERR_SOCKET|socket hang up|socket terminated|read ECONN|connection (?:reset|aborted)|\bembeddings (?:api input limit exceeded:\s*max\s+\d+\s*,\s*got\s+\d+|max input length is\s+\d+)\b|\bbatch size is invalid,?\s+it should not be larger than\s+\d+\b)/i;
+const OPENCLAW_MEMORY_RETRY_BASE_DELAY_MS = resolvePositiveIntegerEnvironmentVariable("OPENCLAW_MEMORY_RETRY_BASE_DELAY_MS", 500);
+const OPENCLAW_MEMORY_RETRY_MAX_DELAY_MS = resolvePositiveIntegerEnvironmentVariable("OPENCLAW_MEMORY_RETRY_MAX_DELAY_MS", 8e3);
+if (OPENCLAW_MEMORY_RETRY_MAX_DELAY_MS < OPENCLAW_MEMORY_RETRY_BASE_DELAY_MS) throw new Error("OPENCLAW_MEMORY_RETRY_MAX_DELAY_MS must be at least OPENCLAW_MEMORY_RETRY_BASE_DELAY_MS");
 const SHORT_MEMORY_EMBEDDING_RETRY_BUDGET = {
 	attempts: 3,
-	baseDelayMs: 500,
-	maxDelayMs: 8e3
+	baseDelayMs: OPENCLAW_MEMORY_RETRY_BASE_DELAY_MS,
+	maxDelayMs: OPENCLAW_MEMORY_RETRY_MAX_DELAY_MS
 };
 const MEMORY_EMBEDDING_RETRY_PROFILES = {
 	index: {
@@ -2673,7 +2676,14 @@
 const EMBEDDING_CACHE_TABLE = MEMORY_EMBEDDING_CACHE_TABLE;
 const EMBEDDING_CACHE_PRUNE_BATCH_SIZE = 100;
 const EMBEDDING_BATCH_MAX_TOKENS = 8e3;
-const EMBEDDING_INDEX_CONCURRENCY = 4;
+function resolvePositiveIntegerEnvironmentVariable(name, fallback) {
+	const raw = process.env[name]?.trim();
+	if (!raw) return fallback;
+	const value = Number(raw);
+	if (!Number.isSafeInteger(value) || value <= 0) throw new Error(`${name} must be a positive integer`);
+	return value;
+}
+const EMBEDDING_INDEX_CONCURRENCY = resolvePositiveIntegerEnvironmentVariable("OPENCLAW_MEMORY_INDEX_CONCURRENCY", 4);
 const EMBEDDING_QUERY_TIMEOUT_REMOTE_MS = 6e4;
 const EMBEDDING_QUERY_TIMEOUT_LOCAL_MS = 3e5;
 const EMBEDDING_BATCH_TIMEOUT_REMOTE_MS = 12e4;
diff -u a/dist/missing-configured-plugin-install-FlzguTw_.mjs b/dist/missing-configured-plugin-install-FlzguTw_.mjs
--- a/dist/missing-configured-plugin-install-FlzguTw_.mjs
+++ b/dist/missing-configured-plugin-install-FlzguTw_.mjs
@@ -333,7 +333,7 @@
 		return !knownIds.has(pluginId) && !hasRecord && !bundledPluginsById.has(pluginId) || hasRecord && !bundledPluginsById.has(pluginId) && isPayloadMissing(env, records[pluginId]?.installPath);
 	}));
 	const installCandidatePluginIds = /* @__PURE__ */ new Set([...missingPluginIds, ...officialReplacementPluginIds]);
-	for (const candidate of collectDownloadableInstallCandidates({
+	if ((params.env ?? process.env).OPENCLAW_NIX_MODE !== "1") for (const candidate of collectDownloadableInstallCandidates({
 		cfg: params.cfg,
 		env,
 		missingPluginIds: installCandidatePluginIds,
@@ -608,7 +608,7 @@
 		return !knownIds.has(pluginId) && !hasRecord && !bundledPluginsById.has(pluginId) || hasRecord && !bundledPluginsById.has(pluginId) && isPayloadMissing(env, nextRecords[pluginId]?.installPath);
 	}));
 	const installCandidatePluginIds = /* @__PURE__ */ new Set([...missingPluginIds, ...officialReplacementPluginIds]);
-	for (const candidate of collectDownloadableInstallCandidates({
+	if ((params.env ?? process.env).OPENCLAW_NIX_MODE !== "1") for (const candidate of collectDownloadableInstallCandidates({
 		cfg: params.cfg,
 		env,
 		missingPluginIds: installCandidatePluginIds,
diff -u a/dist/openclaw-agent-db-fItexY2B.mjs b/dist/openclaw-agent-db-fItexY2B.mjs
--- a/dist/openclaw-agent-db-fItexY2B.mjs
+++ b/dist/openclaw-agent-db-fItexY2B.mjs
@@ -14,7 +14,7 @@
 import { n as enableNodeSqliteKyselyStatementCache, t as clearNodeSqliteKyselyCacheForDatabase } from "./kysely-sync-cache-state-Z6YwXUsn.mjs";
 import { i as getNodeSqliteKysely, n as executeSqliteQuerySync } from "./kysely-sync-CrjZjQJR.mjs";
 import { c as supportsNodeSqliteExtensionLoading, t as openNodeSqliteDatabase } from "./node-sqlite-B7YCpTW3.mjs";
-import { a as runSqliteIntegrityOperationSync, i as isTerminalSqliteIntegrityError, o as sqliteIntegrityCheckSteps, r as confirmSqliteFileIntegrity, t as assertSqliteIntegrity } from "./sqlite-integrity-CmJEy1JT.mjs";
+import { a as runSqliteIntegrityOperationSync, i as isTerminalSqliteIntegrityError, o as sqliteIntegrityCheckSteps, r as confirmSqliteFileIntegrity, v as assertSqliteQuickIntegrity, t as assertSqliteIntegrity } from "./sqlite-integrity-CmJEy1JT.mjs";
 import { d as clearOpenClawDatabaseQuarantine, f as createOpenClawDatabaseVerificationError, h as createSqliteTerminalOpenLatch, p as readOpenClawDatabaseQuarantine } from "./openclaw-state-db-cache-BXmHZWzx.mjs";
 import { o as OPENCLAW_SQLITE_BUSY_TIMEOUT_MS } from "./openclaw-state-db-contract-C8vwd-Ud.mjs";
 import { t as VERSION } from "./version-DXOj91SV.mjs";
@@ -2083,14 +2083,26 @@
 	});
 	const hasPendingCurrentVersionMigration = userVersion === 19 && (hasPendingMemoryChunkMetadataMigration(database) || hasPendingSessionKeyContractSchemaMigration(database) || hasRetiredAgentStateLeaseSchema(database) || hasPendingSessionConversationRouteContextColumn(database) || hasPendingSessionTranscriptContextEligibilityColumn(database) || hasPendingInputConsumptionColumnMigration(database) || hasPendingSessionProjectColumn(database));
 	if (userVersion === 19 && !hasPendingCurrentVersionMigration) {
-		yield* verifyAndRepairCanonicalSqliteIndexSteps(database, pathname, OPENCLAW_AGENT_SCHEMA_SQL, {
+		const configuredIntegrityCheck = process.env.OPENCLAW_AGENT_DB_STARTUP_INTEGRITY_CHECK?.trim().toLowerCase() || "full";
+		if (configuredIntegrityCheck !== "full" && configuredIntegrityCheck !== "quick" && configuredIntegrityCheck !== "none") throw new Error("OPENCLAW_AGENT_DB_STARTUP_INTEGRITY_CHECK must be full, quick, or none");
+		const indexRepairOptions = {
 			allowMissingColumns: true,
 			validateAfterRepair: () => assertOpenClawAgentCurrentRuntimeSchema(database, {
 				agentId,
 				pathname
-			}),
+			})
+		};
+		if (configuredIntegrityCheck === "full") yield* verifyAndRepairCanonicalSqliteIndexSteps(database, pathname, OPENCLAW_AGENT_SCHEMA_SQL, {
+			...indexRepairOptions,
 			diagnostics
 		});
+		else {
+			if (configuredIntegrityCheck === "quick") assertSqliteQuickIntegrity(database, pathname);
+			repairCanonicalSqliteIndexes(database, pathname, OPENCLAW_AGENT_SCHEMA_SQL, {
+				...indexRepairOptions,
+				verifyPhysicalIntegrity: false
+			});
+		}
 		assertOpenClawAgentCurrentRuntimeSchema(database, {
 			agentId,
 			pathname
diff -u a/dist/openclaw-database-verify-CVkK3KtV.mjs b/dist/openclaw-database-verify-CVkK3KtV.mjs
--- a/dist/openclaw-database-verify-CVkK3KtV.mjs
+++ b/dist/openclaw-database-verify-CVkK3KtV.mjs
@@ -13,7 +13,19 @@
 import { fork } from "node:child_process";
 //#region src/state/openclaw-database-verify.impl.ts
 const OPENCLAW_DATABASE_VERIFY_INITIAL_DELAY_MS = 3e5;
-const OPENCLAW_DATABASE_VERIFY_INTERVAL_MS = 864e5;
+function resolveOpenClawDatabaseVerifyInterval() {
+	const raw = process.env.OPENCLAW_DATABASE_VERIFY_INTERVAL_MS?.trim();
+	if (!raw) return 864e5;
+	const value = Number(raw);
+	if (!Number.isSafeInteger(value) || value <= 0) throw new Error("OPENCLAW_DATABASE_VERIFY_INTERVAL_MS must be a positive integer");
+	return value;
+}
+const OPENCLAW_DATABASE_VERIFY_INTERVAL_MS = resolveOpenClawDatabaseVerifyInterval();
+function resolveOpenClawDatabaseVerifyEnabled() {
+	const raw = process.env.OPENCLAW_DATABASE_VERIFY?.trim().toLowerCase() || "on";
+	if (raw !== "on" && raw !== "off") throw new Error("OPENCLAW_DATABASE_VERIFY must be on or off");
+	return raw === "on";
+}
 const log$1 = createSubsystemLogger("state/database-verify");
 const DATABASE_VERIFY_CHILD_ARG = "--openclaw-database-verify-child";
 function isVerifyResult(value) {
@@ -223,7 +235,8 @@
 			if (!stopped) schedule(OPENCLAW_DATABASE_VERIFY_INTERVAL_MS);
 		}
 	};
-	schedule(OPENCLAW_DATABASE_VERIFY_INITIAL_DELAY_MS);
+	if (resolveOpenClawDatabaseVerifyEnabled()) schedule(OPENCLAW_DATABASE_VERIFY_INITIAL_DELAY_MS);
+	else log.info("database integrity verifier disabled by OPENCLAW_DATABASE_VERIFY=off");
 	return { stop: async () => {
 		stopped = true;
 		if (timer) {
diff -u a/dist/sqlite-integrity-CmJEy1JT.mjs b/dist/sqlite-integrity-CmJEy1JT.mjs
--- a/dist/sqlite-integrity-CmJEy1JT.mjs
+++ b/dist/sqlite-integrity-CmJEy1JT.mjs
@@ -201,6 +201,11 @@
 	runSqliteForeignKeyCheck(database, databaseLabel);
 	return { integrityCheck };
 }
+function assertSqliteQuickIntegrity(database, databaseLabel) {
+	const integrityCheck = runSqliteCheck(database, databaseLabel, "quick_check");
+	runSqliteForeignKeyCheck(database, databaseLabel);
+	return { integrityCheck };
+}
 /** Run integrity checks and preserve whether a failure proves persistent damage. */
 function confirmSqliteIntegrity(database, databaseLabel) {
 	try {
@@ -350,4 +355,4 @@
 	return `${violation.table} ${row} references ${violation.parent} (foreign key ${violation.fkid.toString()})`;
 }
 //#endregion
-export { runSqliteIntegrityOperationSync as a, readStableSqliteFileGeneration as c, copyFileHandle as d, hashFileDescriptorSync as f, isTerminalSqliteIntegrityError as i, sameSqliteFileGeneration as l, assertSqliteTableIntegrity as n, sqliteIntegrityCheckSteps as o, sameFileMutationFingerprint as p, confirmSqliteFileIntegrity as r, parseSqliteFileGeneration as s, assertSqliteIntegrity as t, serializeSqliteFileGeneration as u };
+export { runSqliteIntegrityOperationSync as a, readStableSqliteFileGeneration as c, copyFileHandle as d, hashFileDescriptorSync as f, isTerminalSqliteIntegrityError as i, sameSqliteFileGeneration as l, assertSqliteTableIntegrity as n, sqliteIntegrityCheckSteps as o, sameFileMutationFingerprint as p, confirmSqliteFileIntegrity as r, parseSqliteFileGeneration as s, assertSqliteQuickIntegrity as v, assertSqliteIntegrity as t, serializeSqliteFileGeneration as u };
