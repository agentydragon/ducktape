/spawn Build a markdown link checker CLI tool called 'linkcheck' that finds and validates all HTTP/HTTPS links in markdown files. Requirements: parallel checking with asyncio, smart caching to avoid rechecking same URLs, retry logic for failures, progress indicator, multiple output formats (text/JSON/markdown), handles redirects/timeouts/rate-limiting. CLI should support checking single files or directories recursively with options like --timeout, --max-concurrent, --output. Include comprehensive tests and documentation.
