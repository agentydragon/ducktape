load("@rules_python//python:defs.bzl", "py_library")
load("//devinfra/python:defs.bzl", "py_binary")

py_library(
    name = "probe_models",
    srcs = ["probe_models.py"],
    deps = [
        "//cluster/cdk8s:litellm_config",
        "//util/bazel:runfiles",
        "@pypi//httpx",
        "@pypi//jinja2",
        "@pypi//pydantic",
        "@pypi//pyyaml",
    ],
)

py_binary(
    name = "probe_models_bin",
    data = ["report.html.j2"],
    main_module = "debug.litellm_probe.probe_models",
    deps = [":probe_models"],
)
