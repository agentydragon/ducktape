# Already built — reuse directly

> Historical inventory: the OpenClaw operator and OpenShell paths in this table
> were retired in 2026-08. See
> <../../../cluster/archive/2026_08_openclaw_namespace_retirement.md>.

| Requirement                          | What exists                                                                                          | Where                                                                                                                  |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| C5 (egress control)                  | mitmproxy FQDN allowlist (Kyverno-injected CA) — and OpenClaw's gateway is already forced through it | `cluster/k8s/agents/haku-egress-proxy/`, `haku-zones-mitmproxy/`, `openclawinstance.yaml` `security.networkPolicy`     |
| C5 (L7, per-sandbox)                 | OpenShell terminates TLS per-sandbox with an ephemeral CA; host/path/protocol rules, default-deny    | `OpenShellPolicy` CRD; <https://docs.nvidia.com/openshell/reference/policy-schema>                                     |
| C6 (LLM-level rollouts)              | OpenClaw writes per-turn JSONL transcripts (system prompt, response, tool calls/results)             | <https://docs.openclaw.ai/logging>; gateway PVC per `openclawinstance.yaml` `storage.persistence`                      |
| C3 (Codex token renewal)             | Handled — LiteLLM rotates the OAuth token on a PVC; CLIProxyAPI refreshes independently              | `cluster/k8s/litellm/app/chatgpt-deployment.yaml`                                                                      |
| C3 (Codex subscription via LiteLLM)  | Two parallel paths already wired: native `chatgpt/` provider, and CLIProxyAPI                        | `cluster/k8s/litellm/app/test_litellm_config.py:65-92,145-159`                                                         |
| H3-ish (OpenClaw exec sandboxing)    | OpenClaw ran its exec plugin through OpenShell in this cluster, not hypothetically                   | Retired deployment; see the archive note above                                                                         |
| B1 (name-length bug)                 | **Resolved and verified live at the time** — the shim worked in OpenShell mirror mode                | Retired deployment; see the archive note above                                                                         |
| C7 (declarative sandbox policy)      | Sandbox policy is a GitOps-owned ConfigMap passed via `--policy`, not inherited from a mutable image | `cluster/k8s/agents/openshell/openclaw/policy.yaml`                                                                    |
| W1 (credential proxy)                | OpenShell does credential substitution natively at its L7 proxy (placeholders → real secrets)        | see C5 below; <https://docs.nvidia.com/openshell/reference/policy-schema>                                              |
| B4 (OpenClaw operator)               | A real Helm-based operator (`openclaw-operator` chart) was deployed, not just CRD YAML by hand       | Retired deployment; see the archive note above                                                                         |
| B4 (OpenShell operator)              | `openshell-operator` Helm chart, delegates its k8s primitives to `kubernetes-sigs/agent-sandbox`     | `cluster/k8s/agents/openshell/operator/helmrelease.yaml`; upstream: <https://github.com/kubernetes-sigs/agent-sandbox> |
| P3 (public-coder platform survey)    | An almost directly-applicable survey already exists in-repo                                          | `docs/self_hosted_coding_agent_platforms.md`                                                                           |
| H4/K1-ish (SandboxTemplate/WarmPool) | Haku already uses `kubernetes-sigs/agent-sandbox` CRDs for pooled, ephemeral exec workspaces         | `cluster/k8s/haku/workspaces/app/sandboxtemplate-haku.yaml`, `sandboxwarmpool-haku.yaml`                               |
| C9 (overlong tool output)            | Already surveyed across candidate platforms; OpenClaw specifically verified (see below)              | `docs/self_hosted_coding_agent_platforms.md` §"Tool-output robustness"; OpenClaw detail in "public coder" section      |
