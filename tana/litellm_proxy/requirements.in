# Tana LiteLLM proxy appliance runtime.
#
# Keep this separate from the repo-wide pyproject.toml lock. LiteLLM's proxy
# extra owns exact pins for packages such as rich and boto3 that conflict with
# unrelated monorepo dependencies.
litellm[proxy]==1.82.0

# Local custom provider code imports httpx directly.
httpx>=0.27.0

# The cluster config enables LiteLLM's langfuse_otel callback.
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
opentelemetry-exporter-otlp-proto-http>=1.20

# LiteLLM proxy startup imports these when loading server settings/metrics.
prometheus-client
pydantic-settings>=2.0.0
